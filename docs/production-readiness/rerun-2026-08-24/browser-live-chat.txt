Fresh live-browser rerun evidence, 24 Aug 2026:

Opened http://localhost/chat/ in My Browser after Docker recovered. The page loaded as “Chat with the Guru — AskMukthiGuru” and rendered the sidebar, conversation history, language selector, response-preferences control, sources control, composer, and voice control. The UI displayed a “Guru is waking up” slow-response notice.

Submitted the harmless prompt “What is stillness?” through the rendered textarea. The browser created a new turn and displayed the user message, AI-generated state, Thinking state, a live search status for Ekam, a stop control, and a daily-teaching opt-in prompt. This confirms the browser-to-backend request path begins and produces streaming/progress UI in the current local stack. A completed final answer, citations, and final grounding state were not captured before this checkpoint; do not treat this as completed answer-quality evidence.

The pre-existing conversation visible before submission included a “What is stillness?” turn labelled “Reflective guidance” with “No grounded teaching was found for this response; treat it as reflective guidance.” This is consistent with the repository’s fail-closed distinction between grounded teaching and reflective guidance, but it is not a quantitative faithfulness result.

After the streamed turn settled, the live browser displayed `ERR_UNKNOWN`, “Something went wrong”, and “Failed to fetch”. The technical detail identified the cause as “An unexpected error occurred while preparing the reply”, with Retry available. The user message remained visible, but no final answer or citations were produced. This is a fresh confirmed live-runtime failure in the local Docker stack, despite `/api/healthz` returning alive and `/api/health` returning `ready:false` because `runtime_artifacts.missing_required` contained `okf_compiled`. The browser failure is therefore consistent with serving readiness being blocked by missing curated artifacts, but the exact backend exception still requires container-log correlation.
