=== TRACK: docs_agent_ready ===
UTC: 2026-08-24T13:52:06Z
HEAD: 1f882077616b92b5e2b59a31d9ee4fb246e8fe68
./.agent/skills/claude-seo/AGENTS.md
./.agents/skills/agentic-design-patterns/cheatsheet.md
./.agents/skills/agentic-design-patterns/glossary.md
./.agents/skills/agentic-design-patterns/patterns.md
./.agents/skills/agentic-design-patterns/skill.md
./.agents/skills/ai-agents-in-action/cheatsheet.md
./.agents/skills/ai-agents-in-action/glossary.md
./.agents/skills/ai-agents-in-action/patterns.md
./.agents/skills/ai-agents-in-action/skill.md
./.agents/skills/ai-agents-langchain-mcp/cheatsheet.md
./.agents/skills/ai-agents-langchain-mcp/glossary.md
./.agents/skills/ai-agents-langchain-mcp/patterns.md
./.agents/skills/ai-agents-langchain-mcp/skill.md
./.agents/skills/ai-engineering-chip-huyen/cheatsheet.md
./.agents/skills/ai-engineering-chip-huyen/glossary.md
./.agents/skills/ai-engineering-chip-huyen/patterns.md
./.agents/skills/ai-engineering-chip-huyen/skill.md
./.agents/skills/build-llm-app-scratch/cheatsheet.md
./.agents/skills/build-llm-app-scratch/glossary.md
./.agents/skills/build-llm-app-scratch/patterns.md
./.agents/skills/build-llm-app-scratch/skill.md
./.agents/skills/build-llm-from-scratch/cheatsheet.md
./.agents/skills/build-llm-from-scratch/glossary.md
./.agents/skills/build-llm-from-scratch/patterns.md
./.agents/skills/build-llm-from-scratch/skill.md
./.agents/skills/building-apps-ai-agents/cheatsheet.md
./.agents/skills/building-apps-ai-agents/glossary.md
./.agents/skills/building-apps-ai-agents/patterns.md
./.agents/skills/building-apps-ai-agents/skill.md
./.agents/skills/building-llms-for-production/cheatsheet.md
./.agents/skills/building-llms-for-production/glossary.md
./.agents/skills/building-llms-for-production/patterns.md
./.agents/skills/building-llms-for-production/skill.md
./.agents/skills/career-in-ai/cheatsheet.md
./.agents/skills/career-in-ai/glossary.md
./.agents/skills/career-in-ai/patterns.md
./.agents/skills/career-in-ai/skill.md
./.agents/skills/database-internals/cheatsheet.md
./.agents/skills/database-internals/glossary.md
./.agents/skills/database-internals/patterns.md
./.agents/skills/database-internals/skill.md
./.agents/skills/debug-issue/skill.md
./.agents/skills/designing-data-intensive-apps-2e/cheatsheet.md
./.agents/skills/designing-data-intensive-apps-2e/glossary.md
./.agents/skills/designing-data-intensive-apps-2e/patterns.md
./.agents/skills/designing-data-intensive-apps-2e/skill.md
./.agents/skills/designing-distributed-systems/cheatsheet.md
./.agents/skills/designing-distributed-systems/glossary.md
./.agents/skills/designing-distributed-systems/patterns.md
./.agents/skills/designing-distributed-systems/skill.md
./.agents/skills/explore-codebase/skill.md
./.agents/skills/hands-on-llms/cheatsheet.md
./.agents/skills/hands-on-llms/glossary.md
./.agents/skills/hands-on-llms/patterns.md
./.agents/skills/hands-on-llms/skill.md
./.agents/skills/ieee-innovative-trends-2019/cheatsheet.md
./.agents/skills/ieee-innovative-trends-2019/glossary.md
./.agents/skills/ieee-innovative-trends-2019/patterns.md
./.agents/skills/ieee-innovative-trends-2019/skill.md
./.agents/skills/kleppmann-ddia-big-ideas/cheatsheet.md
./.agents/skills/kleppmann-ddia-big-ideas/glossary.md
./.agents/skills/kleppmann-ddia-big-ideas/patterns.md
./.agents/skills/kleppmann-ddia-big-ideas/skill.md
./.agents/skills/llms-in-production/cheatsheet.md
./.agents/skills/llms-in-production/glossary.md
./.agents/skills/llms-in-production/patterns.md
./.agents/skills/llms-in-production/skill.md
./.agents/skills/prompt-engineering-llms/cheatsheet.md
./.agents/skills/prompt-engineering-llms/glossary.md
./.agents/skills/prompt-engineering-llms/patterns.md
./.agents/skills/prompt-engineering-llms/skill.md
./.agents/skills/rag-made-simple/cheatsheet.md
./.agents/skills/rag-made-simple/glossary.md
./.agents/skills/rag-made-simple/patterns.md
./.agents/skills/rag-made-simple/skill.md
./.agents/skills/refactor-safely/skill.md
./.agents/skills/review-changes/skill.md
./.agents/skills/supabase-postgres-best-practices/CHANGELOG.md
./.agents/skills/supabase-postgres-best-practices/SKILL.md
./.agents/skills/supabase/CHANGELOG.md
./.agents/skills/supabase/SKILL.md
./.agents/skills/system-design-llm-era/cheatsheet.md
./.agents/skills/system-design-llm-era/glossary.md
./.agents/skills/system-design-llm-era/patterns.md
./.agents/skills/system-design-llm-era/skill.md
./.agents/skills/token-optimization/SKILL.md
./.agents/skills/video-generator/SKILL.md
./.claude/agents/hyperresearch-browser-fetcher.md
./.claude/agents/hyperresearch-cite-checker.md
./.claude/agents/hyperresearch-corpus-critic.md
./.claude/agents/hyperresearch-depth-critic.md
./.claude/agents/hyperresearch-depth-investigator.md
./.claude/agents/hyperresearch-dialectic-critic.md
./.claude/agents/hyperresearch-draft-orchestrator.md
./.claude/agents/hyperresearch-fetcher.md
./.claude/agents/hyperresearch-instruction-critic.md
./.claude/agents/hyperresearch-loci-analyst.md
./.claude/agents/hyperresearch-patcher.md
./.claude/agents/hyperresearch-polish-auditor.md
./.claude/agents/hyperresearch-readability-recommender.md
./.claude/agents/hyperresearch-source-analyst.md
./.claude/agents/hyperresearch-synthesizer.md
./.claude/agents/hyperresearch-width-critic.md
./.claude/GOAL.md
./.claude/handoff.md
./.claude/launch.json
./.claude/plans/onnx-int8-embedding-validation.plan.md
./.claude/plans/onnx-reranker-colbert-optimization.plan.md
./.claude/plans/semantic-model-router.md
./.claude/plans/uncommitted-batch-2026-08-01.plan.md
./.claude/scheduled_tasks.lock
./.claude/settings.json
./.claude/settings.local.json
./.claude/skills/hyperresearch-1-5-chapter-partition/SKILL.md
./.claude/skills/hyperresearch-1-decompose/SKILL.md
./.claude/skills/hyperresearch-10-triple-draft/SKILL.md
./.claude/skills/hyperresearch-11-synthesize/SKILL.md
./.claude/skills/hyperresearch-12-critics/SKILL.md
./.claude/skills/hyperresearch-13-gap-fetch/SKILL.md
./.claude/skills/hyperresearch-14-5-cite-check/SKILL.md
./.claude/skills/hyperresearch-14-patcher/SKILL.md
./.claude/skills/hyperresearch-15-polish/SKILL.md
./.claude/skills/hyperresearch-16-readability-audit/SKILL.md
./.claude/skills/hyperresearch-2-width-sweep/SKILL.md
./.claude/skills/hyperresearch-3-contradiction-graph/SKILL.md
./.claude/skills/hyperresearch-4-loci-analysis/SKILL.md
./.claude/skills/hyperresearch-5-depth-investigation/SKILL.md
./.claude/skills/hyperresearch-6-cross-locus-reconcile/SKILL.md
./.claude/skills/hyperresearch-7-source-tensions/SKILL.md
./.claude/skills/hyperresearch-8-corpus-critic/SKILL.md
./.claude/skills/hyperresearch-9-evidence-digest/SKILL.md
./.claude/skills/hyperresearch/SKILL.md
./.claude/tasks/2026-07-28-okf-v02-full-scope.md
./.claude/tasks/2026-08-16-batched-local-transcription-railway-ingestion.md
./.claude/tasks/2026-08-16-mobile-polish-language-icon-profile-redesign.md
./.claude/tasks/admin_verification.md
./.claude/tasks/agentic_graph_rag_traversal_task1.md
./.claude/tasks/agentic-graph-rag-loop.md
./.claude/tasks/audit_hardening.md
./.claude/tasks/audit-fixes-2026-08-19.md
./.claude/tasks/auth_domain_auto_signout.md
./.claude/tasks/auth_persistence_and_restrictions.md
./.claude/tasks/benchmark_analysis.md
./.claude/tasks/cache_debug_plan.md
./.claude/tasks/cleanup_unused_markdown.md
./.claude/tasks/clickable-citations.md
./.claude/tasks/complete_comprehensive_report_implementation.md
./.claude/tasks/complete_goals_and_pending_cases.md
./.claude/tasks/COMPREHENSIVE_FIX_PLAN.md
./.claude/tasks/comprehensive_report_checklist.md
./.claude/tasks/comprehensive-fix-plan.md
./.claude/tasks/corpus-remediation-phase1.md
./.claude/tasks/cortex-memory-refinements.md
./.claude/tasks/cost_spike_investigation_aug2026.md
./.claude/tasks/DATA_QUALITY_SCALING_PLAN.md
./.claude/tasks/disable_ollama_local_models.md
./.claude/tasks/docker_and_latency_cleanup.md
./.claude/tasks/E6-UIUX-KG.md
./.claude/tasks/edge_case_security_latency_quality_plan.md
./.claude/tasks/end_to_end_transcript_review.md
./.claude/tasks/EU_AI_ACT_COMPLIANCE_AND_PROVENANCE_PLAN.md
./.claude/tasks/explore_codebase.md
./.claude/tasks/feature-wave.md
./.claude/tasks/fix_audit_issues_2.md
./.claude/tasks/fix_audit_issues_3.md
./.claude/tasks/fix_audit_issues.md
./.claude/tasks/fix_backend_and_lessons_issues.md
./.claude/tasks/fix_backend_bugs.md
./.claude/tasks/fix_backend_oom.md
./.claude/tasks/fix_backend_syntax_and_warm_cache.md
./.claude/tasks/FIX_CODE_REVIEW_FINDINGS.md
./.claude/tasks/fix_dropped_graph_nodes.md
./.claude/tasks/fix_effort_not_supported.md
./.claude/tasks/fix_ingest_services_ontology.md
./.claude/tasks/fix_issues.md
./.claude/tasks/fix_knowledge_graph.md
./.claude/tasks/fix_metrics_and_multi_device_resume.md
./.claude/tasks/fix_sarvam_http_gateway.md
./.claude/tasks/fix_scaling_and_quality_integration.md
./.claude/tasks/fix_semaphore_leaks.md
./.claude/tasks/fix_tayari_issues.md
./.claude/tasks/fix_vulnerabilities.md
./.claude/tasks/fix-google-origin-e2e.md
./.claude/tasks/fix-merge-conflicts.md
./.claude/tasks/fix-security-and-quality-issues.md
./.claude/tasks/fix-youtube-railway-plan.md
./.claude/tasks/fix-youtube-railway.md
./.claude/tasks/frontend_lockfile_fix.md
./.claude/tasks/full-conversation-reverification.md
./.claude/tasks/ground-up-audit-and-top-notch-upgrade.md
./.claude/tasks/GURU_BRAIN_HALLUCINATION_FREE.md
./.claude/tasks/GURU_BRAIN_TONE_ALIGNMENT.md
./.claude/tasks/handoff.md
./.claude/tasks/headroom_advanced.md
./.claude/tasks/headroom_setup.md
./.claude/tasks/INTELLIGENCE_LAYER_USER_LAYERS.md
./.claude/tasks/kg_ui_ux_and_demo.md
./.claude/tasks/kg-fixes-2026-07-28.md
./.claude/tasks/knowledge_graph_redesign.md
./.claude/tasks/landing_scroll_google_auth.md
./.claude/tasks/language_support_completion.md
./.claude/tasks/LATENCY_OPTIMIZATION.md
./.claude/tasks/live_query_polling.md
./.claude/tasks/locale_translations_plan.md
./.claude/tasks/LOOP_ENGINEERING_REMEDIATION.md
./.claude/tasks/MALAYALAM_LOCALIZATION.md
./.claude/tasks/master-implementation-plan.md
./.claude/tasks/memory_compaction_and_ui_fix.md
./.claude/tasks/microphone-attachment-ingestion.md
./.claude/tasks/mobile-app-launch.md
./.claude/tasks/nim_benchmark_rotation.md
./.claude/tasks/NORMAL_INGESTION_PIPELINE_RUTHLESS_AUDIT.md
./.claude/tasks/nvidia_nim_integration.md
./.claude/tasks/offline_locale_translations.md
./.claude/tasks/OLLAMA_QUOTA_CHAT_UI_PLAN.md
./.claude/tasks/ontology_and_cache_plan.md
./.claude/tasks/openrouter_anthropic_cache.md
./.claude/tasks/P0-6-remove-test-auth-backdoor.md
./.claude/tasks/parallelize_guardrails.md
./.claude/tasks/product_demo_recording.md
./.claude/tasks/product-storytelling-enterprise-readiness.md
./.claude/tasks/production_audit_fixes.md
./.claude/tasks/production_benchmark_20260820.md
./.claude/tasks/production_hardening_and_audit_fixes.md
./.claude/tasks/production_readiness_plan.md
./.claude/tasks/production_readiness_playbook.md
./.claude/tasks/production-hardening-reconciliation.md
./.claude/tasks/prompt-caching-context-separation.md
./.claude/tasks/prompt-restructuring.md
./.claude/tasks/queue_backpressure.md
./.claude/tasks/queue-enhancements.md
./.claude/tasks/ragflow_integration.md
./.claude/tasks/railway_dependency_fix.md
./.claude/tasks/railway-pro-best-practices.md
./.claude/tasks/recording_user_demo.md
./.claude/tasks/regional-locale-translations.md
./.claude/tasks/remediation_and_benchmarks.md
./.claude/tasks/restrict_languages.md
./.claude/tasks/retrieval_and_demo.md
./.claude/tasks/REWRITE_CLAUDE_MD.md
./.claude/tasks/ruthless_audit_and_cleanup.md
./.claude/tasks/ruthless_audit_fixes.md
./.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md
./.claude/tasks/RUTHLESS_INGESTION_AUDIT_FIX.md
./.claude/tasks/ruthless_plan_execution.md
./.claude/tasks/ruthless_production_audit_plan.md
./.claude/tasks/RUTHLESS_QUALITY_KG_UI_PLAN.md
./.claude/tasks/ruthless_research_and_integration_plan.md
./.claude/tasks/ruthless_review_p0_p1.md
./.claude/tasks/ruthless-complete-delivery-plan.md
./.claude/tasks/ruthless-end-to-end-product-transformation.md
./.claude/tasks/ruthless-ingestion-audit/AUDIT_REPORT.md
./.claude/tasks/ruthless-productionization.md
./.claude/tasks/security-fixes.md
./.claude/tasks/seed_admin_credentials.md
./.claude/tasks/sequential_guardrails.md
./.claude/tasks/serene_mind_tabs.md
./.claude/tasks/setup_free_claude_code.md
./.claude/tasks/setup_scrapling.md
./.claude/tasks/start_docker_and_run_benchmarks.md
./.claude/tasks/TASK_ADMIN_PAGES_RESILIENCY.md
./.claude/tasks/transcript_remediation_and_corpus_update_plan.md
./.claude/tasks/transcript-md-quality.md
./.claude/tasks/translate-locales.md
./.claude/tasks/ui_layout_alignment_fix.md
./.claude/tasks/ui_ux_audit_and_rework.md
./.claude/tasks/ui_ux_audit_p2_p4.md
./.claude/tasks/ui-regression-fixes.md
./.claude/tasks/understand_anything.md
./.claude/tasks/verify_and_record_demo.md
./.claude/tasks/websearch_testing.md
./.claude/tasks/wire_anthropic_gateway_and_thresholds.md
./.claude/tasks/world-class-uplift.md
./.claude/tasks/write_claudemd.md
./.codex/config.toml
./.codex/hooks.json
./.opencode/.gitignore
./.opencode/AGENTS.md
./.opencode/node_modules/.package-lock.json
./.opencode/node_modules/cross-spawn/index.js
./.opencode/node_modules/cross-spawn/LICENSE
./.opencode/node_modules/cross-spawn/package.json
./.opencode/node_modules/cross-spawn/README.md
./.opencode/node_modules/detect-libc/index.d.ts
./.opencode/node_modules/detect-libc/LICENSE
./.opencode/node_modules/detect-libc/package.json
./.opencode/node_modules/detect-libc/README.md
./.opencode/node_modules/effect/LICENSE
./.opencode/node_modules/effect/package.json
./.opencode/node_modules/effect/README.md
./.opencode/node_modules/fast-check/LICENSE
./.opencode/node_modules/fast-check/package.json
./.opencode/node_modules/fast-check/README.md
./.opencode/node_modules/find-my-way-ts/LICENSE
./.opencode/node_modules/find-my-way-ts/package.json
./.opencode/node_modules/find-my-way-ts/README.md
./.opencode/node_modules/ini/LICENSE
./.opencode/node_modules/ini/package.json
./.opencode/node_modules/ini/README.md
./.opencode/node_modules/isexe/.npmignore
./.opencode/node_modules/isexe/index.js
./.opencode/node_modules/isexe/LICENSE
./.opencode/node_modules/isexe/mode.js
./.opencode/node_modules/isexe/package.json
./.opencode/node_modules/isexe/README.md
./.opencode/node_modules/isexe/windows.js
./.opencode/node_modules/kubernetes-types/package.json
./.opencode/node_modules/kubernetes-types/README.md
./.opencode/node_modules/kubernetes-types/runtime.d.ts
./.opencode/node_modules/kubernetes-types/version.d.ts
./.opencode/node_modules/msgpackr-extract/binding.gyp
./.opencode/node_modules/msgpackr-extract/index.js
./.opencode/node_modules/msgpackr-extract/LICENSE
./.opencode/node_modules/msgpackr-extract/package.json
./.opencode/node_modules/msgpackr-extract/README.md
./.opencode/node_modules/msgpackr/benchmark.md
./.opencode/node_modules/msgpackr/index.d.cts
./.opencode/node_modules/msgpackr/index.d.ts
./.opencode/node_modules/msgpackr/index.js
./.opencode/node_modules/msgpackr/iterators.js
./.opencode/node_modules/msgpackr/LICENSE
./.opencode/node_modules/msgpackr/node-index.js
./.opencode/node_modules/msgpackr/pack.d.cts
./.opencode/node_modules/msgpackr/pack.d.ts
./.opencode/node_modules/msgpackr/pack.js
./.opencode/node_modules/msgpackr/package.json
./.opencode/node_modules/msgpackr/README.md
./.opencode/node_modules/msgpackr/rollup.config.js
./.opencode/node_modules/msgpackr/SECURITY.md
./.opencode/node_modules/msgpackr/stream.js
./.opencode/node_modules/msgpackr/test-worker.js
./.opencode/node_modules/msgpackr/unpack.d.cts
./.opencode/node_modules/msgpackr/unpack.d.ts
./.opencode/node_modules/msgpackr/unpack.js
./.opencode/node_modules/multipasta/LICENSE
./.opencode/node_modules/multipasta/package.json
./.opencode/node_modules/multipasta/README.md
./.opencode/node_modules/node-gyp-build-optional-packages/bin.js
./.opencode/node_modules/node-gyp-build-optional-packages/build-test.js
./.opencode/node_modules/node-gyp-build-optional-packages/index.js
./.opencode/node_modules/node-gyp-build-optional-packages/LICENSE
./.opencode/node_modules/node-gyp-build-optional-packages/node-gyp-build.js
./.opencode/node_modules/node-gyp-build-optional-packages/optional.js
./.opencode/node_modules/node-gyp-build-optional-packages/package.json
./.opencode/node_modules/node-gyp-build-optional-packages/README.md
./.opencode/node_modules/path-key/index.d.ts
./.opencode/node_modules/path-key/index.js
./.opencode/node_modules/path-key/license
./.opencode/node_modules/path-key/package.json
./.opencode/node_modules/path-key/readme.md
./.opencode/node_modules/pure-rand/LICENSE
./.opencode/node_modules/pure-rand/package.json
./.opencode/node_modules/pure-rand/README.md
./.opencode/node_modules/shebang-command/index.js
./.opencode/node_modules/shebang-command/license
./.opencode/node_modules/shebang-command/package.json
./.opencode/node_modules/shebang-command/readme.md
./.opencode/node_modules/shebang-regex/index.d.ts
./.opencode/node_modules/shebang-regex/index.js
./.opencode/node_modules/shebang-regex/license
./.opencode/node_modules/shebang-regex/package.json
./.opencode/node_modules/shebang-regex/readme.md
./.opencode/node_modules/toml/index.d.ts
./.opencode/node_modules/toml/index.js
./.opencode/node_modules/toml/LICENSE
./.opencode/node_modules/toml/package.json
./.opencode/node_modules/toml/README.md
./.opencode/node_modules/uuid/LICENSE.md
./.opencode/node_modules/uuid/package.json
./.opencode/node_modules/uuid/README.md
./.opencode/node_modules/which/CHANGELOG.md
./.opencode/node_modules/which/LICENSE
./.opencode/node_modules/which/package.json
./.opencode/node_modules/which/README.md
./.opencode/node_modules/which/which.js
./.opencode/node_modules/yaml/bin.mjs
./.opencode/node_modules/yaml/LICENSE
./.opencode/node_modules/yaml/package.json
./.opencode/node_modules/yaml/README.md
./.opencode/node_modules/yaml/util.js
./.opencode/node_modules/zod/index.cjs
./.opencode/node_modules/zod/index.d.cts
./.opencode/node_modules/zod/index.d.ts
./.opencode/node_modules/zod/index.js
./.opencode/node_modules/zod/LICENSE
./.opencode/node_modules/zod/package.json
./.opencode/node_modules/zod/README.md
./.opencode/package-lock.json
./.opencode/package.json
./.opencode/plans/close-out-audit-gaps.md
./.opencode/plans/emergent-security-audit.md
./.opencode/plans/prod-code-work.md
./.opencode/plans/qdrant_binary_quantization_pilot.md
./.opencode/plans/ruthless-all-gaps.md
./.opencode/plans/ruthless-prod-readiness.md
./.opencode/plans/ruthless-system-error-and-enhancement.md
./.opencode/plans/technical-debt-security-audit.md
./.opencode/plans/worldclass-delivery-plan.md
./AGENTS.md
./brag-output/composition/AGENTS.md
./external_repos/headroom/AGENTS.md
./external_repos/ragflow/AGENTS.md
./external_repos/surfsense/surfsense_obsidian/AGENTS.md
./lessons.md
./mcp-servers/graphify/AGENTS.md
./mcp-servers/hermes-agent/AGENTS.md
./memory/AGENTS.md
--- instruction counts ---
     142
--- conflicting/stale claims ---
README.md:3:> **Active release baseline — verified 2026-08-12.** The supported frontend gate is `npm run build`; source lint is expected to have zero errors; `npm audit --omit=dev` must be clean. See the [release evidence pack](docs/operations/release-evidence-pack.md) for the complete safety, documentation, and privileged-integration checklist. Historical counts elsewhere in this document are architectural context, not live service assertions.
README.md:24:| **Vector DB** | Qdrant (`spiritual_wisdom`: 89,053 points, `second_brain_vault`) | `6333` |
README.md:35:- **Qdrant Vector Base (`spiritual_wisdom`)**: Ingested 89,053 items covering books, 450+ YouTube discourses, meditations, and lectures.
README.md:49:  - *Tier 3 (User Core Vault)*: Protected user core memory. Inactive accounts (>365 days) automatically purged via `scripts/ops/cleanup_inactive_user_data.py`.
README.md:66:### 4. Interactive Obsidian-Style Knowledge Graph
README.md:144:### Automated Inactive User Memory Cleanup
README.md:145:To purge inactive user data (>365 days inactive):
README.md:148:backend/.venv/bin/python backend/scripts/ops/cleanup_inactive_user_data.py --days 365
README.md:169:│   ├── archive/                   # Historical audit reports & completed plans
README.md:233:### Proactive Healing Courses (Streak-Based)
README.md:234:- `backend/services/healing_course_service.py`: assigns a healing course only on distress streaks — ≥2 consecutive turns, ≥3-of-5 frequency, escalating severity, or same SufferingSignal ≥2× in 24h; never duplicates an active course (`user_course_progress`).
AGENTS.md:14:- Local validation in the sandbox is dependency-limited: do not install packages. `py_compile`, the regex safety gate, `git diff --check`, and the corpus guard are valid local gates; `pytest` and dependency-complete tests must run in CI or the production/test image.
AGENTS.md:24:- Curated `/app/memory/okf/compiled.json` and `/app/data/doctrine_lexicon.json` remain absent. Do not manufacture placeholders, activate ONNX/RRF/DBSF, mutate Neo4j schema, widen graph concurrency, or modify `scripts/ingestion/corpus/` without approved audited artifacts and held-out evidence.
AGENTS.md:36:- The local backend health response was HTTP 200 with `ready=true,status=degraded`; the only reported degraded component was optional OCR. Local Qdrant data, absent OKF compiled artifacts, and local provider configuration remain non-equivalent to the deployed production environment. Do not claim a production retest from these results. No Railway deployment was started.
AGENTS.md:64:- **Qdrant `spiritual_wisdom`**: 89,053 points (full corpus: books, 450+ YouTube discourses, meditations, lectures)
AGENTS.md:65:- **Neo4j Knowledge Graph**: 8,750+ nodes active over private network (`bolt://gb-neo4j-railway-template.railway.internal:7687`)
AGENTS.md:76:  3. *Tier 3 (User Core Memories & Vault)*: Protected while user is active. Accounts inactive $>365\text{ days}$ auto-purged via `scripts/ops/cleanup_inactive_user_data.py`.
AGENTS.md:140:- **Embedding warm-up canary** (`main.py`): Fires ONNX encoder before `startup_complete=True`. Validates dimension contract (logs ERROR if dim != `settings.embedding_dimension`).
AGENTS.md:162:- **PERF-2 DOCUMENTED** (`app/api/chat.py`): `asyncio.to_thread()` wrapping of sync Supabase client is the correct pattern. Added `# PERF-2 TODO: migrate to async Postgres client` to track technical debt.
AGENTS.md:166:- **CODE-2 PARTIAL** (`app/api/admin.py`): Moved `datetime/timedelta/UTC` to module-level imports; removed redundant inline `from app.telemetry_db import get_kpis, get_node_latencies` (already at module top). Added comment explaining remaining intentional lazy-loads (circular import avoidance for celery tasks, cost tracker, prompt store).
AGENTS.md:209: - **`FORWARDED_ALLOW_IPS` gate**: `start_railway.py` requires an explicit non-wildcard allowlist (`app.config.settings.forwarded_allow_ips`) and exits at startup when missing or `"*"`. Docker compose runs plain uvicorn (`backend/Dockerfile` CMD) — unaffected. Railway is currently **paused**; set `FORWARDED_ALLOW_IPS` (e.g. `10.0.0.0/8`) before the next Railway deploy.
AGENTS.md:273:- **Ingestion Pipeline Isolation**: Flushing these query-side caches has **zero** impact on the active or pending ingestion processes. Ingestion is an ETL pipeline that writes exclusively to Qdrant and Neo4j and maintains its own resumption checkpoints in `scripts/ingestion_state.json`. Agents can confidently assure the user that cache flushing is fully isolated and safe to execute.
AGENTS.md:275:## Non-Interactive Shell Commands
AGENTS.md:276:**ALWAYS use non-interactive flags** with file operations to avoid hanging on confirmation prompts.
AGENTS.md:278:Shell commands like `cp`, `mv`, and `rm` may be aliased to include `-i` (interactive) mode on some systems, causing the agent to hang indefinitely waiting for y/n input.
AGENTS.md:293:- `scp` - use `-o BatchMode=yes` for non-interactive
AGENTS.md:316:- [ ] **docs/PRODUCT_OPPORTUNITIES.md** (Roadmap section): Mark items as complete or add new technical debt discovered during the change.
AGENTS.md:326:1. **File issues for remaining work** - Create issues for anything that needs follow-up
AGENTS.md:455:- **Known TODOs**: ~~Apple Sign-In~~ ✅ implemented (native iOS, `AuthPage.tsx` — requires Supabase Apple provider config before submission). ~~Delete-account flow~~ ✅ implemented (`ProfilePage.tsx` + `delete-my-account` edge function).
AGENTS.md:565:- **Status (Aug 11, 2026)**: Railway is **paused** (stopped for a while). Before the next deploy, set `FORWARDED_ALLOW_IPS` (e.g. `10.0.0.0/8`) — `start_railway.py` now exits at startup without an explicit non-wildcard value (fail-closed; see Security Invariants). Until then, dev continues on the docker compose stack, which runs plain uvicorn and needs no such var.
AGENTS.md:572:  - `/api/health` — real per-service health, returns `ready: false` until `startup_complete=True`
AGENTS.md:615:- **RAGatouille**: kept in `requirements.txt` with a TODO comment marking it deprecated. Do NOT remove until the RAGatouille path is confirmed dead in `cascaded_rerank`.
AGENTS.md:651:The 2026-08-22 production verification observed the bounded comparison at approximately 7 ms with a 367-character limited-support response and no citations. The active 8655709 runtime returned an English FAQ under Hindi preference in approximately 4.3 seconds and a native Hindi FAQ in approximately 4.0 seconds, while the benchmark matrix also captured a 10.8-second pipeline/13.8-second wall-clock Hindi run, so multilingual latency remains variable. The attempted 940eb55 translation-timeout rollout failed during `Initialization › Snapshot code` after 15 minutes; Build/Deploy never started, and Railway continued to show 8655709 as ACTIVE. Deep health reported `ready=true`, `status=healthy`, embedding dimension 1024, queue size 0, exact-cache keys 0, and healthy Qdrant, Redis, Neo4j, LLM, embedding, fast graph, and LightRAG checks. The translation-timeout code is production-active through the successful `806799d` rollout; continue to treat multilingual tail variance as an open performance risk.
AGENTS.md:653:The sandbox has no `pytest` or `pydantic-settings`. Do not install packages for a release check when the task forbids installation, and never represent static compilation or regex safety as a substitute for dependency-complete tests. The focused regression test is committed for execution in CI or the locked production test image.
AGENTS.md:660:Warm repeated Hindi FAQ requests completed in roughly 7 seconds wall-clock with 3.3–3.8 seconds reported pipeline latency, but the final matrix also recorded a 21.2-second Hindi wall-clock outlier during rollout. Multilingual latency therefore remains a tail-observability item, not a solved claim. The current query is now passed into Second Brain recall; preserve this contract for all authenticated memory paths.
AGENTS.md:665:- Runtime `36e6f22` is the latest verified production revision; backend deployment `c1387196-6860-4da9-b7f5-1c74acfce205` and worker deployment `49e64d1d-e979-4459-9f3c-226cf002b8a7` completed successfully. `/api/health` returned `ready=true`, `status=healthy`, Qdrant/Redis/Neo4j/LLM/embedding/graph/LightRAG checks healthy, and chat backpressure clear.
AGENTS.md:679:- Full question-bank production load tests must not run uncontrolled against user-serving production. Use staging or a dedicated benchmark replica, unbuffered progress, a global budget, and bounded concurrency. The interrupted live run is evidence of tail load, not a complete benchmark pass.
AGENTS.md:695:- Post-deploy logs show BGE-M3 is fetched at startup and the reranker is not in the image cache; OKF compiled index and doctrine lexicon are absent in the deployed archive. Do not hide these warnings or create empty artifacts. Treat them as separate image/reingestion follow-up work; never modify `scripts/ingestion/corpus/`.
AGENTS.md:707:- `Dockerfile.railway` must not pre-cache or imply active ONNX while production uses FlagEmbedding-compatible Qdrant vectors. Runtime model downloads, missing OKF compiled index, and missing doctrine lexicon are packaging/cold-start findings; do not create empty placeholders or modify `scripts/ingestion/corpus/`.
AGENTS.md:713:- Explicitly authorized Second Brain E2E completed in the connected Lovable browser: the labelled disposable reflection `Disposable production verification — delete me` was created, confirmed after reload, deleted with `Forget this`, and confirmed absent after a settled reload. The route also unlocked successfully after the final BRAIN_KEK process refresh.
AGENTS.md:714:- Explicitly authorized BRAIN_KEK rotation completed without printing or committing key material. Dry-run validated 2 Mode-A rows; CAS apply rewrapped exactly 2 rows. The replacement was promoted to backend and worker `BRAIN_KEK`, `BRAIN_KEK_NEXT` was deleted from both scopes, and both processes were explicitly redeployed again afterward. Final backend deployment `108888e3-757d-4a37-9c77-0a890c5a51c4` and worker deployment `f9d05731-6fb0-4b8d-92bd-21fb14b01aa9` reached `SUCCESS`; `/api/health` returned ready/healthy with embedding dimension 1024. A name-only audit confirmed BRAIN_KEK present and BRAIN_KEK_NEXT absent in both scopes.
AGENTS.md:716:- Current repository head is `c0bbd81`; worktree is clean and the immutable `scripts/ingestion/corpus/` directory remains untouched. Strict verdict remains NOT READY because reviewed OKF/doctrine runtime artifacts are absent, cost/memory and long-tail latency gates are open, Lovable SSE-final publication is unproven, broader auth/RLS/upload/recovery checks remain incomplete, and custom-domain/browser breakpoint coverage is not closed.
AGENTS.md:722:- Dependency-complete Docker validation passed the backend safety/grounding/chat groups plus the new queued-SSE regression: `88 passed in 7.87s`. Docker frontend transport validation passed `9 tests`, and TypeScript typecheck passed. After backend restart, queued Docker probes completed in `364ms` and `1,704ms`, with first events at `95ms` and `1,118ms`; this is local transport evidence only.
AGENTS.md:727:- The full local retrieval-quality gate ran in a root-mounted dependency-complete Docker test image: `5 passed, 4 skipped`. The four NDCG integration strategies did not execute because the reachable local `spiritual_wisdom` Qdrant collection has `0` points. A metadata-only flagship probe returned `QUERY`, `no_context_short_circuit`, zero citations, and a 38-character final for both Serene Mind and Four Sacred Secrets. This is a local data-plane failure, not proof of a production quality regression or closure.
AGENTS.md:734:- A metadata-only local sweep covered 20 prompts across greetings, flagship teachings, Indic languages, comparison, crisis, domestic violence, off-topic, prompt injection, personal-memory, Second Brain, graph, unknown doctrine, and follow-up behavior. With the local OpenRouter circuit open, substantive prompts returned transparent `ERROR`/`system_error` results with zero citations; acute crisis returned blocked `DISTRESS` with no final answer, and deterministic domestic-violence/prompt-injection blocks remained active.
AGENTS.md:742:- Curated runtime artifacts `okf_compiled` and `doctrine_lexicon` are required. `inspect_runtime_artifacts()` returns `missing_required` and `readiness_ok`; deep `/api/health` reports them as critical. Do not manufacture absent artifacts.
AGENTS.md:748:- Full backend suite was attempted twice and stalled around 41% in the connected desktop environment; never report it as passed until CI/test image completes it with per-test duration reporting.
.opencode/plans/emergent-security-audit.md:97:- Fix remaining findings
.opencode/plans/worldclass-delivery-plan.md:39:| Second Brain code | ✅ | `read` all files + `diff` vs worldclass | **Complete**: crypto, service, vault_index, API router, migration, 6 tests. **Diverged** from worldclass (shared Qdrant collection vs per-user, `generate()` vs `complete()` API, `get_container()` vs `app.state`) |
.opencode/plans/technical-debt-security-audit.md:156:**Status**: Deferred until ingestion complete + chat UX stable
.opencode/plans/technical-debt-security-audit.md:190:- `docs/ROADMAP.md` - Mark completed items, update status
.opencode/plans/close-out-audit-gaps.md:97:- Triple-layer rate limiting active
.opencode/plans/close-out-audit-gaps.md:110:- Update both RUTHLESS_AUDIT_REPORT.md and memory/audit_summary.md checkboxes as items complete
.opencode/plans/prod-code-work.md:29:Phase B (sequential — needs Task 2 complete)
.opencode/plans/prod-code-work.md:114:**If Task 2 (i18n) is complete**, use `t()` for picker copy. If not, hardcode English (safe fallback).
.opencode/plans/prod-code-work.md:148:   - `onClick` logs `TODO: embed product video`
.opencode/plans/prod-code-work.md:177:  B1 (standard): Task 3 — Onboarding language picker (depends on i18n complete)
.opencode/plans/ruthless-system-error-and-enhancement.md:159:- (b) ConversationSourcesPanel.tsx — interactive source snippets on hover.
.opencode/plans/ruthless-system-error-and-enhancement.md:160:- (c) ProactiveSereneMindTrigger in ChatInterface.tsx — detect rising distress, offer meditation modal before explicit ask.
.opencode/plans/ruthless-system-error-and-enhancement.md:269:These remaining items get documented in roadmap, not implemented.
.opencode/plans/ruthless-system-error-and-enhancement.md:318:**Gate**: Phase G runs ONLY after Phases A, AB, B, E2–E11 are complete and unit tests pass. This is the verification capstone — boris #14 ("Did it actually work?") applied to the whole change set.
.opencode/plans/ruthless-system-error-and-enhancement.md:377:  - Distress trigger fires on query #6 (Serene Mind path active).
.opencode/plans/ruthless-system-error-and-enhancement.md:440:| H2.6 | ensure_profile_and_role failure — no visible feedback | User-reported | Add visible toast or inline error so users know profile setup didn't complete |
.opencode/plans/ruthless-system-error-and-enhancement.md:454:| H3.8 | flashrank reranker TypeError | Ranker(model_name=None). Pre-existing, cross-encoder active | Silence traceback noise (guard the load) |
lessons.md:10:- **Rule**: Do not stop databases or the serving backend merely to save idle resources; first disable inactive ingestion/observability services and measure the result. Do not delete volumes or build caches without explicit authorization.
lessons.md:21:- **Rule**: Record the harness, mount mode, and exact test selection with every container result. Never convert an intentionally incomplete artifact tree into a synthetic production artifact just to make a test pass.
lessons.md:31:- **Fix applied**: `format_final_answer` now returns a clearly labelled extractive `grounded_partial_evidence` envelope when usable citable documents exist after refusal/retry exhaustion. It preserves absolute URLs, `partial=true`, `verification.passed=false`, and `faithfulness_score=0.0`; it never promotes the rejected draft to faithful doctrine. The envelope is capped at two excerpts of 360 characters each.
lessons.md:51:- **Fix applied**: A narrow doctrine-phrase override routes `inner truth/truth/meaning/nature of suffering` and `suffering state` to `QUERY` only when first-person or acute self-harm language is absent. A fresh production control returned grounded `QUERY`; an acute self-harm control remained blocked `DISTRESS` with no citations.
lessons.md:88:- **Fix applied**: Ran `py_compile`, the regex safety gate, `git diff --check`, the corpus-file guard, and a dependency-free source check where possible. The new regression test is committed for the dependency-complete CI/production test image; no package was installed.
lessons.md:89:- **How to prevent**: Separate static/source gates from dependency-complete test results in release reports. Never claim the pytest suite passed when the required runtime dependencies are absent.
lessons.md:95:- **Fix applied**: Re-ran smoke tests using the repository-confirmed route and token contract. Health was healthy; simple English completed in 14.67s pipeline latency, comparative completed in 31.39s and honestly abstained with zero verified sources, and Hindi completed in 19.90s with faithfulness 0.80. The comparative node trace identified retrieval (7.74s), decomposition (2.68s), and intent routing (1.04s) as the dominant measured stages.
lessons.md:105:- **Fix applied**: Added an allowlist projection for public provenance evidence fields. Private memory, attachment, prompt, safety, and arbitrary internal keys are excluded from the streaming manifest while public source text, source segment, relation, hop, confidence, rights, and entity fields remain available for the ProvenanceDrawer. Regression coverage injects private keys and verifies they are absent from the stream projection.
lessons.md:111:- **How to prevent**: Evaluate quality and latency together. A grounded answer at 37 seconds is not product-complete; optimize retrieval/decomposition/provider tails only after a held-out comparative benchmark confirms evidence quality does not regress.
lessons.md:137:### L-SCALE-1. Aggregate embedding overlap cannot override corpus-completeness gates
lessons.md:139:- **Fix applied**: The existing validator failed closed when any mandatory corpus source was unavailable or any category fell below `0.85`; `EMBEDDING_BACKEND=onnx_int8` remains off. The next run must use a reachable production Qdrant endpoint and the complete re-ingested corpus.
lessons.md:140:- **How to prevent**: Never approve a model/index migration from a single aggregate score. Require complete-source checks, per-category floors, held-out NDCG/faithfulness, and vector compatibility.
lessons.md:150:- **How to prevent**: Treat queue profiles as deployment policy. Validate at startup, keep user-memory work explicit, and change concurrency only after active-task throughput and SLA evidence.
lessons.md:168:- **How to prevent**: Do not install ad hoc packages into the agent sandbox. Distinguish syntax/security validation from dependency-complete test execution and record both outcomes.
lessons.md:172:- **Fix applied**: The frontend now auto-continues only when the local `meditationStep` already indicates an active practice. A first distress response teaches and offers Serene Mind without opening it uninvited; voluntary MEDITATION requests remain non-gated.
lessons.md:173:- **How to prevent**: Separate model intent, progress metadata, and user consent. Only an explicit user meditation request or an already active local practice may open automatically.
lessons.md:261:- **Fix applied**: `check_and_record()` runs at admission (endpoint gate) and enforces 429 immediately; a completed interaction commits the reservation via the explicit `AnonQuotaPort.claim(session_id, reservation_id)` (not a no-op — see L-REV-12 for the claim deadline); failed/cancelled/guardrail-blocked interactions release it via `AnonQuotaPort.release(session_id, reservation_id)`. `QuotaResult` gained an optional `reservation_id` token so release removes exactly the right member (Redis ZREM / memory deque removal) instead of popping the latest.
lessons.md:271:- **Fix applied**: Under the reservation model the success path claims explicitly via `_claim_anon_quota` right after `result = await pipeline_task` (the `completed = True` flag set there gates whether the `_sse` finally releases on pipeline failed/timed out/cancelled), and the blocked branch releases (guardrail-blocked streams stay uncharged, preserving historical behavior).
lessons.md:277:- **How to prevent**: Validate numeric metadata at the boundary and reject `bool` explicitly; `None` means "absent", never a value.
lessons.md:284:### L-REV-1. Quota gates must inspect, never consume; charge only on completed work
lessons.md:342:- **Fix applied**: Backend returns `429` with `{quota_exceeded: true, retry_after_seconds, remaining, total_limit}`. Frontend `httpStatusToErrorCode()` maps that body to the new `quota_exceeded` `MessageErrorKind`. `buildMessageError()` renders a friendly error with `actionLabel: 'sign_in'`. `ChatInterface` tracks `quotaExceeded` state, disables the composer, and renders a `QuotaAuthPrompt` banner above the composer pointing to `/auth`.
lessons.md:364:- **Fix applied**: Removed the auto-pass. The citation-section strip (`re.sub(r"📚 \*Sources & Teachings:\*.*")`) stays so remaining sentences are scored honestly against `lettuce_detect_threshold` + `max_sim`.
lessons.md:454:### L-PAR-13. Erasure flows fail CLOSED: vector deletion must complete before plaintext row delete
lessons.md:477:- **How to prevent**: When a rate limiter swaps backend, audit every teardown/reset path; in-memory-only resets silently miss the Redis window. Test-backed limiters need a reset that covers all active backends.
lessons.md:496:- **What**: The embedding-dimension warm-up canary logged ERROR on mismatch but left `startup_complete=True` — `/api/health` reported ready while Qdrant dense search was guaranteed to fail (the Jul 16 dimension-drift incident, recurring).
lessons.md:498:- **How to prevent**: Startup validation failures must write the shared readiness signal (`startup_error`/`startup_complete`), never only a log line; the health endpoint is the only consumer ops watches.
lessons.md:544:- **Fix applied**: Bulk Python script replaced all 74 with `except Exception as _e: logger.debug("[context] suppressed non-critical error: %s", _e)`. Zero remaining in production as of this commit.
lessons.md:622:- **What**: `test_anonymous_session_purge.py` and `test_cleanup_inactive.py` both did `for key in list(sys.modules): if key.startswith("scripts.ops"): del sys.modules[key]` to reimport `cleanup_inactive_user_data` with patched `create_client`. But this evicted ALL `scripts.ops.*` including `hallucination_anomaly`, which `test_hallucination_anomaly.py` had imported at collection time. The hallucination test held a reference to the OLD (deleted) `run_anomaly_check`; `mock.patch("scripts.ops.hallucination_anomaly._fetch_responses")` targeted a NEW re-imported instance → real `_fetch_responses` ran → DNS error.
lessons.md:624:- **How to prevent**: Never blanket-evict `sys.modules` by prefix. Evict ONLY the specific module (`sys.modules.pop("scripts.ops.cleanup_inactive_user_data", None)`) + its parent namespace package (`scripts.ops`) so the reimport gets a fresh module. Other submodules stay cached and their references remain valid.
lessons.md:632:- **What**: `conftest.py` overrode `REDIS_URL` to `127.0.0.1` (line 33) but NOT `QDRANT_URL`. `backend/.env` (symlinked) carries docker hostnames (`qdrant:6333`, `supabase:54321`) that don't resolve on the host. Tests that reached qdrant got `ConnectError: [Errno 8] nodename nor servname provided` — but ONLY in the full suite (standalone tests that didn't hit qdrant passed). The env override was half-complete.
lessons.md:684:- **What**: Blue stores verbatim transcript (`video_enhanced`) and abstractive RAPTOR summaries (`summary`) for the same source, **sharing one `chunk_index` space** — both have a chunk at index 0 and index 1. `_list_source_groups` sorted by `chunk_index` alone, so reconstruction interleaved them. Result, stored as doctrine: `"a new generation ion with oneself"`, `"you are the whole. fter a few months"`, `"you enter a when you live in a beautiful state"`.
lessons.md:702:- **What**: Re-ingested source `5hNCT4duOgc` from blue with every gate active: **82 contextual chunks in, 1 written**. Not a bug — the gate was correct. That source is **46.4% contaminated in blue** (247/532 chunks carry chain-of-thought like `2. **Interpret the Input`), and re-ingest reconstructs the document *from that text*, so the poison flows straight through.
lessons.md:712:- **How to prevent**: Resolve layout-dependent paths by **probing which candidate exists** (repo layout, then image layout, then a writable fallback) and allow an env override. And treat a checkpoint write failure as at least an ERROR — a resume checkpoint that cannot be written is not a degraded feature, it is an absent one.
lessons.md:759:- **What**: Admitting spellings that many independent sources agree on is a sound way to learn ordinary English — until a *systematic* ASR error qualifies. Measured: `akam` is absent from the books yet appears in **26 sources / 401 uses**, because Whisper mishears `Ekam` the same way every time. Once admitted it becomes permanently uncorrectable, since vocabulary membership is treated as proof of correctness.
lessons.md:771:- **How to prevent**: Every lesson whose prevention clause says "always X" or "never Y" must be paired with a test that fails when X is absent — otherwise it is documentation of a habit nobody is keeping. Concretely from today: a test asserting the corrector preserves length on a realistic chunk; a test asserting no summary chunk reaches transcript reconstruction; a test asserting the pooling default matches what the collection contains. **A lesson without a failing test is a wish.**
lessons.md:778:- **What**: Fixed-character and basic recursive chunking cut paragraphs mid-thought or mid-fable. `SemanticChunker` computes sentence-level embedding cosine distances using BGE-M3 and splits text at dynamic distance spikes ($85^{\text{th}}$–$90^{\text{th}}$ percentile), producing complete, self-contained semantic topic clusters (e.g. 6 topic clusters vs 14 fixed window splits).
lessons.md:782:### L2. Automated LLM Proofreading Prevents Reactive Term Whack-a-Mole
lessons.md:784:> This lesson is correct about the *motivation* and dangerously incomplete about the *risk*.
lessons.md:789:- **What**: Hardcoded term glossaries (`doctrine_terms.py`) are reactive and incomplete. Integrating an LLM proofreader (`TranscriptCorrector`) into `ContextualReingestEngine._reconstruct_full_text()` automatically proofreads raw ASR auto-captions zero-shot (*"eye consciousness"* $\rightarrow$ **"I-Consciousness"**, *"soul sink"* $\rightarrow$ **"Soul Sync"**, *"uncrafted"* $\rightarrow$ **"unclouded"**) *before* chunking or contextual header generation.
lessons.md:929:- **Why:** A default changed in anticipation of a migration that never completed.
lessons.md:970:- **How to prevent:** Before asserting "landed", verify by content (`grep` for a distinctive symbol from each edit), not by memory of having made the edit. Regression tests that fail when the fix is absent are the cheapest tripwire — they are what caught this. For work that matters, commit to a branch rather than leaving it in the working tree.
lessons.md:1111:- **Lesson**: Add to any filter-repo runbook: "answer N to continuation prompt; re-add remote after run completes."
lessons.md:1127:## Jul 31, 2026 — Proactive Healing Course Assignment (Task 10)
lessons.md:1132:- **Fix/Pattern:** `healing_course_service.assign_course_if_needed` wraps both the active-course check and the upsert in `asyncio.to_thread`. Route handlers (Task 11) must reuse `assign_course_if_needed` / `maybe_assign_healing_course` rather than inlining awaited client chains.
lessons.md:1155:- **Incident/Observation:** Direct instantiations of `QdrantClient` in `second_brain/vault_index.py`, `semantic_adapter.py`, `pipeline.py`, `reprocess_contextual.py`, and `phase05_audit.py` lacked explicit `api_key` configuration, causing authorization failures when Qdrant API key enforcement was active.
lessons.md:1195:- **Problem:** `adapter.invalidate_by_query(query_text, 5.0)` had an internal Qdrant client timeout of 5s, and `asyncio.wait_for(task, timeout=5.0)` also used 5s. If both fire simultaneously, `wait_for` cancels the future, the `async with` block exits releasing the semaphore, but the sync thread (from `asyncio.to_thread`) continues running Qdrant/Redis I/O in the background. The semaphore is released while the worker is still active.
lessons.md:1196:- **Fix:** Set wrapper timeout to 10.0s (5s safety margin above the operation timeout). Use `asyncio.shield(future)` to prevent the future from being cancelled on timeout. On `TimeoutError`, await the shielded future with a second 10.0s timeout — the semaphore stays held until the worker actually completes.
lessons.md:1211:- **Fix:** Used a nested try/except: outer block (holds semaphore) → inner `shield(wait_for)` (detects timeout) → on timeout, second `wait_for(future)` (waits for actual thread completion). The semaphore is released only after both awaitables complete, even if the first timed out.
lessons.md:1270:  3. *Tier 3 (Core User Memories & Second Brain Vault)*: Retained while active; accounts inactive $>365\text{ days}$ auto-purged via `scripts/ops/cleanup_inactive_user_data.py`.
lessons.md:1408:  │      └─ ingestion_pipeline_runner() → updates job status pending→running→completed/failed
lessons.md:1415:             4. `finish_playlist_ingestion` updates parent job → completed/failed
lessons.md:1428:      ├─ On success: job status → 'completed'
lessons.md:1440:- Status CHECK: `'pending'`, `'running'`, `'completed'`, `'failed'` — NOT `'queued'`
lessons.md:1475:Option 2 is simpler but blocks the response until full pipeline completes if not backgrounded.
lessons.md:1489:- **Qdrant**: `spiritual_wisdom` collection has 89,053 points, 165,689 indexed vectors, status green. Other collections (vault, lightrag, semantic cache, global_memory) all empty.
lessons.md:1702:1. **Refined Serialization Guard**: Use a ref `isSavingRef.current` to determine if a save is currently active. If no save is active, invoke the save function synchronously (which yields a 1-tick microtask delay just like the original code). Only chain on the promise ref if a save is already in progress.
lessons.md:1713:1. `start_railway.py` sends `lifespan.startup.complete` immediately (correct)
lessons.md:1725:- The ASGI wrapper's `lifespan.startup.complete` is sent BEFORE `create_task`, so uvicorn starts accepting connections while the import runs in the background thread
lessons.md:1740:When an ASGI wrapper sends `startup.complete` and then starts background init via `create_task`:
lessons.md:1784:Root cause: `start_railway.py` sent `lifespan.startup.complete` without first calling `receive()` to consume the `lifespan.startup` message from uvicorn. The ASGI lifespan protocol requires:
lessons.md:1787:3. App sends `{"type": "lifespan.startup.complete"}`
lessons.md:1793:1. Wrapper sends startup.complete without consuming startup message
lessons.md:1812:await send({"type": "lifespan.startup.complete"})
lessons.md:1816:await send({"type": "lifespan.shutdown.complete"})
lessons.md:1885:## Jul 16, 2026 — Background Init: asyncio.create_task Never Scheduled, scheduler_shutdown, startup_complete
lessons.md:1889:- **Fix**: **Stop using `create_task` for critical init.** Run all init inline in the lifespan before `yield`, wrapped in `asyncio.wait_for(timeout=180)`. The `start_railway.py` wrapper returns 200 for `/api/healthz` for 90s, so Railway won't kill the container. The real `/api/health` returns `ready: false` until `startup_complete = True` at the end of the inline init.
lessons.md:1897:### startup_complete should reflect actual deferred init state
lessons.md:1898:- **Problem**: `startup_complete = True` was set immediately after `startup()` (synchronous container build), before LightRAG init, ontology seeding, or queues started. The health endpoint reported "ready" while critical services were still uninitialized.
lessons.md:1899:- **Fix**: Changed to `app.dependencies.startup_complete = False` initially. Only set to `True` at the end of `_background_startup_body`, after all deferred init completes. Timeout/failure paths keep it `False` and set `startup_error`.
lessons.md:1900:- **Pattern**: `startup_complete` should represent "all initialization finished", not "core container constructed". The ASGI wrapper provides a grace period for `/api/healthz`; the real `/api/health` exposes the true state.
lessons.md:1904:- **Pattern**: For health probes, distinguish between "service object exists" (created at construction) and "service worker is active" (started later). Report the former as healthy; the latter as degraded if non-critical.
lessons.md:1920:- **Problem**: `_background_startup` called `await asyncio.to_thread(container.embedding._ensure_encoder)` but the encoder was already loaded during `startup()` (ContainerBuilder calls it during graph build). The redundant `to_thread` could deadlock if the thread pool was starved by concurrent `prewarm_remaining` downloads.
lessons.md:1924:### startup_complete should be set early, not deferred to background init
lessons.md:1925:- **Problem**: `startup_complete = False` was set in the lifespan, and only set to `True` after `_background_startup` completed (LightRAG, ontology, schedulers, queues). If background init hung, `/api/health` never reported services.
lessons.md:1926:- **Fix**: Set `startup_complete = True` immediately after `startup()` (synchronous container build). Background init is fire-and-forget.
lessons.md:1927:- **Pattern**: The health endpoint should reflect the container's actual state (which services are up/down), not whether background tasks finished. Set `startup_complete` when the core container is ready, not when optional init completes.
lessons.md:1977:- **Problem**: In multi-step interactive wizard/preview cards (like the `DemoModal` slides), if each slide has a different content height (e.g., Slide 1 has a chat transcript, Slide 2 has a breathing guide, Slide 3 has a graph), the entire modal container will continuously resize and shift height as the user clicks "Next". This creates a jittery, unpolished feel.
lessons.md:2036:- **Pattern**: Swagger docs expose the complete API surface. Gate them behind `IS_PRODUCTION` in `main.py` when creating the FastAPI app.
lessons.md:2099:- **Problem**: Animating elements with framer-motion that are absolutely positioned (`absolute right-0 top-0`) can be broken if a Tailwind `relative` class is accidentally appended at the end of the `className`. The `relative` style overrides the absolute placement, causing the drawer to display inline or be completely hidden.
lessons.md:2137:- **Pattern**: A grep returning nothing is ambiguous between "absent" and "wrong cwd". Print `pwd` when a search unexpectedly comes back empty.
lessons.md:2172:- **Problem**: Backend runs gunicorn with 2 workers and `--preload`. Under concurrent load (3+ clients), requests queue behind active ones. A single request can take 60-120s (OpenRouter model inference), so queued requests hit 180-360s timeouts.
lessons.md:2189:- **Pattern**: Interactive actions that are hidden by default should be placed outside the content card wrapper to prevent layout spacing inflation.
lessons.md:2203:- **Fix**: Remove the `origin` query parameter from the `iframe` source URL and render a persistent "Watch on YouTube ↗" link absolute-positioned over the active iframe container when hovered.
lessons.md:2211:### Keep active chat composer outside the scroll transcript
lessons.md:2213:- **Fix**: Render the active composer as a separate flex child below the scroll region, reserve transcript bottom padding, and move floating voice/status indicators above the composer.
lessons.md:2214:- **Pattern**: ChatGPT/Claude-style active chat layouts should be `header → scroll transcript → fixed composer rail`; only empty-state composer belongs inside centered hero content.
lessons.md:2240:- **Problem**: In intent routing, checking if the session is active coerced `meditation_step` only if it was a string and caught only `ValueError`, leaving `None` or other types vulnerable to comparison crashes.
lessons.md:2241:- **Fix**: Standardized the active-session check in `intent.py` using `int(raw_step)` wrapped in a `try/except` block catching both `TypeError` and `ValueError`, reverting to `0` on any parsing exception.
lessons.md:2299:- When extracting CSS color strings into component props, ensure HSL functions are syntactically complete.
lessons.md:2337:- **Results**: Full 90K scan completed in ~25s. Missing language dropped from 44.7% to 7%. Remaining 7% are videos with <100 chars transcript content (below `--min-chars` threshold).
lessons.md:2361:- **Lesson**: Qdrant defaults are fine for small collections but suboptimal for 90K+ points with 1024d vectors. Always set HNSW params explicitly. Payload indexes must exist for every filterable field at collection creation time — retroactively adding them requires Qdrant to rebuild indexes for existing points.
lessons.md:2427:- The quality verification script assumed BGE-M3 model collection suffix (`baai_bge_m3_1024d`), but the active model was `intfloat/multilingual-e5-large-instruct` (`intfloat_multilingual_e5_large_instruct_1024d`), leading to false quality check failures.
lessons.md:2429:- **Lesson**: Never hardcode exact collection names when they contain model identifiers. Use wildcard/prefix matching or read from active settings to maintain robust quality audit coverage.
lessons.md:2440:- LightRAG's `llm_func` also routes through OpenRouter → entity/keyword extraction silent-failed → **Neo4j completely empty**
lessons.md:2509:- **Fix**: Added `claude-3-7-sonnet-20250219` to `SUPPORTED_CLAUDE_MODELS` list in `api/model_catalog.py` of `free-claude-code`. Modified `~/.claude/settings.json` to change the active model to `claude-3-7-sonnet-20250219` and removed the `"availableModels"` array allowlist entirely.
lessons.md:2510:- **Lesson**: When `"availableModels"` is defined in `settings.json`, the Claude Code client strictly enforces it and completely bypasses querying the proxy's `/v1/models` endpoint for discovery. Removing `"availableModels"` entirely enables dynamic model discovery from the proxy gateway's `/v1/models` endpoint, populating the `/model` picker with all configured provider models.
lessons.md:2513:- **Problem**: Needed to finalize the Teacher-Student Ralph loop verification and construct an interactive, premium visual RAG pipeline graph in the Admin console without breaking React routing or Vite builds.
lessons.md:2529:- **Lesson**: Background async workers must have comprehensive error handling to avoid silently discarding queue events on signature mismatch crashes. Always declare and validate all active pipeline configurations inside Pydantic `Settings` to prevent runtime `AttributeError` crashes when enabling gated flags.
lessons.md:2549:- **Fix:** Restructured the function into four phases: (1) identify orphan citation indices by checking `retrieved_docs` range and URL presence, (2) split the normalized text into alternating `[text, marker, text, marker, ...]` segments via `re.split` with capture group, (3) rebuild by dropping orphan markers AND the text segment preceding them (the text the orphan marker claimed), (4) delegate remaining orphan markers to `strip_orphan_markers` and verify grounding on the cleaned text. A valid marker keeps both itself and its preceding text.
lessons.md:2559:- **Fix:** Wrapped in `asyncio.to_thread` — starts the thread before `asyncio.gather` for primary vector results, then awaits the BM25 future after gather completes.
lessons.md:2619:- **Fix:** Moved the truncation after the full pagination loop completes. Each page is collected; only at the end is the list sliced to `overall_limit`.
lessons.md:2639:- **Fix:** Removed `r"\bdeep\b"` entry. Kept `r"\bgo deeper\b"` and remaining patterns.
lessons.md:2655:- **Pattern:** Regex patterns that match citation markers must handle all active formats in the codebase. Use `re.split` with capture groups when you need to preserve delimiters during reassembly.
lessons.md:2673:- **Problem:** `test_memory_scored_retrieval.py` had no assertion that the lower-scoring duplicate claim ("Seeker includes Soul Sync in daily routine") was absent from the context.
lessons.md:2675:- **Pattern:** Dedup tests must assert both the positive (unique claim appears) and the negative (lower-scoring duplicate is absent). A dedup test that only checks presence can never catch dedup failures.
lessons.md:2738:- **Context Window Management**: Balanced the context window by increasing the frontend "active" message slice (last 20 messages) while maintaining long-term coherence through the generated summary system message.
lessons.md:2765:- **Query Trace Details Wiring (Unit 9)**: Implemented `get_query_trace` in `telemetry_db.py` to query telemetry tables (`chat_queries`, `chat_responses`, `retrieval_events`, `trace_spans`, `trigger_events`, `safety_events`) sequentially by `query_id` and compile the detailed trace metadata. Exposed this via auth-guarded `/api/admin/traces/{trace_id}` endpoint in the FastAPI backend, and updated the React admin frontend `api.ts` to request backend first with dev fallback. This completes the wiring from the Admin Console to the telemetry database, enabling admin debugging of live RAG inputs and outputs.
lessons.md:2772:- **Traceability in Database UIs**: To ensure complete clarity when an administrator views vector database collections or knowledge graphs (Neo4j), prepending a clear, human-readable source header (e.g. `[Source: The_Four_Sacred_Secrets.pdf | Chapter: {context_title}]\n` or `[Source: YouTube Video: {title} (URL: {url})]\n`) directly to the chunk text guarantees that the source of every piece of knowledge is instantly identifiable, fulfilling strict audit and lineage requirements.
lessons.md:2792:- **Adaptive Chunk Evaluation**: Implemented Size Compliance (SC) and Intrachunk Cohesion (ICC) metrics to dynamically grade and select the best chunking candidates (Semantic Split vs. Recursive Split) on the fly using preview text sampling, completely eliminating manual text heuristics.
lessons.md:2796:- **Safe Cache Operations**: Added a unified `make flush-cache` task which safely deletes the query-side semantic caches (GPTCache database files and Redis keys). Because the ingestion pipeline is an isolated write-only ETL process targeting Qdrant and Neo4j and maintains checkpoints in `scripts/ingestion_state.json`, flushing transient query caches has **zero impact** on active or pending document indexing runs.
lessons.md:2797:- **Premium Chat Auto-Scrolling**: Implemented a highly responsive `ResizeObserver` layout-tracking pattern on the chat message wrapper. If a user is near the bottom, it locks the scroll position to the bottom automatically when new tokens arrive or message lists shrink (during regenerate/inline edits), completely preventing disorienting jumps. Manual scrolling up immediately suspends snap-locking to allow uninterrupted history reading.
lessons.md:2823:Before claiming a feature is "production-ready," verify:
lessons.md:2829:- **Automatic Multi-Stage Self-Healing**: Instead of relying on manual runner commands or standalone scripts, the ingestion pipeline now automatically runs the complete self-healing and data quality suite at the end of every raw text ingestion batch in [pipeline.py](file:///Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/backend/ingest/pipeline.py#L1010-L1099). This automatically deduplicates entities, deletes orphaned Neo4j nodes (0 relationships), prunes corrupted entity type names, and synchronizes Qdrant by scrolling through collections and pruning points that do not map to Neo4j, guaranteeing database integrity.
lessons.md:2936:**Root Cause**: Three backend routes (`routers/admin.py`, `app/trace_dashboard.py`, `app/main.py::ingest_status_endpoint`) used `current_active_user` from `fastapi-users`, which only accepts **FastAPI-Users-issued local JWTs** (from the internal SQLite user DB). The admin frontend authenticates via **Supabase** and sends Supabase JWTs. These two auth systems are completely separate; FastAPI-Users rejects Supabase tokens as invalid.
lessons.md:2941:- `current_active_user` → FastAPI-Users (local DB) — for internal/legacy use only
lessons.md:2944:Any new protected endpoint MUST use `get_current_user_from_supabase`. Do not add new uses of `current_active_user`.
lessons.md:2948:### Root Cause 3: Supabase appeared "not running" but was already active on correct ports
lessons.md:3260:class CircuitBreakerRegistry:       # Registry + active provider management
lessons.md:3269:- **Unified API**: `container.circuit_breaker_registry.get_active().can_execute()`
lessons.md:3283:active_breaker = container.circuit_breaker_registry.get_active()
lessons.md:3284:if active_breaker and not active_breaker.can_execute():
lessons.md:3287:# Manual reset (works for any active provider)
lessons.md:3363:- State: `reflectionStep: 0|1|2|3`, `selectedMood`, `journalText`, `gratitudeText`. All saved to `meditationStorage` via the new `extras` parameter of `completeMeditationSession`.
lessons.md:3439:  This spawns a background `caffeinate` process that monitors the current Python script's PID. It keeps the computer fully awake for the exact duration of the ingestion and automatically self-terminates when the Python process completes or exits.
lessons.md:3454:- **Problem**: When LightRAG initialized its Qdrant integration, it logged warnings complaining that collections (`lightrag_vdb_entities`, `lightrag_vdb_relationships`, etc.) were missing a sanitized model name suffix. This occurred because `EmbeddingFunc` was initialized without the `model_name` parameter, raising risks of cross-workspace data collisions if multiple models were active on the same Qdrant node.
lessons.md:3456:- **Architectural Lesson**: Always provide an explicit model name to vector database interface wrappers. Sanitize and append this model name as a suffix to all dynamically created collection names to prevent index bleed and ensure complete tenant/workspace data isolation.
lessons.md:3460:- **Solution**: Refactored `SarvamCloudService.rewrite_query` to support both `reasons` and `grading_reasons` as inputs, gracefully mapping them to maintain complete backward compatibility and absolute interface alignment.
lessons.md:3469:  - **Strict Keyword Preservation**: Ensured that all public-facing LLM methods (e.g., `generate`, `_generate_fast`) forward `**kwargs` completely to `_call_api` rather than silently dropping custom operational parameters.
lessons.md:3492:- **Resumption Advantage**: Combined with the persistent, atomic state tracking in `scripts/ingestion_state.json`, stopping the pipeline and running it again allows us to skip all successfully indexed videos and run a targeted recovery sweep using the patched code on the remaining queue.
lessons.md:3498:  - **Hybrid Cookies Integration (Method A & B)**: Designed and implemented a hybrid dual-bypass cookies strategy across all four yt-dlp entry points. If a local `cookies.txt` is present (Method A), the loader/downloader uses it directly. If no `cookies.txt` is found, the system automatically falls back to dynamically extracting active session cookies from the local **Google Chrome** installation (Method B) via the Python `cookiesfrombrowser` parameter and the CLI `--cookies-from-browser chrome` option. This offers zero-config, out-of-the-box bot-bypass for host-based execution.
lessons.md:3502:- **macOS System GUI Boundary**: Because of macOS's native sandboxing and credential protection, when a command-line tool (like `yt-dlp` or `python` inside a virtual environment) attempts to read Google Chrome's **"Chrome Safe Storage"** keychain item (which holds the symmetric key for browser cookies), macOS will *always* trigger an interactive **system GUI dialog popup** to confirm consent.
lessons.md:3504:- **Cache Minimization**: Refactoring `youtube_loader.py` and `whisper_local_service.py` to use a persistent `cookies.txt` file (which is only refreshed when downloads actively fail or expire) ensures that the keychain is almost never read under normal operation, reducing user-facing prompts to near-zero.
lessons.md:3535:- **Solution 1**: Modified the profile sync merge logic to safeguard active local non-default selections. The frontend merge prevents asynchronous backend loads from overwriting custom language selections made dynamically during the session.
lessons.md:3536:- **Problem 2 (Playwright Speech API Mocking)**: In modern Chromium environments, `window.speechSynthesis` is a read-only, non-configurable property. Direct assignments like `window.speechSynthesis = ...` in Playwright initialization scripts fail silently, causing the browser to fallback to real system speech voices (which are active on macOS hosts). This bypasses the synthetic voice failure/fallback path and causes E2E tests for TTS failure toasts to fail.
lessons.md:3549:- **Result**: LightRAG now returns the raw, structured retrieved graph context (entities, relations) within **1–2 seconds** instead of 20 seconds, completely bypassing LightRAG's internal synthesis phase. The raw context is then fed directly into the primary LangGraph generation pipeline, eliminating redundant LLM calls, reducing API costs, and resolving the connection timeout issue entirely.
lessons.md:3572:- **Problem**: When deploying reasoning-capable models (such as `sarvam-30b`) on highly structured, non-conversational operations (like topic extraction, text correction, classification, and metadata grading), the model might consume all of its token allotment on intermediate thought chains (`<think>` blocks). This results in a completely empty main `content` string, which pollutes the knowledge graph and vector database. In addition, standard Radix UI `<ScrollArea>` components placed within absolute popup layouts (like the multilingual `LanguageSelector`) perform complex viewport calculations that often fail in constrained viewports, causing the dropdown to clip or collapse entirely.
lessons.md:3596:- **Lesson learned**: In pipeline processes that mix remote web scrapers, local models, and remote LLM correction steps, rate limits and hardware boundaries must be policed proactively via inline throttle delays, rolling-window rate limiters, and exponential backoffs. For local deep-learning operations on Apple Silicon (MPS), always enable the PyTorch MPS fallback environment flag to prevent unexpected `NotImplementedError` crashes. Always write state instantly ("per-item") when processing items that take several seconds, as disk overhead is negligible compared to the cost of lost work.
lessons.md:3599:- **Problem**: When fetching large lists of YouTube video transcripts using the third-party `johnvc/YoutubeTranscripts` Apify actor, submitting large batches of video URLs (e.g., `BATCH_SIZE = 50`) frequently caused the Apify actor executions to hit the platform's hard 5-minute timeout window. This resulted in aborted actor runs, incomplete datasets, and frequent transient timeouts showing up in the Apify console.
lessons.md:3600:- **Solution**: Reduced the active URL batch size to `10` (`BATCH_SIZE = 10` in [extract_transcripts.py](file:///Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/scripts/ingestion/extract_transcripts.py)). This keeps individual actor runs small, fast, and guaranteed to finish well within the 5-minute execution window while utilizing the script's existing atomic checkpointing to seamlessly resume work across runs.
lessons.md:3604:- **Problem**: When managing hundreds of spiritual video transcripts, minor scraping errors (such as partial transcript segments returned by YouTube, or Apify actor execution timeouts) would slip through the pipeline, writing corrupted or incomplete `.md` files to the knowledge base. Hardcoding sensitive API credentials in source files also blocked commits under strict GitHub Push Protection scans, and running long-running operations sequentially introduced severe latency.
lessons.md:3606:  - **100% Data Quality Gate**: Implemented `validate_transcript_completeness()` in `extract_transcripts.py` requiring both a **95% timestamp coverage ratio** (last segment time vs video length) and a **30 WPM minimum density check**, preventing any partial or corrupt transcripts from being saved.
lessons.md:3607:  - **Resilient Polling & Categorization**: Reduced Apify scraper batch sizes from `10` to `5` with a `180s` timeout. Restructured the tracking state to categorize issues into `incomplete` (fails quality threshold) and `timeout_victims` (actor aborted), separating them from permanent failures.
lessons.md:3611:  - **Interactive Audit Mode**: Added `--audit` flag to recursively validate all pre-existing transcripts, successfully flagging and re-queuing corrupt files (such as an empty LLM error response).
lessons.md:3612:  - **Git push Protection Secret Removal**: Safely removed hardcoded tokens using a Git soft-reset, squashing 5 commits into a single high-quality commit with zero secrets in its history, completely resolving GitHub's Push Protection block.
lessons.md:3616:- **Problem**: During bulk ingestion, extracting entities and relations into the LightRAG Knowledge Graph (Layer 3) represents the most fragile phase of the pipeline due to strict external LLM API rate limits (e.g. 60 RPM on Sarvam Cloud). If a single chunk insertion failed due to rate limits or transient network drops, the entire video ingestion would either crash or skip that chunk permanently, leading to incomplete knowledge graphs. There was also no lightweight recovery path to retry failed chunks without re-running full time-consuming Qdrant/Whisper pipelines from scratch.
lessons.md:3619:  - **Persistent Failed Chunk Tracking**: When a chunk completely fails all 3 attempts, it is persistently serialized into `scripts/ingestion_state.json` under a `failed_lightrag_chunks` array, preserving the exact source name, video ID (if applicable), chunk index, total chunks, actual chunk text, error details, and attempt counts.
lessons.md:3668:- **Problem**: When expanding the cognitive capabilities of multiple local and global AI agents (such as Hermes and standard workspaces), skills often need to be imported dynamically from scattered community GitHub repositories. Furthermore, in non-interactive background agent sandboxes, running standard `git push` on newly provisioned files fails because the macOS Keychain helper (`osxkeychain`) fails with authorization errors (`errSecAuthFailed -25293`) without a GUI session to prompt for user authentication, and the SSH agent has no active identities.
lessons.md:3673:- **Lesson learned**: When provisioning multi-agent system skills from public repositories, write standalone download-and-standardize scripts that enforce standard frontmatter and run locally. Since keychain helpers fail in sandboxed background shells, stage ignored/parent-ignored directories directly via `git add -f` and direct users to run the final push in their interactive terminals where secure keychains can be unlocked.
lessons.md:3690:  - Manually fixed all remaining Ruff linter warnings (e.g. renaming ambiguous loop variables `l` to `lang`/`line`, removing unused `NoTranscriptFound` and `torch` imports).
lessons.md:3691:- **Lesson learned**: Always use standard library typing objects (like `typing.Callable` or `collections.abc.Callable`) for union type hinting rather than the built-in function `callable` which does not support the `|` operator across Python versions. Ensure build dependencies only reflect actual active runtime imports to prevent CI/CD environments from breaking on heavy or incompatible transitive packages.
lessons.md:3706:- **Lesson learned**: Never store state inside container boundaries. Always externalize databases, vector stores, and model caches to persistent named volumes, and mount code directly for hot-reloading to ensure that data is completely insulated from container lifecycles.
lessons.md:3803:  - **Chat Interface Scroll Layout Defect**: In the chat view, parent flex columns defaults to `min-height: auto`, allowing them to grow vertically to fit overflow content instead of shrinking within the viewport height. This pushes the message input area and navigation footer completely off-screen.
lessons.md:3804:  - **LangGraph RAG Pipeline Timeouts**: High-latency upstream reasoning calls on auxiliary nodes (intent classification, knowledge tree navigation) taking over 30s can hit the hard 40s LangGraph wrapper timeout, resulting in complete service timeouts.
lessons.md:3837:- **Lesson learned**: When writing E2E tests for progressive web applications (PWAs) with active Service Workers, always mock or stub `navigator.serviceWorker` in an initialization script to prevent worker-level network interception from bypassing Playwright's route mocks. Ensure all page-evaluation callbacks are strictly self-contained and free of environment-specific build constants.
lessons.md:3842:  - **Chat Gating Bypass and Interception**: When the chat is gated (locked) due to distress alerts, the input area was completely disabled. This prevented the user from asking the chatbot to "open Serene Mind" or "do Serene Mind now" to unlock the chat, forcing them to interact exclusively via clicking overlays.
lessons.md:3847:  - **Interactive Gate Interception**: Unlocked the message input area during the gated state, allowing users to type. In `handleSubmit`, any messages sent while gated are sent to the backend; if the LLM react agent classifies the intent as `MEDITATION`, the Serene Mind practice opens; otherwise, the response is blocked and a helpful reminder is displayed, preventing any chat bypass.
lessons.md:3848:- **Lesson learned**: Implement a configurable state machine to separate visual breathing visualizations from timing parameters. Keep input controls active during modal-gated sessions to allow natural dialogue commands, but intercept and route those messages through intent classification to enforce compliance without blocking conversational entrypoints.
lessons.md:3853:  - **Lack of High-Fidelity Audio/Video controls**: The audio and video tabs simply displayed raw YouTube iframe embeds, which looked generic and did not present the meditation instructions sequentially or allow interactive playback state synchronization.
lessons.md:3856:  - **Step-by-Step Practice Guide Card**: Rendered a structured card showing the 5 core steps of the practice in the Breathing tab, dynamically highlighting the active step matching the current breathing phase.
lessons.md:3857:  - **Premium Custom Audio Player**: Created a custom glass-card audio player UI for the Audio tab. The YouTube iframe is hidden, and seekers interact with custom Play/Pause/Reset buttons that send `postMessage` player commands to control the off-screen audio stream. Built a 3-minute simulated seekbar, showing elapsed/total time, and dynamically highlighted the active step of the 3-minute progression (0-30s Step 1, 30-60s Step 2, etc.) in a gorgeous indicator list.
lessons.md:3859:- **Lesson learned**: When integrating guided video/audio meditations into a custom web application, hide the raw cross-origin iframe to prevent generic player branding, and construct a bespoke frontend wrapper that communicates with the iframe via `postMessage`. This enables rich custom progress tracks, animated pulsing graphics, and interactive step highlights that synchronize directly with playback.
lessons.md:3879:- **Lesson learned**: When developing with deep reasoning models, always isolate persona constraints, instructions, and formatting rules into the `system` message role to keep reasoning private and prevent thought chains or formatting rules from leaking into user-facing content. Additionally, proactively scale and cap the token budget to the exact limit of the subscription tier (e.g., `4096` tokens) to ensure the model has enough room to complete its reasoning trace without triggering fatal Bad Request errors.
lessons.md:3910:- **Problem**: No in-flight request tracking. When Kubernetes sends SIGTERM during rolling updates, the lifespan exit would immediately tear down services (Neo4j, Qdrant connections, embedding models) even with active requests in the 20-node LangGraph pipeline. Mid-graph requests would produce abrupt client errors.
lessons.md:3912:- **Lesson learned**: The correct order for graceful Gunicorn shutdown is: (1) stop accepting new connections (handled by Gunicorn's `graceful_timeout`), (2) wait for in-flight requests (app-level drain), (3) teardown services. Step 2 requires explicit application-level tracking; Gunicorn alone does not wait for streaming SSE responses to complete.
lessons.md:3925:- **Problem**: When a reasoning model thinks out loud during ingestion and writes contaminated instructions (or incomplete reasoning blocks missing `<think>` termination) into the knowledge graph, in-place cleaning requires regex-based `<think>` block stripping. Additionally, dummy/nonsense entities generated from prompt injection (e.g., `entity_name`, `Capitalization`, `A-Come`, `Fruit Bats`) pollute the semantic retrieval space and must be cleaned up.
lessons.md:3931:- **Problem**: The app had 3 SQLite databases (`cost_tracking.db`, `prompt_store.db`, `mukthi_users.db`) alongside Supabase. SQLite files got wiped on `make docker-rebuild-web`, causing data loss. The `mukthi_users.db` was a FastAPI-Users legacy auth DB with 0 active users.
lessons.md:3962:- **Problem**: The consolidated `backend/benchmarks/` suite existed, but the production-readiness score was incomplete: it did not restore explicit multi-turn memory scoring, citation scoring, cache/performance reporting, or Self-RAG/CoVe scoring. The `/api/chat` response also hid verifier metadata, forcing benchmarks to infer quality only from final text.
lessons.md:3995:- **Solution**: Modified `backend/benchmarks/ruthless_benchmark.py` to record and output the complete, untruncated queries and answers in the `SingleResult` objects across standard query categories, multi-turn suites, and stability test runs. This ensures the full response payload is visible in both the `results` and the `errors` keys of `ruthless_report.json`.
lessons.md:4027:- **Broken migration fix**: `20260530141027` had `UPDATE alert_rules SET active = enabled` where `enabled` never existed — removed the broken UPDATE line.
lessons.md:4107:- **Solution**: Built a config-watcher service in [config_watcher.py](file:///Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/backend/services/config_watcher.py) that uses `watchfiles` for native filesystem changes but automatically falls back to an active 5-second polling loop to inspect config file modification times when OS events fail.
lessons.md:4111:- **Problem**: Mid-stream errors (e.g., token timeouts, DB disconnects) during Server-Sent Events (SSE) streaming result in truncated TCP connections or unhandled exceptions that leave client applications waiting indefinitely on active loader states.
lessons.md:4127:- **Solution**: Built a tiered semantic router fallback in [semantic_router_fallback.py](file:///Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/backend/services/semantic_router_fallback.py). It cascades searches through: Qdrant vector retrieval ➔ Neo4j FTS (Full-Text Search) keyword index ➔ LightRAG local graph traversal, ensuring successful context acquisition even if Qdrant is completely offline.
lessons.md:4141:- **Problem**: Storing raw user prompts and chat histories in plaintext logs violates GDPR and other data privacy regulations, yet omitting logs completely makes it impossible to audit usage patterns or troubleshoot runtime errors.
lessons.md:4188:- **Solution**: Authored a comprehensive [WHATSAPP_BOT_INTEGRATION.md](file:///Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/WHATSAPP_BOT_INTEGRATION.md) detailing step-by-step instructions for Twilio and Meta Cloud API webhooks, including a complete Python + Redis webhook implementation that maintains conversation state using `whatsapp_<phone_number>` as session IDs, slices histories to 10 turns, and secures requests using JWT Bearer authentication.
lessons.md:4207:- **Solution**: Implemented dynamic query generation using the active LLM service in [ruthless_benchmark.py](file:///Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/backend/benchmarks/ruthless_benchmark.py) to construct challenging variants on the fly, and built a premium, responsive cosmic-theme HTML report generator in [generate_dashboard.py](file:///Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/backend/benchmarks/generate_dashboard.py) mapping results into SVG progress indicators, collapsible debug cards, and live healthchecks.
lessons.md:4231:- **What *Does* Work in <60s**: Simple `tier2_simple` queries that bypass the full pipeline (skipping HyDE, decomposition, tree navigation, reflection, verification, and contradiction checks) can complete in **15–30 seconds** because they reduce the call count to 3–4. However, the user explicitly asked about **complex questions**, which always hit the full chain.
lessons.md:4245:  1. `MessageList.tsx` computed `isStreaming` as `isStreamingMsg && (streamingContent ? streamingContent.length > 0 : message.content.length > 0)`. When the streaming message had no content yet, `isStreaming` became `false`, so the ChatMessage component didn't know the message was actively streaming.
lessons.md:4252:- **Lesson learned**: Never conflate "content exists" with "is streaming". `isStreaming` should track the *connection state*, not the received data state. Messages in an active stream but without tokens yet should show a contextual placeholder — a blank bubble is brokenness, not a loading state.
lessons.md:4375:   - Built a Python script to copy, rename all lowercase `skill.md` to `SKILL.md`, and completely **flatten** the directory structure to exactly one directory level under the skills root (e.g. `skills/clean-code-python-clean-tests/SKILL.md` instead of `skills/clean-code/python/clean-tests/SKILL.md`).
lessons.md:4440:- **Clean Separation of Concerns**: Endpoint definitions are completely decoupled from request processing pipelines.
lessons.md:4484:- **Solution**: Cleaned up the comment to align with the active implementation, ensuring maintainability.
lessons.md:4518:- **Problem**: There was no standard way to systematically evaluate chunking strategies (Recursive vs. Semantic) across various chunk size configurations against the complete set of five ekimetrics/adaptive-chunking metrics (SC, ICC, DCC, BI, RC).
lessons.md:4567:**Solution**: Implemented a dynamic context capping helper inside `generate_answer` (in `backend/rag/nodes/generation.py`) that calculates the estimated tokens of the entire system/user prompt combination (including base prompts and history), computes the remaining safe token budget for the knowledge layer, and crops the knowledge context dynamically prior to calling the LLM provider.
lessons.md:4582:2. If the LLM-based intent router fails due to OpenRouter rate limits (429), it defaults to a basic `FACTUAL` intent without setting `needs_web_search = True`, completely bypassing the web search node for temporal queries.
lessons.md:4714:- **Problem**: The `guru-chat` Supabase Edge Function hard-required `LOVABLE_API_KEY` at the top of `Deno.serve()`, returning a **500 crash** when the key was missing. This made the function completely unusable in environments where Lovable is not configured (local Docker, self-hosted deployments, CI).
lessons.md:5073:- **Problem**: Need to activate and register Nvidia NIM as the active LLM provider, update configurations, and ensure the provider is validated in the test suite without making live network requests.
lessons.md:5093:- **NIM/Ollama Support in MemoryService**: Added support for `nim` and `ollama` as active LLM providers in `MemoryService` for both extraction and compaction. This prevents crashes or empty returns when non-OpenRouter/Sarvam providers are selected as the active `LLM_PROVIDER` in development or testing.
lessons.md:5123:- **Root Cause**: Two state files existed: `scripts/ingestion_state.json` (active, 717 videos) and `scripts/ingestion/ingestion_state.json` (stale, 0 videos). `verify_ingestion_quality.py` was prioritizing the stale local copy.
lessons.md:5149:- **Fix**: Added sentence-boundary aware partitioning: finds the last sentence-ending punctuation (`.`, `!`, `?`) within max_chars, or fallback to last space. Then appends remaining text as overflow partition.
lessons.md:5264:- **Biggest finding**: The user's complaint that the sidebar was "just icons, not looking good" was diagnosed as a missing feature — but `DesktopSidebar.tsx` already has a complete claude.ai-style labeled/grouped sidebar (New Conversation, Serene Mind, Practices, search, date-grouped conversation history, memory stats, user menu) implemented and working. It was simply defaulting to the collapsed icon-only rail (`useSidebarCollapsed`'s fallback was `true`) with the expand toggle being a small, easy-to-miss chevron at the bottom of the rail. **Before building new UI for a "missing" feature, check whether it already exists but is just hidden/defaulted off** — this one-line default flip (`localStorage` fallback `true` → `false`) delivered the exact outcome a multi-component rebuild would have, for near-zero risk.
lessons.md:5279:- **Current state**: LLM_PROVIDER=nim with meta/llama-3.1-8b-instruct for both generation and classification. NIM key is active and fast (~2.5s for simple CASUAL greeting, ~700ms for guardrail blocks).
lessons.md:5310:- **Context**: User reported Ollama Cloud quota exhaustion and wanted Sarvam / NIM / OpenRouter to work without Ollama. The active provider was already LLM_PROVIDER=nim in backend/.env, with NimService holding an internal Sarvam fallback on 429/connection errors.
lessons.md:5320:- **First cause found**: `navigator.serviceWorker.getRegistrations()` showed an **active** service worker (`public/sw.js`) controlling the tab, serving cached JS. Unregistering it + clearing its caches (`mukthiguru-core-v1`, `mukthiguru-meditation-v1`) made subsequent loads succeed reliably.
lessons.md:5331:  - **Critical (data-loss) findings fixed**: destructive admin actions with zero confirmation — `AdminsPage.tsx` "Revoke" access, `EvalsPage.tsx` delete golden question, `DailyTeachingPage.tsx` remove active teaching + per-history-item delete — all wrapped in the existing (previously unused anywhere) shadcn `AlertDialog` primitive. Also added missing `aria-label`s on `EvalsPage.tsx`'s icon-only Edit/Delete buttons, and bumped one `/60` low-contrast hint text to `/80`.
lessons.md:5400:- **Lesson**: Teacher-level partitioning requires payload indexes at collection creation time. Retroactive indexes work but trigger background rebuild.
lessons.md:5435:- **Lesson**: An optional dependency whose auto-tune resolves to `None` should short-circuit cleanly rather than throw a TypeError that looks like a real failure. The CrossEncoder is the active path regardless.
lessons.md:5576:- Cuts verified firing in logs: HyDE absent, compression bypassed (6441 < 20000), generate_answer 1.56s (was 10-25s with 800 num_predict)
lessons.md:5630:- **Problem**: The `requirements.txt` file declared `llama-index-core>=0.10.0` and `llama-index-vector-stores-qdrant>=0.1.0`. These dependencies were completely unused in the codebase but triggered `ResolutionImpossible` errors in the standard pip environment during Railway deployments.
lessons.md:5673:### Railway `railway run` cannot run interactive CLI tools for Neo4j restore
lessons.md:5711:4. **Recoverable**: On failure, prints exact manual steps to complete; supports `--resume-from <phase>`
lessons.md:5876:- **Problem**: When a page requiring authentication loads, `onAuthStateChange` often fires a `SIGNED_OUT` (or equivalent no-session) event immediately on registration before `supabase.auth.getSession()` resolves. If not handled, this causes a race condition redirect to `/auth` on page load, even if the user has a valid active session. Furthermore, if a session is present but fails validation (e.g. email domain not allowed or invalid test account), calling `signOut` triggers a subsequent `SIGNED_OUT` event which incorrectly fires a "Your session has ended" toast.
lessons.md:5887:- **Problem**: When managing a multi-language app, hardcoding the complete list of regional or national languages (like all 22+ scheduled Indian languages) in frontend components leads to drift and bugs where users can select unsupported languages with no translation files.
lessons.md:5888:- **Fix**: Replaced static arrays with dynamic filtering matching `Object.keys(i18n.options.resources)`. This queries the active translation config at runtime to automatically filter a master database of languages, restricting selections to only translated locales (English, Hindi, Telugu, Kannada, Tamil, Marathi) and dynamically scaling as new json translation resources are registered.
lessons.md:5927:- **Postgres partial unique index + `on_conflict`**: `ON CONFLICT (platform, token)` requires the index predicate to match exactly. Drop `WHERE active` from the index for upsert compatibility.
lessons.md:5956:- **Why**: `from app.dependencies import startup_complete` binds the local name to `False` at import time. When the background startup task later sets `startup_complete = True`, the local name never updates.
lessons.md:5957:- **Hard rule**: Always import the module itself: `import app.dependencies as deps` and read `deps.startup_complete`. Never bind a mutable flag to a local name at import time.
lessons.md:6024:- **Why**: Railway uses rolling deployments by default. While a new container is building, the previous successful container (`SUCCESS` status) stays active and serves 100% of live traffic.
lessons.md:6025:- **Hard rule**: Never panic when you see `INITIALIZING` alongside an active `SUCCESS` deployment. The site is up. Intermediate builds between the current success and the latest commit will be automatically `REMOVED` once the new build completes. No manual rollback needed unless the new build `CRASHED`.
lessons.md:6151:1. **Ensure URLs are completely clean**: Use `http://qdrant.railway.internal:6333` directly without any backslashes or escape sequences.
lessons.md:6434:6. **`railway up` (tarball upload)**: the most reliable method when the above all fail. Deploys the current *working tree* (not a git commit — ships uncommitted local changes, be deliberate about that). Confirmed: when a deployment was wedged in `INITIALIZING` for 36+ minutes and both `redeploy` and `restart` refused to touch it, `railway up` cleanly kicked off a fresh build+deploy that actually completed. This project's CLAUDE.md already documented `railway up` over `railway redeploy --from-source` for the backend specifically — this generalizes to "when a Railway service is stuck, `railway up` is the most reliable unstick lever available," not just a deploy-method preference.
lessons.md:6441:1. **`ps aux` cannot see processes started outside the current sandbox.** A `python scripts/ingest_lightrag_data.py` process had been running for 16.5 hours, actively checkpointing, completely invisible to `ps aux | grep` from inside a Claude Code session — concluding "not running" from a sandboxed process check nearly caused a duplicate process to be launched against the same production targets.
lessons.md:6456:- **Approach**: Created comprehensive implementation plan (`PRE_LAUNCH_CHECKLIST_PLAN.md`), then executed systematically by category, marking each check as complete in the source CSV.
lessons.md:6462:- **Rate Limiting**: Multi-layer rate limiting active — `TokenBucketMiddleware` (per-user, 20/min on `/api/chat`), slowapi limiter (200/min default, auth endpoints 5/min), admin limiter (5/min), breath-teaching (10/min).
lessons.md:6612:**The misleading part**: the Neo4j deploy logs show a *completely clean, successful boot* right up to the moment Railway kills it:
lessons.md:6654:1. **After changing an environment variable, verify a NEW deployment actually became active** — `railway deployment list --service <name>` and confirm the top entry is `SUCCESS`, not `SKIPPED`/`REMOVED`. Cross-check `activeDeployments[].createdAt` against the time you changed the variable: if the running container predates the change, the change is not in effect.
lessons.md:6702:**Problem**: mid-incident, `railway status` reported the backend's `latestDeployment` as `status: FAILED`, `id: 48861e1e...`, causing real alarm (was this a new, current failure?). Cross-checking with `railway deployment list --service <name> --json` showed the actual most recent deployment was a completely different ID, created *today*, with `48861e1e`'s own `createdAt` timestamp reading **2026-07-17** — over a week stale. `railway status`'s plain-text mode had shown similar staleness earlier in the session too (repeatedly reporting a deployment ID that hadn't changed across several genuinely-different redeploys, and once returning byte-identical log content across two calls minutes apart).
lessons.md:6716:**What was observed, once the limit was presumably raised/lifted**: `askmukthiguru-8119b0e8` (backend) and `celery-worker` both came back cleanly and automatically the moment a new deploy was triggered (`railway up` completed BUILDING → DEPLOYING → SUCCESS in under 2 minutes, no manual intervention needed beyond that one push). But **`gb-neo4j-railway-template` and `qdrant` did not self-recover** — Neo4j sat in `status: FAILED, deploymentStopped: true, replicas.running: 0` until manually redeployed (`railway redeploy --service gb-neo4j-railway-template --yes`, which worked and started a fresh `DEPLOYING` cycle), and **Qdrant had no deployment record at all** (`deploymentId: null, latestDeployment: null` — `railway redeploy` returned `"No deployment found for service"`, a state the CLI can't recover from). Both services' data volumes remained intact and `READY` (Neo4j 1.4GB/50GB, Qdrant 2.3GB/50GB) — only the running container/deployment was lost, not the underlying storage.
lessons.md:6718:**Rule**: "automatic redeploy on limit raise" is not guaranteed uniform across every service type in a project — template/managed-image services (a community Neo4j template, a `qdrant/qdrant:v1.17.1` image with no connected git repo) appear less likely to self-heal than git-connected app services that redeploy from a repo. After a Hard Limit shutdown is lifted, **check every service individually** (`railway service list --json`, look at `status`/`deploymentStopped`/`replicas.running` per service) rather than assuming the one service you actively redeployed proves the whole project recovered. A service showing `"No deployment found for service"` on `redeploy` needs a fresh deploy triggered a different way (Railway dashboard "Deploy" button, or recreating the service pointed at the same existing volume) — the CLI's `redeploy` command specifically requires an existing deployment record to redeploy *from*, and has no path to originate a brand-new one for an image-based service.
lessons.md:6736:2. **`celery-worker` runs 24/7** whether or not there's active ingestion work. `QUEUE_ENABLED=false` disables `job_queue`, but a separate `llm_queue_enabled` (default `True`, not overridden in production vars per the earlier remediation plan's own finding) likely keeps it consuming LLM-queue tasks continuously. This was flagged as needing verification *before* touching it as far back as this session's own earlier remediation plan ("Before touching celery-worker: pull Railway deploy logs/metrics over a real window to confirm actual task throughput. Only downsize/remove if genuinely idle — do not assume") — that verification was never completed. It remains the single highest-value next step: if genuinely idle most of the time, this is a real, avoidable 24/7 cost with a known Railway-recommended fix (scale-to-zero / sleep for non-critical services).
lessons.md:6747:The prior section's cost model was built from public pricing estimates. The user then supplied the actual Railway bill for this cycle (Jul 12 – Aug 12), which changes the priority ranking completely:
lessons.md:6828:1. **ColBERT reranking offline.** Model outputs colbert_vecs but pipeline ignores them. Dense-sparse hybrid (RRF) is the active path. Fix: either implement NumPy MaxSim scorer over ONNX colbert output, or make RAGatouille ColBERTv2 available.
lessons.md:6886:A single distress signal must NOT trigger course assignment (false positives). Triggers: ≥2 consecutive distress turns, ≥3-of-5 frequency, escalating severity, or the same SufferingSignal ≥2× within 24h. Idempotency: never assign when an active course exists in `user_course_progress`. Implemented in `backend/services/healing_course_service.py` with 37 tests; frontend card at `src/components/chat/HealingPathCard.tsx` (17 tests) mirrors the same trigger rules locally as fallback and prefers the backend `recommended_course`.
lessons.md:6917:`LICENSE-EXCEPTIONS.md` covers code *dependencies* only. Copyrighted books/PDFs fed to the RAG ingestion pipeline are a completely different rights category (copyright, not software licensing). Added `CONTENT-RIGHTS.md` as the parallel registry for ingested content. Both must be maintained. Rights basis for ingested third-party content must be documented *before* ingestion.
lessons.md:6933:The landing page may accept a final product-video URL through `VITE_LANDING_DEMO_VIDEO_URL`, but the app must still provide an accessible demo experience when that URL is unset. If video-generation capacity or credits are unavailable, record a production-ready storyboard, asset list, narrative, acceptance criteria, and deployment instructions rather than claiming that a video was generated. Generate optional BGM only after the video brief is confirmed and the actual video-production workflow reaches its audio-planning phase.
lessons.md:6941:Unknown or incomplete mappings must fail closed to the default corpus.
lessons.md:6972:field to all three, store it on the completed message, and test each boundary;
lessons.md:6997:A complete Vite production build and prerender exercise real module boundaries,
lessons.md:7008:typed answer-envelope quality contract are valuable but are not complete merely
lessons.md:7028:activated through a transaction that supersedes the prior active release for the
lessons.md:7069:surface may silently regress to an incomplete or misleading answer.
lessons.md:7135:protection, and content-free reliability/cost controls active, and disclose that
lessons.md:7136:inference providers still receive the active request when generation is needed.
lessons.md:7219:declaring a large remediation pass complete catches what scoped runs
lessons.md:7260:(`tenant_id`+`corpus_id`) that matched 0 of 89,053 production points** —
lessons.md:7321:same chain checking a completely different claim (`aal` on the JWT, not
lessons.md:7379:- **What**: The pre-launch build required Vite Supabase variables that were absent from CI, while local auth regression suites only use fake sessions and do not require real provider secrets.
lessons.md:7393:- **Fix applied**: Updated active copies to `+91 9999 666 555` and added India emergency services `112` to the registry and fallback.
lessons.md:7394:- **How to prevent**: Verify contacts against authoritative sources on a release cadence, scan active locales/UI copy for deprecated literals, and test fallback parity.
lessons.md:7397:- **What**: Proactive Serene Mind could set `isGated=true` and `isAwaitingSereneMind`, trapping moderate-distress users until practice completion.
lessons.md:7398:- **Fix applied**: Streaming and non-streaming proactive paths now offer grounding without gating, delay, or completion prerequisite; the backend prelude preserves continued chat and human support.
lessons.md:7407:- **What**: A parseable but incomplete YouTube URL could appear as a source and produce an invalid embed.
lessons.md:7438:## Latency-complete pass — 2026-08-22
lessons.md:7444:- **L-LAT-5 — Production proof and deployment boundary.** The active Railway endpoint returned the bounded comparison in approximately 7ms with 367 characters, `route_decision=bounded_comparison_short_circuit`, no citations, `grounding_state=abstained`, and `verification.method=limited_comparison_fallback`. The same active runtime returned an English FAQ under Hindi preference in approximately 4.3s and a native Hindi FAQ in approximately 4.0s, while the benchmark matrix also captured a 10.8s pipeline/13.8s wall-clock Hindi run, proving that multilingual latency remains variable. The attempted `940eb55` rollout was not active: Railway marked it FAILED during `Initialization › Snapshot code` after 15 minutes, with Build/Deploy never started; the dashboard continued to show `8655709` as ACTIVE. Health reported `ready=true`, `status=healthy`, Qdrant/Redis/Neo4j/LLM/embedding/fast-graph/LightRAG checks healthy, embedding dimension 1024, exact-cache keys 0, queue size 0, and chat backpressure clear. The translation-timeout code is now production-proven through the successful `806799d` rollout; its remaining limitation is tail variability rather than deployment uncertainty.
lessons.md:7454:The final smoke matrix exposed a remaining quality defect: the Telugu query `శాంతి అంటే ఏమిటి?` returned a 52-character Telugu refusal even though one source citation and a language-aware faithfulness score of 0.80 were reported. The generator now recognizes the observed Telugu refusal variants and performs one evidence-aware retry; the fix is committed in `899b17c`/`4833a95` but is not yet production-proven because Railway failed the rollout during code-snapshot initialization. The active runtime remains `8655709`, so the short-term benchmark result must be treated as a live regression signal rather than a closed defect.
lessons.md:7456:Railway's dashboard diagnosis is more specific than the CLI error: `940eb55` failed after 15 minutes at `Initialization › Snapshot code`, with Build, Deploy, Network, and Post-deploy never starting. The same control-plane failure affected subsequent 899b17c/4833a95 attempts, while `/api/health` and `/api/healthz` remained healthy on the prior active runtime. Do not use `railway down`, global cache eviction, or a rollback action without a known-good replacement; the active deployment is still serving users.
lessons.md:7462:- **L-LAT-8 — Hindi latency has a warm-path floor but a cold/outlier tail.** The final matrix captured a 21.2-second Hindi wall-clock run during the rollout window, while three repeated warm Hindi requests completed at approximately 7.0 seconds wall-clock and 3.3–3.8 seconds pipeline latency. Treat the warm result as the operating baseline and the 21-second observation as a tail requiring continued provider/request-stage telemetry; do not claim multilingual latency is solved.
lessons.md:7478:- **Production proof**: Commit `36e6f22` and observability commit `df5e2d1` are deployed successfully. The post-prewarm Hindi matrix request completed in 17.7s wall clock / 14.4s pipeline in one run, while Telugu completed in 7.6s / 4.6s and later warm runs remained materially faster. The remaining Hindi tail is still open because the full wall-clock budget is not explained by graph node timings.
lessons.md:7488:- **Fix applied**: No ONNX INT8 migration, RRF/DBSF change, Neo4j schema mutation, or broad graph parallelization was activated. The existing evidence-gate runbook remains authoritative until a dependency-complete held-out benchmark produces NDCG/faithfulness/citation/abstention/latency/rollback evidence.
lessons.md:7494:- **How to prevent**: Keep route smoke checks in the release matrix; separately schedule authenticated vault mutation and complete mobile/tablet/font/accessibility verification.
lessons.md:7499:### L-GRAPH-1. Remote uniqueness constraints are online, but ordinary index maintenance is incomplete
lessons.md:7511:After the benchmark load, `What is the meaning of stillness?` returned a 38-character `refusal_quality_gate` response with faithfulness `0.0` and no citations, even though its provenance context contained three direct-source items and the `Inner Stillness` entity. The comparative, Hindi, Telugu, and distress controls remained structurally correct. This is an open P0 evidence-filtering/refusal-path regression, not a latency-only issue; add a production-shaped regression before claiming generic simple-query quality is complete.
lessons.md:7515:The all-question-bank benchmark at concurrency three generated sustained 30–83 second cases and was stopped after more than twenty minutes before writing a report. A sequential core benchmark likewise produced no buffered progress within five minutes and was stopped. These partial runs are evidence of tail behavior, not complete scorecards. Future full runs must use staging or a dedicated benchmark replica, unbuffered per-case output, a global time budget, and no user-serving production load.
lessons.md:7521:The current Railway billing period reported $28.7854 usage: $27.0931 memory (94.1%), $1.3513 CPU (4.7%), $0.2645 volume (0.9%), and $0.0766 egress (0.3%). The previous completed period was $30.0068. The current workspace hard limit is $30; current usage is already 95.95% of that limit and Railway estimates $53.84 for the current period. Treat memory reduction as the first cost optimization target. At Railway’s official rate, one sustained GB is approximately $10/month and one sustained vCPU is approximately $20/month.
lessons.md:7546:The post-deploy image logs reported that BGE-M3 was fetched at startup, the reranker model was not cached, and the OKF compiled index and doctrine lexicon were absent. Do not suppress these warnings or create empty artifacts. Resolve image/reingestion packaging separately, without editing or uploading `scripts/ingestion/corpus/`.
lessons.md:7557:- **Browser verification:** The canonical Railway service domain includes the `-production` suffix. A stale domain returns Railway fallback 404 even when deployment is healthy. Always confirm the active service domain before declaring the API unavailable.
lessons.md:7567:- **Evidence**: Deployment `a88a9362-a256-49c1-b654-6a9a8e314b72`; focused Railway suite `27 passed, 2 skipped`. The preceding broader focused set completed `105 passed, 3 skipped`.
lessons.md:7577:- **Evidence**: Post-release SSH observations showed process RSS around `2.78–3.02 GiB` and cgroup current around `4.26–4.40 GB`; the ONNX directory was absent. The cleanup is disabled for `onnx_int8` and can be disabled with `PRUNE_UNUSED_HF_VARIANTS=false`.
lessons.md:7582:- **Fix applied**: Kept the serving-image test correction and ran the dependency-complete serving-focused suite instead of expanding the production archive with corpus or maintenance files.
lessons.md:7594:- **Fix applied**: Return `all_docs` at the final contract boundary. No retrieval ranking, graph selection, embedding backend, corpus, or cache policy was changed. The dependency-complete Railway suite then passed `83 passed, 2 skipped`.
lessons.md:7595:- **How to prevent**: When instrumenting a pipeline node, run the empty-result and final-contract tests before deployment. Inspect the complete function after adding timing code; do not infer the final variable name from an intermediate block.
lessons.md:7601:- **How to prevent**: Distinguish retrieval-quality repair from UX fallback repair. Measure both; preserve abstention metadata and zero citations whenever evidence is absent; never use a friendly fallback as proof that retrieval is grounded.
lessons.md:7608:- **Fix applied**: Validated the replacement against 2 Mode-A wrapped DEK rows in dry-run mode, applied exactly 2 compare-and-swap rewraps, promoted the replacement to `BRAIN_KEK` in backend and worker scopes, redeployed both, deleted `BRAIN_KEK_NEXT` from both scopes, explicitly redeployed both again, and verified the final deployments and health. A name-only audit confirmed `BRAIN_KEK` present and `BRAIN_KEK_NEXT` absent in both scopes.
lessons.md:7613:- **Fix applied**: Reloaded the authenticated-looking vault and observed the exact disposable reflection, deleted it with `Forget this`, waited for the settled empty state, and reloaded again after the key cutover. The item was absent and the vault view unlocked normally.
lessons.md:7630:- **What**: The active desktop Docker stack returned the acute self-harm control as blocked `DISTRESS`, but the benign Beautiful State control used `no_context_short_circuit` because the selected local Qdrant collection had zero points.
lessons.md:7634:### L-PROD-14. A broad test command is not evidence when the serving mount is incomplete
lessons.md:7635:- **What**: Running `pytest -q` in the active desktop backend stopped during collection with 12 errors because the stale backend-only mount lacked repository-root ingestion/ops modules and newer tests. A focused dependency-complete suite remained valid and passed 103 tests.
lessons.md:7671:A page can pass a static accessibility assertion while a cookie-consent button under the test cursor fails contrast in its hover state. Run axe against real overlays and pointer states, use contextual foreground tokens, and rerun the complete route set after fixing the first reported violation.
lessons.md:7674:When a full backend suite stalls, retain its partial output and classify the gate as incomplete. Focused dependency-complete clusters can establish local confidence, but they cannot replace a completed full suite with per-test duration data in CI or the production test image.
docs/PHASE_2_3_4_COMPLETE.md:13:Earlier turn already tightened spacing + starter-pill sizing and added `ChatEmptyState` with "Continue last conversation" and "Today's teaching" cards. No additional changes needed for v1; the next high-leverage UX work is reactive streaming token rendering, which is a backend change (already covered in `docs/IMPROVEMENTS_BACKEND.md`).
docs/forensic-2026-06-07.md:11:The recent ruthless benchmark results showed 0% doctrine accuracy, 0% citations, 0% faithfulness, and a p95 latency of 161 seconds. Our forensic analysis has identified three primary bugs that completely explain these failures:
docs/forensic-2026-06-07.md:59:* **Payload Verification:** As a result, the collection contains exactly **zero** sparse vector components. Qdrant marks the collection as `"grey"` (inactive sparse optimizer index status) because a sparse vector field is declared in the schema but no sparse indexes/vectors are actively populated. The dense vector HNSW index remains fully active.
.claude/plans/onnx-reranker-colbert-optimization.plan.md:51:| 6 | Rewrite this plan (done as part of approval) | — | complete |
.claude/plans/onnx-reranker-colbert-optimization.plan.md:65:- **No removal of `ragatouille` from `requirements.txt`** — keep deprecated, add TODO comment.
.claude/plans/onnx-reranker-colbert-optimization.plan.md:197:| MODIFY | `backend/requirements.txt` (TODO comment on ragatouille) | 4 |
docs/PRODUCTION_READINESS_CHECKLIST.md:4:All items must pass before considering the system production-ready.
docs/PRODUCTION_READINESS_CHECKLIST.md:27:- [x] RAG pipeline completes without errors for standard queries
docs/PRODUCTION_READINESS_CHECKLIST.md:63:- [x] API documentation is complete and accurate
docs/PRODUCTION_READINESS_CHECKLIST.md:66:- [x] Onboarding documentation for new developers is complete
docs/PRODUCTION_READINESS_CHECKLIST.md:95:**Production Readiness: GREEN LIGHT 🚦** when all checklist items pass and sign-off is complete.
docs/QUESTION_BANK_REFERENCE.md:28:4. **Fourth Sacred Secret — Practice Spiritual Right Action**: Taking action born of a connected, calm state rather than reactive ego and fear.
docs/QUESTION_BANK_REFERENCE.md:59:- **Biological Mechanism**: Transmission of subtle energy that downregulates the hyperactive parietal lobes (dissolving the rigid ego boundary) and activates the frontal lobes (cultivating empathy, calm, and connection).
.claude/GOAL.md:3:**Scope:** Complete the remaining code-review remediation units, merge all pending worktree changes to `main`, implement the backend integration spec for Custom Assistants & Notes, study and integrate best ideas from `hyper-extract`, and apply retrieval-quality improvements.
.claude/GOAL.md:43:  - `ContainerBuilder` is the active construction path in `get_container()`
.claude/GOAL.md:108:  - Assistant-aware tag filtering and hard `sky` exclusion active
.claude/GOAL.md:117:  - MMR diversity reranking and rerank floor already active
.claude/GOAL.md:156:- [x] Lower `rag_top_k_retrieval` default from 30 to 20 once score-delta cutoff is active and validated
docs/skill-enhancements.md:88:Read active prompt version from Supabase PromptStore per request (with in-memory cache fallback). Add experiment ID to cost tracking for prompt version → score → cost correlation.
docs/skill-enhancements.md:110:**Files**: New `backend/services/active_learning_service.py`, `backend/services/feedback_service.py`
docs/skill-enhancements.md:177:Replace fixed 8-message cap with token-budget allocator. Track `token_budget_remaining` in `GraphState`. Compress/drop turns when approaching limit.
docs/REFERENCE_DEVELOPMENT_GUIDE.md:23:**Net delta**: ~70% of the plan was redundant. The remaining 30% (in this guide) is the actual work.
docs/REFERENCE_DEVELOPMENT_GUIDE.md:143:**The one remaining lever inside retrieval**: `_llm_retrieval_expansions` at line 261 is a serial LLM call BEFORE the parallel section. It uses `_generate_fast` already (good), but a slow Ollama instance still adds ~2s. If you want to shave that, move the expansion call to run in parallel with `summary_task` (it doesn't depend on the chunk results, only the question).
docs/REFERENCE_DEVELOPMENT_GUIDE.md:155:The active cache:
docs/REFERENCE_DEVELOPMENT_GUIDE.md:164:### 2.5 [DONE — Phase 2 completed this session] SSE status frames in 7 priority nodes
docs/REFERENCE_DEVELOPMENT_GUIDE.md:166:**Update**: The user explicitly asked to complete this. Done.
.claude/plans/onnx-int8-embedding-validation.plan.md:88:1. **ColBERT reranking** — Model outputs colbert_vecs (ort_out[2]) but pipeline ignores them. Dense-sparse hybrid (RRF fusion) is active. Fix: NumPy MaxSim scorer over ONNX colbert output, or ship RAGatouille ColBERTv2.
docs/edge-cases-research.md:123:| **Exploitation Vector** | Fire 10-50 concurrent requests simultaneously. Each checks `current_tpm < limit` → all pass because counter hasn't updated yet (only updated after LLM call completes, which takes seconds). For streaming (SSE), the race window is 10-30 seconds. |
docs/edge-cases-research.md:208:| **Potential Impact** | **Medium-High**. Poor UX (incomplete spiritual guidance), increased cost (partial tokens billed, user retries), support tickets. |
docs/edge-cases-research.md:209:| **Mitigation** | Set timeouts at every layer: LLM client (connect=10s, read=60s+ per chunk), nginx `proxy_read_timeout=300s`. Client-side heartbeat every 15s. SSE comment lines (`: heartbeat`) as keepalive. `X-Accel-Buffering: no` to prevent nginx buffering. Graceful degradation: show partial response + "incomplete" badge. |
docs/edge-cases-research.md:221:| **Potential Impact** | **Medium**. Incomplete answers, wasted tokens (cost), user frustration. User retries = duplicate LLM calls for the same question. |
docs/edge-cases-research.md:228:| | 6. Graceful shutdown: drain active streams on deploy (30s max). |
docs/edge-cases-research.md:238:| | **Request coalescing**: In-flight registry (short-TTL Redis hash) — duplicate requests wait for first to complete, share result. |
docs/edge-cases-research.md:276:| **Exploitation Vector** | Many users send long context queries simultaneously. KV cache for each sequence consumes VRAM. Total exceeds GPU memory → OOM → process killed → all active streams fail. |
docs/edge-cases-research.md:286:| **Potential Impact** | **Medium-high**. Query failures, increased latency (queueing), partial response generation with incomplete context. |
docs/edge-cases-research.md:293:| **Description** | Request coalescer holds waiting requests until the first LLM call completes. If that call is slow (tool use, long generation), all coalesced requests are delayed by that same latency. One slow call blocks 50 requests. |
docs/edge-cases-research.md:371:| **Description** | The platform guides users through breathing exercises and meditation sequences. Incorrect parsing of step transitions, timing, or state machine transitions can lead to disjointed or incorrect guidance (e.g., instructing exhale before inhale is complete). |
docs/edge-cases-research.md:430:| **Exploitation Vector** | User completes a meditation on phone (offline). Same time, completes different meditation on tablet (also offline). Both devices sync when online. Last-write-wins overwrites one session's data. Or device A syncs, device B's local state is stale, B overwrites A's updates. |
docs/edge-cases-research.md:441:| **Mitigation** | Use database transactions for feedback + session reference. Or soft-delete sessions (mark as complete, don't delete). Monitor feedback submission failures. Async feedback processing with confirmation to user. Idempotent feedback submission (user can retry safely). |
docs/edge-cases-research.md:492:| **Description** | Next.js hot-reload refreshes modules while SSE connections are active. The WebSocket connection for HMR collides with SSE streams. State reset causes active meditation sessions to lose context. |
docs/edge-cases-research.md:493:| **Exploitation Vector** | Developer edits a meditation component. Hot-reload triggers re-render of all active pages. Client-side state (breathing timer, current meditation step) is lost. User sees a flicker or a blank state. SSE streams from server may be interrupted by server restart. |
docs/edge-cases-research.md:494:| **Potential Impact** | **Medium**. Development friction, hard-to-reproduce bugs. Lost state during active user sessions in dev. |
docs/edge-cases-research.md:495:| **Mitigation** | Preserve client state across HMR (React state, Redux, Zustand persist). Server-side graceful restart: drain active SSE connections before restart. Use production-grade live reload (Turbopack) with proper state persistence. Test state preservation scripts. |
docs/runbooks/STREAM_PROTOCOL.md:120:| `meditation_step` | number | yes | 0 if no active session. Echo this in the next request. |
docs/runbooks/STREAM_PROTOCOL.md:129:| `proactive_serene_mind` | object\|null | optional | If non-null, suggest a meditation. |
docs/runbooks/STREAM_PROTOCOL.md:154:  response is incomplete.
docs/runbooks/STREAM_PROTOCOL.md:244:This is a graceful failure mode. Treat as a complete (but apologetic) response.
docs/INTEGRATION_GUIDE.md:32:| `HARDCODING_AUDIT.md` | Live audit of remaining hardcoded values |
docs/INTEGRATION_GUIDE.md:129:- New `is_meditation_imperative()`, `get_meditation_complete_message()`
docs/INTEGRATION_GUIDE.md:202:| Sample query "Can I practice Soul Sync on Mars?" | Does NOT return *"The meditation is complete..."* |
docs/INTEGRATION_GUIDE.md:309:them: when None, do not emit the legacy "meditation is complete" sentinel.
.claude/plans/uncommitted-batch-2026-08-01.plan.md:24:| `metrics.py` `last_active_at` | `backend/app/api/metrics.py` | Logic correct | `backend/tests/test_metrics_endpoint.py` **untouched** — only exercises the `None`/anonymous path. Add a test: conversation row with `updated_at` set → assert it surfaces. |
.claude/plans/uncommitted-batch-2026-08-01.plan.md:28:- [ ] Add `test_metrics_endpoint.py` case for non-anonymous `last_active_at`
.claude/plans/uncommitted-batch-2026-08-01.plan.md:34:- [ ] **Apply migration** `supabase/migrations/20260801000000_user_course_progress_one_active.sql`
.claude/plans/uncommitted-batch-2026-08-01.plan.md:35:  (partial unique index, one active course per user) to whichever Supabase project is live —
.claude/plans/uncommitted-batch-2026-08-01.plan.md:58:- [ ] `handoff.md` — status-doc accuracy fixes only ("implementation-complete ≠ release-ready"
.claude/plans/uncommitted-batch-2026-08-01.plan.md:76:`CLAUDE.md`'s "Resolved items" section calls this pinning done and production-ready — it was,
.claude/plans/uncommitted-batch-2026-08-01.plan.md:83:`20260801000000_user_course_progress_one_active.sql` is well-reasoned and correctly matches the
docs/COMPLETE_SYSTEM_REFERENCE.md:73:*   **Stream Checkpoints:** To safeguard conversation streaming against accidental screen refreshes, `ChatInterface.tsx` writes active message chunks to `sessionStorage` at `askmukthiguru_stream_checkpoint` every 500ms. If a reload occurs and the checkpoint timestamp is less than 60 seconds old, the app restores the partial stream.
docs/COMPLETE_SYSTEM_REFERENCE.md:189:*   **Parent-Child Context Swap:** A specialized post-retrieval module swaps child chunks (~400-char leaf segments) for their parent paragraphs (~1500-char context window) using `parent_id` foreign keys to ensure the generation stage receives complete context.
docs/COMPLETE_SYSTEM_REFERENCE.md:225:*   **LLM Integration:** Both local Ollama (`ollama_service.py`) and remote Sarvam (`sarvam_service.py`) services automatically intercept LLM raw calls, calculate/parse input and output token counts, and update the active thread's `TokenAccumulator`.
docs/COMPLETE_SYSTEM_REFERENCE.md:226:*   **Database Sync:** An asynchronous `@record_token_usage` decorator and a streaming generator `finally:` block ensure that as soon as the API response completes (via either HTTP stream or standard JSON response), request token counts and calculated USD costs are flushed to the local `token_usage` SQLite database.
docs/COMPLETE_SYSTEM_REFERENCE.md:309:    total_meditations_completed int DEFAULT 0,
docs/COMPLETE_SYSTEM_REFERENCE.md:425:*   **Root Cause:** A total budget of 75 seconds was set for the entire LangGraph pipeline (`settings.llm_timeout + 15`). However, the RAG graph executes 11 to 12 sequential LLM calls (routing, decomposition, HyDE, grading, verification, contradiction check, etc.). Since each call took 10-30s, the entire pipeline mathematically required at least 110-330s. The 75s timeout made complex queries architecturally impossible to complete.
docs/COMPLETE_SYSTEM_REFERENCE.md:426:*   **The Fix:** Decoupled `LLM_TIMEOUT` (per-call timeout, set to 60s) from `PIPELINE_TIMEOUT` (total graph budget, set to 120s or 180s). Configured the streaming pipeline wrapper to use `settings.pipeline_timeout` and introduced `TimeoutBudget` tracing using `contextvars` to track remaining time dynamically across RAG nodes.
docs/COMPLETE_SYSTEM_REFERENCE.md:434:    3. Updated `MessageList.tsx` and `ChatMessage.tsx` to keep the pulsing thinking dots visible as long as `isStreaming` is active and the content string is empty.
docs/COMPLETE_SYSTEM_REFERENCE.md:439:*   **The Fix:** Cleaned up the search pipeline in `qdrant_service.py` to check for empty sparse vectors and omit them entirely. For phonetic-only prefetching, the vector parameter is omitted completely, performing a high-performance payload-only filter query on Qdrant.
docs/runbooks/MIGRATION_ROLLBACK.md:117:| `20260801000000_user_course_progress_one_active.sql` | One active course constraint | Drop trigger/constraint; restore previous allow-many behavior. |
docs/DEPLOYMENT.md:6:> **Sarvam 30B** is a Mixture-of-Experts (MoE) model — 32B total params but only **2.4B active**.
docs/DEPLOYMENT.md:144:> Sarvam 30B (MoE, only 2.4B active) runs great on a single 24 GB GPU.
docs/DEPLOYMENT.md:330:> These are estimates. Sarvam 30B is a MoE model with only 2.4B active parameters,
docs/KUBECTL_RUNBOOK.md:34:# why is a pod not ready?
docs/KUBECTL_RUNBOOK.md:94:   single remaining key.
docs/LATENCY_EVIDENCE_GATES.md:11:| Gate | Minimum evidence | Production action if absent or failed |
docs/LATENCY_EVIDENCE_GATES.md:24:`EMBEDDING_BACKEND=onnx_int8` remains **off** for the serving collection while existing Qdrant vectors are fp32-produced. Approval requires a shadow or blue-green re-embedding of the complete collection, vector-dimension and cosine-compatibility checks, per-class NDCG/faithfulness/citation results, and warm/cold memory and latency measurements. A strong aggregate overlap score is insufficient. If any corpus source is unavailable, any protected category falls below its floor, or index/query vectors are not generated by the same compatible backend, fail closed and retain fp32.
docs/LATENCY_EVIDENCE_GATES.md:36:Only independent, read-only graph branches may be parallelized. Their state writes must use explicit reducers or isolated result fields, their concurrency must be capped to host and Neo4j capacity, and a timeout on any branch must fail open to the vector path. The canary must compare graph-enabled and Qdrant-only runs for answer faithfulness, provenance completeness, graph timeout rate, p95/p99 latency, and resource usage. Do not parallelize branches that mutate shared state, rely on ordering, or may expose private memory through values streaming.
docs/LATENCY_EVIDENCE_GATES.md:40:The current Railway evidence is memory-dominated: the active billing period reported $28.79 usage, with $27.09 (94.1%) from memory, $1.35 (4.7%) from CPU, $0.26 (0.9%) from volume, and $0.08 (0.3%) from egress. The workspace hard limit is $30 and the current estimate is $53.84. These values make memory attribution the first optimization target, but they do not authorize a blind heap, page-cache, model, or replica reduction.
docs/LATENCY_EVIDENCE_GATES.md:50:The production runtime `36e6f22` contains changes that do not alter retrieval evidence: a narrow English bounded-comparison short-circuit after input and distress guardrails, a five-second translation deadline with fail-open multilingual behavior, a privacy-bounded process-local translation cache, translation-stage timing logs, and an on-device intent-model startup prewarm. English messages under an Indic UI preference skip redundant input translation; native Indic and code-switched text retain translation and guardrail coverage. The bounded-comparison shortcut and multilingual evidence-retry are active. Post-prewarm smoke showed comparative about 2.6 seconds wall-clock, relational about 9.0 seconds, Telugu about 7.6 seconds, and a Hindi sample about 17.7 seconds wall-clock; the Hindi tail remains open because the full budget is not yet explained by node timings. All ONNX, RRF/DBSF, Neo4j schema, and broad graph-parallelization defaults remain unchanged in production.
docs/LATENCY_EVIDENCE_GATES.md:56:The sandbox lacks `pytest`, `pydantic-settings`, and `langgraph`, so dependency-complete tests and runtime graph construction were not executed locally. Static compilation, regex safety, diff, corpus immutability, and graph-source topology checks ran; missing dependencies are recorded as indeterminate and never treated as a pass.
.claude/agents/hyperresearch-loci-analyst.md:108:   - **`dialectical`** — a place where sources in the width corpus actively
docs/runbooks/SECRET_ROTATION.md:31:Tick each box as you complete it. Do not skip ordering — secrets rotation
docs/runbooks/SECRET_ROTATION.md:41:      - `cookies.txt` (~300 KB) — active YouTube OAuth session cookie
docs/runbooks/SECRET_ROTATION.md:92:- [ ] **C4.** Delete `cookies.txt` (active session — no backup needed, it
docs/runbooks/SECRET_ROTATION.md:191:      - **Redis `REDIS_PASSWORD`** — If your Redis provider supports dual-password rotation (e.g. ElastiCache user rotation or Redis ACL with two active passwords), add the new password as a secondary auth credential first; otherwise plan a short maintenance window.
docs/runbooks/SECRET_ROTATION.md:197:      - **Neo4j `NEO4J_PASSWORD`** — `ALTER CURRENT USER SET PASSWORD` **does not** support overlapping active passwords (it replaces the password; the old one stops working immediately). Choose one of:
docs/runbooks/SECRET_ROTATION.md:213:        ⚠️ Invalidates all active user login sessions (forces re-login). Coordinate with a maintenance window.
docs/architecture/OPEN_SOURCE_ADOPTION_MATRIX.md:17:| [Neo4j GraphRAG Python](https://github.com/neo4j/neo4j-graphrag-python) | First-party Neo4j package; Apache/Python licensing files; active package with KG builder and retrievers | Directly compatible with existing Neo4j and Qdrant | **Build a measured retrieval spike** | No direct writes to production graph; enforce existing ontology and release IDs |
docs/runbooks/ALERTING.md:47:Local staging: `docker compose up -d prometheus alertmanager` (add to compose if absent), then simulate a 10s latency by pausing the backend container (`docker pause backend`) — the `ChatLatencySLOPage` burn rule will fire within ~2-3 min (`for: 0m` + group_wait 30s). Verify the alert appears in Prometheus UI (`http://localhost:9090`), then `docker unpause backend`.
docs/architecture/source-verified-report.md:11:| Sarvam must be self-hosted; ₹55K–₹2.5L/month GPU costs | **Wrong**. Sarvam's 30B and 105B Chat APIs are **free per token**. You only pay for rate-limit upgrades if needed. This completely invalidates my GPU-first recommendation. | Critical |
docs/architecture/source-verified-report.md:28:- **Architecture**: MoE with 128 experts. 30B has 2.4B active params, 105B has 10.3B active params [^72^][^70^]
docs/architecture/source-verified-report.md:64:- **Architecture**: MoE, 30B total with 3B active per forward pass
docs/architecture/source-verified-report.md:107:Given that **Sarvam API is free per token**, the economics completely change. Here is a revised, verified deployment architecture:
docs/architecture/source-verified-report.md:212:| GitHub repo recommendations | **9/10** | All repos verified to exist and be actively maintained |
.claude/skills/hyperresearch-1-5-chapter-partition/SKILL.md:79:2. Record progress: `$HPR run step <vault_tag> <N> --status done --chapter <id> -j` after each chapter-step completes.
.claude/skills/hyperresearch-1-5-chapter-partition/SKILL.md:82:**Budget check at every chapter boundary:** `$HPR run status <vault_tag> -j`. If `status` is `blocked` with `blocked_on: "budget"`, STOP spawning and surface to the user. Never silently skip profile-mandated steps — shrink the NEXT chapter's fan-out (fewer wave-2 fetchers, lower depth budgets) when `budget_remaining_usd` is under ~30% instead.
docs/engineering-notes/MASTER_REMEDIATION_PLAN.md:80:## Part D — Data-quality system (DONE; here for completeness)
docs/engineering-notes/MASTER_REMEDIATION_PLAN.md:124:4. **C** (Supabase table + admin UI) — makes the data-quality system operator-complete.
docs/runbooks/REDIS_NAMESPACE_BUDGET.md:26:Record the JSON output in the dated scalability audit. Treat `truncated=true` as an incomplete measurement, not as a zero or a pass. Do not copy Redis URLs, passwords, or key values into reports.
docs/runbooks/REDIS_NAMESPACE_BUDGET.md:40:A release is not complete until the following are true:
docs/runbooks/REDIS_NAMESPACE_BUDGET.md:46:| Tests | Focused Redis budget tests pass in the dependency-complete Railway image/CI | Any regression or missing locked dependency |
docs/runbooks/REDIS_NAMESPACE_BUDGET.md:49:| Quality | ONNX shadow validator passes on complete corpus and held-out faithfulness/NDCG | Any missing corpus source or category regression |
docs/architecture/source-report.md:11:The system architecture follows a **Service Container pattern** for dependency injection, making the codebase testable and maintainable. The RAG pipeline is orchestrated using **LangGraph**, which provides a state-machine-like execution model where each processing step is represented as a node in a directed graph. This design enables conditional branching (such as routing distressed users to meditation flows), iterative refinement (CRAG loops for query rewriting), and clear error handling paths. The ingestion pipeline implements a **4-tier fallback strategy** for YouTube transcript extraction, progressing from manual captions through auto-generated captions, faster-whisper transcription, and finally yt-dlp subtitle download. This robust extraction chain ensures maximum content coverage from the source videos. The system also incorporates **RAPTOR (Recursive Abstractive Processing for Tree-Organized Retrieval)** hierarchical indexing, which clusters semantically similar chunks and generates LLM summaries, enabling thematic overview retrieval alongside granular detail search.
docs/architecture/source-report.md:38:The **third critical gap is the complete absence of multilingual prompt support**. While the system can ingest content in 10 Indian languages and the embedding model (bge-m3) supports 100+ languages, all LLM prompts including the core system prompt, intent classification, relevance grading, faithfulness checking, and verification are written exclusively in English. This means that even when users ask questions in Hindi or Telugu, the model processes them through an English-language reasoning framework, significantly degrading response quality and cultural authenticity. Additionally, the system **lacks authentication, rate limiting, and audit logging**, which are essential for any production API serving sensitive spiritual guidance. Without these protections, the system is vulnerable to abuse, cannot track usage patterns, and lacks accountability for the guidance provided.
docs/architecture/source-report.md:48:The `/api/chat` endpoint orchestrates the full pipeline flow: NeMo Input Rail → Depression Detection → Response Cache Check → LangGraph RAG Pipeline → NeMo Output Rail. This linear flow with conditional branching (distress bypasses RAG, cache hits skip processing) represents a well-thought-out execution path. The endpoint handles both standard JSON responses and includes a streaming variant (`/api/chat/stream`) using Server-Sent Events (SSE), which improves perceived latency by sending tokens as they are generated rather than waiting for the complete response. The `/api/ingest` endpoint accepts content URLs and processes them in background tasks using FastAPI's `BackgroundTasks`, allowing the API to respond immediately while ingestion continues asynchronously. This non-blocking design is appropriate for long-running ingestion operations.
docs/architecture/source-report.md:999:The code includes graceful fallback logic: if the fast classification model fails, the system automatically falls back to the main generation model. This fail-safe ensures that temporary model unavailability does not break the entire pipeline. The service also supports streaming generation through `generate_stream`, which yields tokens as they are generated rather than waiting for the complete response. However, the implementation has several production concerns. **Ollama is fundamentally a development tool** designed for local experimentation, not production serving. It lacks request batching, concurrent request queuing, PagedAttention for GPU memory optimization, and horizontal scaling capabilities. Under concurrent load, Ollama's performance degrades significantly as it processes requests sequentially rather than leveraging GPU parallelism. The service also has **no connection pooling**, creating a new HTTP connection for each LLM call. There are **no retry mechanisms** for transient failures, **no request timeouts** (a hanging LLM call can block the entire pipeline indefinitely), and **no circuit breaker** to prevent cascade failures when the LLM service is overloaded.
docs/architecture/source-report.md:1911:        if is_meditation_complete(meditation_step):
docs/architecture/source-report.md:1993:            resolved = asyncio.get_event_loop().run_until_complete(
docs/architecture/source-report.md:2025:    def record_complete(self):
docs/architecture/source-report.md:2037:        metrics.record_complete()
docs/architecture/source-report.md:2039:            f"[{request_id_var.get()}] Node '{node_name}' completed in {metrics.duration_ms:.1f}ms"
docs/architecture/source-report.md:2053:        metrics.record_complete()
docs/architecture/source-report.md:2473:        logger.info("Full sync complete")
docs/architecture/source-report.md:2504:| `services/ollama_service.py` | Ollama not production-ready | CRITICAL | Replace with vLLM for serving |
.claude/agents/hyperresearch-synthesizer.md:380:  chars) or subsection addresses future implications. If absent, write
docs/engineering-notes/handoff.md:35:## 3. Files actively being edited (uncommitted)
docs/engineering-notes/handoff.md:63:6. Separately, lower priority: the already-ingested contaminated Qdrant chunks (old RAPTOR headers with corrupted topic labels baked into embedded chunk text) still need either re-ingestion or a one-off cleanup script. Not scoped this session — the shipped fixes stop the leak from reaching users and stop new corruption, but don't retroactively clean existing vectors.
docs/engineering-notes/handoff.md:68:- **The in-process job queue doesn't match any of Railway's recommended background-work patterns.** Per `docs.railway.com/guides/cron-workers-queues`, Railway expects background work as either a cron service, a dedicated always-on worker service, or a queue with a separate consumer service. This app's chat job queue runs its workers as an asyncio background task *inside the same process* as the HTTP server (via `start_railway.py`'s lazy lifespan pattern), which means its health is completely invisible to Railway's platform-level monitoring. Notably, there's already a separate `celery-worker` service in this same project for ingestion tasks — the long-term right answer is probably moving chat job processing to a similarly separate service, not patching the in-process pattern further.
docs/engineering-notes/handoff.md:76:- **The "2 active deployments" / stuck `INITIALIZING` state flagged early this session was never explicitly confirmed cleared.** Run `railway status` at the start of the next session before assuming it's resolved.
docs/engineering-notes/handoff.md:77:- **The UI/UX audit was nowhere near complete.** Only the landing page (mobile + desktop) got a real pass, plus a source-level read of `ProfilePage.tsx`. Practices page, the meditation/Serene Mind modal UI, the chat page at scale, tablet breakpoints, and dark/light theme parity were never touched. If "ruthless across all device types" is still the bar, there's a lot left.
.claude/agents/hyperresearch-polish-auditor.md:108:- `summary:` ending in `...` (truncated) — rewrite to a complete
.claude/agents/hyperresearch-polish-auditor.md:281:If the evidence is absent or weak, the hedge is honesty and should
.claude/agents/hyperresearch-depth-critic.md:111:Same as dialectic-critic. Flag any interim note the draft completely
.claude/skills/hyperresearch-5-depth-investigation/SKILL.md:81:3. **Wait for all K to complete.** Investigators can fail independently. Proceed with whichever succeeded. If >50% failed, stop and reassess loci quality with the user.
.claude/tasks/world-class-uplift.md:75:- [x] Wave 1 complete (9/9 OK)
.claude/tasks/world-class-uplift.md:77:- [x] Wave 2 complete (3/3 OK)
.claude/tasks/world-class-uplift.md:79:- [x] Wave 3 complete (1/1 OK)
docs/engineering-notes/subsystem-inventory.md:5:Reference doc for backend subsystems that ship in code but are absent or only
docs/engineering-notes/subsystem-inventory.md:88:below are complete; auth = Supabase JWT):
docs/engineering-notes/subsystem-inventory.md:92:| POST | `/api/healing-course/assign` | Evaluate `history` for a distress trigger; assign matching course. Idempotent — an active-course user gets no second one. |
docs/engineering-notes/subsystem-inventory.md:166:LightRAG's `entity_id`; returns empty gracefully when the driver is absent.
docs/engineering-notes/subsystem-inventory.md:209:complete; auth = Supabase JWT):
docs/engineering-notes/subsystem-inventory.md:221:uses a Supabase RPC. Service is optional — routes 503 when absent.
docs/engineering-notes/subsystem-inventory.md:287:| POST | `/api/memory/knowledge-graph/export` | Export the KG as a standalone interactive HTML file. |
docs/engineering-notes/subsystem-inventory.md:341:- TTS/translate return 500 when the API key is a dummy/absent; TTS maps bare
docs/engineering-notes/OKF_AND_ANTI_HALLUCINATION.md:17:(89,053 Qdrant chunks, 7,564 Neo4j nodes). This is the honest gap — OKF is a curated, reviewed layer on
docs/engineering-notes/OKF_AND_ANTI_HALLUCINATION.md:78:Qdrant `spiritual_wisdom` = 89,053 vectors, Neo4j = 7,564 nodes (matches the ~7,512 documented after the
docs/engineering-notes/OKF_AND_ANTI_HALLUCINATION.md:89:3. **Container `/app/data/lightrag/` is missing every KV-store file except `kv_store_llm_response_cache.json`** — all of `doc_status / full_entities / full_relations / entity_chunks / relation_chunks / full_docs / text_chunks` are absent.
docs/engineering-notes/OKF_AND_ANTI_HALLUCINATION.md:94:**Fix:** re-run ingestion from inside the container so `ainsert()` rebuilds all three stores consistently. The canonical replay script is `backend/scripts/migrate_data.py`; it scrolls leaf chunks from the intact `spiritual_wisdom` Qdrant collection (89,053 points), reconstructs full text per `source_url`, and re-ingests via `pipeline.ingest_raw_text(max_accuracy=True)` which fires `ainsert` at `backend/ingest/pipeline.py:699`.
docs/engineering-notes/OKF_AND_ANTI_HALLUCINATION.md:100:completeness regression.
.claude/agents/hyperresearch-fetcher.md:139:   - Is this actually relevant to the research topic? If completely off-topic, deprecate it:
docs/engineering-notes/ingestion-handoff-2026-08-16.md:18:  complete first, or you'll reproduce this.
docs/engineering-notes/ingestion-handoff-2026-08-16.md:307:Signal that ingestion is actively progressing (not stuck): new
docs/engineering-notes/ingestion-handoff-2026-08-16.md:314:To check via the admin dashboard instead (needs a real login + completed AAL2
docs/engineering-notes/ingestion-handoff-2026-08-16.md:317:completed jobs; TOTAL RUNS / OK / PARTIAL / FAILED counters update as chord
docs/engineering-notes/ingestion-handoff-2026-08-16.md:318:callbacks (`playlist_complete` task) land. "Active Jobs" panel is
docs/engineering-notes/corpus-remediation-and-migration-plan.md:237:| 5 | Pronoun rule inverted at all 4 prompt sites + `GURU_SYSTEM_PROMPT` | ✅ | the gurus' retrieved "I" is preserved and attributed instead of flattened to "they"; manufacturing a first-person sentence absent from context remains forbidden. This is the mechanism behind the *Miracle of Mind* effect. |
docs/engineering-notes/corpus-remediation-and-migration-plan.md:452:clean corpus's remaining edge cases. Fix coverage in the same pass: a graph over
docs/engineering-notes/corpus-remediation-and-migration-plan.md:746:   generate and review its SBOM, and scan the complete resolved runtime
docs/engineering-notes/retrieval_patterns.md:24:## RAPTOR (Recursive Abstractive Processing)
docs/architecture/multilingual-chat-guidance-contract.md:13:| Priority language controls | English, Hinglish, Hindi, Telugu, Tamil, and Kannada are directly selectable | The complete selector remains available for the broader supported language set |
docs/architecture/multilingual-chat-guidance-contract.md:31:The redesign requires focused component and translation regressions, the complete
.claude/agents/hyperresearch-width-critic.md:76:   but absent from the draft? Not every corpus cluster deserves a draft
.claude/agents/hyperresearch-width-critic.md:136:    forward-looking implications or strategic recommendations absent
docs/transcript_end_to_end_review_report.md:11:The corpus contains **745 video directories**, of which **657 are non-empty packages** and **88 are incomplete or empty directories**. The completed packages preserve the intended separation between clean transcript prose, lossless canonical segments, quality metadata, reversible correction ledgers, raw sources, and artifact manifests. Across the non-empty packages, the audit found **zero listed-artifact SHA-256 mismatches and zero raw-source SHA-256 mismatches**, which is strong evidence that the material currently on disk has not diverged from the hashes recorded in its manifests. The audit also found **648 packages with no structural or integrity issue**, although 177 packages carry non-fatal warnings.
docs/transcript_end_to_end_review_report.md:17:The review recovered the prior workflow from the referenced conversation and followed the repository’s corpus contract. Each video directory was evaluated independently, allowing the package checks to run in parallel without changing any corpus file. The audit checked package completeness, raw-source presence, transcript structure, canonical-segment validity, timestamp monotonicity and overlap, correction-ledger schema and reversibility metadata, manifest-listed artifact hashes, raw-source hashes, and agreement between selected manifest and quality-report metadata.
docs/transcript_end_to_end_review_report.md:23:| Package completeness | Required canonical files and non-empty `raw_sources/` | Detects missing or unmaterialized packages |
docs/transcript_end_to_end_review_report.md:36:| Incomplete or empty packages | **88** |
docs/transcript_end_to_end_review_report.md:58:### P0 — 88 incomplete package directories
docs/transcript_end_to_end_review_report.md:62:The appropriate remediation is to classify each directory explicitly as **completed**, **dead-lettered**, **retryable**, or **intentionally reserved**. A retryable directory should be regenerated from its source URL or ingestion manifest. A dead-lettered directory should receive a small status record explaining the failure and should be excluded from downstream projections. The remediation should preserve directory identity and should not silently fabricate canonical files.
docs/transcript_end_to_end_review_report.md:72:The repository’s own quality state machine correctly avoids trusting a single-source transcript without explicit review. The result is operationally conservative but means that **the corpus has no completed promotion gate**: 652 packages are `needs_review`, five are `sound_only`, and none are `trusted` or `trusted_after_review`. This is not a cryptographic failure and should not be “fixed” by changing state strings. Instead, the project needs a review protocol that promotes packages only after evidence is attached: waveform spot checks, terminology checks, and explicit reviewer metadata.
docs/transcript_end_to_end_review_report.md:104:The external research supports a layered review design rather than a single score. Apple’s HEWER work demonstrates that major semantic and proper-noun errors deserve more attention than harmless filler differences [2]. The ASR evaluation study likewise cautions that results vary across datasets, speakers, accents, and demographic groups, and that normalization is needed to reduce non-semantic differences [4]. GigaSpeech 2 shows a contemporary corpus workflow combining initial ASR, forced alignment, iterative label refinement, and multidimensional filtering [3]. These sources support the following conclusion: the local corpus has a sound **artifact architecture**, but its semantic and acoustic promotion gates are not yet complete.
docs/transcript_end_to_end_review_report.md:117:| P0 | Classify and resolve the 88 empty directories | Every directory is completed, retryable with owner/status, dead-lettered, or explicitly reserved; no empty directory reaches downstream ingestion |
.claude/agents/hyperresearch-browser-fetcher.md:6:  pages, bot-walled sites, interactive/infinite-scroll pages, viewer-rendered
.claude/agents/hyperresearch-browser-fetcher.md:110:   PYTHONIOENCODING=utf-8 /Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/.hyperresearch-venv/bin/hyperresearch escalation add "<paper url>" --reason interactive_needed --tag <vault_tag> --suggested-by <scholar-note-id> --detail "high-citation Scholar hit" -j
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:15:The project should not yet open unrestricted public traffic. The complete backend suite did not finish in the connected desktop environment, the dependency vulnerability scan stalled on network access and the prior audit identified unresolved backend package advisories, the local HTTP/Docker runtime was unresponsive during the final runtime probe, and production-like provider, RLS, backup/restore, migration rollback, authenticated deletion, and cross-browser identity evidence remain unverified. Most importantly, the repository’s curated OKF and doctrine-lexicon artifacts remain intentionally absent; the new health contract fails readiness rather than manufacturing placeholders. [3] [4]
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:36:| Frontend lint | **PASS** | 0 errors, 31 non-blocking warnings. Meaningful hook warnings addressed; remaining warnings are largely Fast Refresh export advisories. |
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:38:| Production frontend build | **PASS** | Build and prerender completed for 28 routes. |
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:40:| Mobile-targeted build | **PASS** | `npm run build:mobile` completed successfully. |
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:52:| Dependency vulnerability audit | **INCOMPLETE/BLOCKED** | `pip-audit` did not complete within the available connected-desktop run because the vulnerability service/network call stalled. Prior audit evidence reported 36 advisories across 16 backend packages; this remains a release gate until rerun in CI with network access. |
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:65:The runtime-artifact change is deliberately fail-closed. It does not create `compiled.json`, `doctrine_lexicon.json`, embeddings, graph data, or other corpus substitutes. When curated artifacts are absent, deep health reports the missing required names and readiness becomes false. This prevents a healthy-looking process from being mistaken for an approved corpus-backed teaching service. The actual artifacts still require an audited build, manifest/checksum validation, rights review, and held-out retrieval/faithfulness evidence before release.
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:69:The focused security, privacy, upload, memory, vault, authorization, and safety clusters passed in the dependency-complete virtualenv. This raises confidence in the tested contracts but does not replace credentialed two-user RLS probes, authenticated deletion across Postgres/Qdrant/Redis/queue stores, backup/restore verification, or production-provider isolation tests. Those are environment-backed controls and remain open until executed against a disposable staging project.
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:73:The first post-remediation accessibility rerun found a hover-state contrast defect in the cookie banner that was not visible from the initial `/auth` failure. The darker hover token was applied and the complete Chromium axe suite then passed. This is evidence for the audited Chromium routes only; it is not evidence for Firefox, WebKit, mobile browsers, tablet layout stress, or authenticated production redirects.
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:79:| P0 | Backend dependency advisories remain unresolved or unverified. | Upgrade in a dedicated compatibility branch; run `pip-audit`, the complete backend suite, Bandit, RAG evaluation, and container tests against the resulting lockfile. |
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:80:| P0 | Curated serving artifacts are absent in the inspected repository/runtime. | Produce approved `compiled.json` and doctrine lexicon artifacts through the audited corpus pipeline, attach checksums/version/rights metadata, bake them into the image, and verify deep health. Never add placeholders. |
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:81:| P0 | Live backend and provider round trips were not completed in the final environment. | Run bounded anonymous-session, harmless query, comparative query, Indic query, acute-distress query, streaming, queued, and cache-hit probes against staging with raw responses and latency. |
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:82:| P1 | Full backend suite stalled in the connected desktop environment. | Run the full suite in the dependency-complete CI/test image with per-test timeout and duration reporting; investigate any test exceeding the suite budget. |
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:84:| P1 | Cross-browser and mobile browser evidence remains incomplete. | Run Chromium, Firefox, WebKit, and configured mobile projects with service workers blocked and capture axe, auth, chat, and responsive artifacts. |
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:85:| P2 | Frontend lint still reports 31 non-blocking warnings. | Triage remaining Fast Refresh export warnings and any newly appearing hook warnings; keep zero errors as a hard gate. |
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:94:# Dependency-complete full backend suite in CI/test image
docs/production-readiness/LOOP-REMEDIATION-2026-08-24.md:104:This loop materially reduced the application’s known local failure surface and converted several silent or misleading states into explicit, testable contracts. The repository now has a repeatable validation runner and a stronger fail-closed posture. It is **not honest to mark the system production-ready yet**, because the final evidence still lacks a complete backend run, a completed dependency audit, approved curated runtime artifacts, and live staging proof. The correct next loop is environment-backed: dependency upgrades, artifact production and verification, staging probes, RLS/deletion/restore/rollback checks, and cross-browser execution.
docs/architecture/grounded-guru-response-standard.md:64:- The legacy instruction to translate founders’ first person into third person is present in an active generation prompt.
docs/production-readiness/BASELINE.md:59:5. Integrate changes in security/data-integrity-first order and rerun the complete release gate.
docs/architecture/backend-optimization-guide.md:147:- Set `keep_alive=300` (5 minutes) for active conversations.
.claude/skills/hyperresearch-10-triple-draft/SKILL.md:30:- `research/runs/<vault_tag>/temp/evidence-digest.md` — top claims + verbatim quotes — PRIMARY EVIDENCE LAYER (full only; absent for light)
.claude/tasks/edge_case_security_latency_quality_plan.md:102:- **GPTCache vs semcache confusion**: Two semantic cache systems (Qdrant-based 1024d + GPTCache server) → which one is active?
.claude/tasks/edge_case_security_latency_quality_plan.md:155:| 1 | **OpenRouter 429 on every call** | Reliability | Add provider failover: Ollama → Sarvam → Krutrim for classification/memory/distress. Add jittered exponential backoff. Separate API keys for interactive vs batch. |
.claude/tasks/edge_case_security_latency_quality_plan.md:209:4. Separate API keys for interactive vs batch workloads (prevents starvation)
.claude/tasks/edge_case_security_latency_quality_plan.md:316:  is_active   BOOLEAN DEFAULT false,
.claude/tasks/edge_case_security_latency_quality_plan.md:330:  - `get_active()` → `supabase.table("prompt_versions").select("*").eq("name", n).eq("is_active", true).single().execute()`
.claude/tasks/edge_case_security_latency_quality_plan.md:331:  - `rollback()` → UPDATE is_active=0 for current, UPDATE is_active=1 for target version
docs/runbooks/RERANKER_AB_TEST_RUNBOOK.md:176:- **Did not change the default reranker**. The active model in production stays `ms-marco-MiniLM-L-6-v2` until you flip the env var.
docs/architecture/latency-roadmap.md:14:| **React.lazy admin routes** | Cut initial bundle ~40% (admin code is heavy) | Low | TODO |
docs/architecture/latency-roadmap.md:15:| **Prefetch /chat chunk on landing hover** | Faster navigation to chat | Low | TODO |
docs/architecture/latency-roadmap.md:16:| **Service Worker for static assets** | Instant repeat loads | Medium | TODO |
docs/runbooks/QDRANT_VAULT.md:14:| Vector size | `settings.embedding_dimension` (default `1024`) | Must match the active encoder (`BAAI/bge-m3`). |
docs/runbooks/QDRANT_VAULT.md:105:- Change the active embedding model to a 384-dim model.
docs/runbooks/RETENTION.md:3:Source of truth for retention windows and purge scheduling. Executor: `backend/scripts/ops/cleanup_inactive_user_data.py`.
docs/runbooks/RETENTION.md:11:| Tier 3 | Qdrant user memory vectors | 365d | user inactive (`profiles.last_active_at < now() - 365d`) **AND** point stale (`updated_at < cutoff`) |
docs/runbooks/RETENTION.md:28:.env/bin/python scripts/ops/cleanup_inactive_user_data.py --dry-run
docs/runbooks/BACKUP_RESTORE_DRILL.md:172:3. Do not mark the monthly reminder issue as complete until the root cause is fixed and the drill is re-run.
.claude/agents/hyperresearch-draft-orchestrator.md:8:  then writes one complete draft from the assigned angle. The main
.claude/agents/hyperresearch-draft-orchestrator.md:132:Write your complete draft to `output_path`. Your draft must:
docs/PENDING_AND_EDGE_CASES.md:28:## Main impactful work completed (Jun 25, 2026)
docs/PENDING_AND_EDGE_CASES.md:35:- The `assistant_slug` column is active in the database.
docs/admin/backend-integration.md:25:  () => llm.complete(prompt));
.claude/tasks/EU_AI_ACT_COMPLIANCE_AND_PROVENANCE_PLAN.md:37:> 2. **Retrospective Corpus Backfill**: A standalone scanner/migration (`scripts/ops/eu_ai_act_backfill.py`) will classify the 89,053 historical Qdrant points, Supabase `chat_messages`, and Neo4j nodes into `human_generated` (sacred original texts/discourses) vs `ai_generated` (contextual chunks/synopses).
.claude/tasks/EU_AI_ACT_COMPLIANCE_AND_PROVENANCE_PLAN.md:95:  - `transparency_art50`: Interactive AI conversational interface & synthetic audio generation (requires mandatory disclosure).
.claude/tasks/EU_AI_ACT_COMPLIANCE_AND_PROVENANCE_PLAN.md:171:  - Iterates over all 89,053 points in `spiritual_wisdom`.
docs/admin/evals.md:10:- `active` — only active questions are run
docs/admin/evals.md:15:1. Snapshot the active prompt version
docs/admin/evals.md:16:2. For each active golden question, call the assistant exactly as a user would
docs/transcript_audit_research_notes.md:12:The local corpus contains 745 video directories. The first corrected audit found 643 packages with no structural or cryptographic issue, 102 with issues, and 177 with warnings. Eighty-eight directories are empty/incomplete and account for all 88 parse/missing-file issue groups. Of the 657 non-empty packages, 5 report an invalid quality state (`sound_only`) under the repository’s current expected states, 166 have most paragraphs outside the nominal 300–500-character target, and 15 contain timestamp-like text in the transcript body. These figures are local measurements from the read-only audit, not external claims.
docs/mukthi_quality_research_notes.md:6:- `https://www.youtube.com/watch?v=fEQlTt7vDb0` (Shape agent memory with ontologies), `https://www.youtube.com/watch?v=JGFoTQt4GA0` (Actionable Knowledge for Agents with Context Graphs), and `https://www.youtube.com/watch?v=W2HVdB4Jbjs` (Architecting Agent Memory) were launched in parallel; the first two long-running analyses had not completed at the time of this note. Their analysis outputs must not be treated as complete until their saved result files show completion.
docs/mukthi_quality_research_notes.md:27:A real local question was submitted successfully from the composer. The UI immediately displayed the user bubble, an active “Searching …”/pipeline state, and a visible Stop control, confirming the expected streaming affordance. After the local backend failed to respond, the UI rendered a bounded `ERR_UNKNOWN` / “Failed to fetch” error with Retry last message and Details controls rather than leaving an endless spinner. The page also exposed copy, read-aloud, save-to-memory, save-as-note, share, edit-and-resend, provenance, and groundedness controls on the assistant message.
docs/admin/alerts.md:16:for each active rule:
docs/CHURN_PLAYBOOK.md:25:### Proactive triggers (surface these without being asked)
docs/CHURN_PLAYBOOK.md:94:| 15 | "Your membership is paused" | matter-of-fact |
docs/COST_EFFECTIVENESS_AUDIT_2026-08-22.md:13:The most economically attractive safe work is memory reduction and cache/accounting instrumentation. A sustained **1 GB reduction is worth approximately $10 per month**, and a sustained **2 GB reduction approximately $20 per month**, using Railway’s official resource rate [1]. However, the current production evidence does not prove that ONNX INT8 would save one or two GB, nor that it would preserve retrieval quality. It therefore remains correctly gated. Neo4j schema mutation and broader graph concurrency have no demonstrated cost payback yet: the remote graph is healthy, current Neo4j CPU is low, and no controlled graph-on versus graph-off experiment has established either a quality lift or a lower cost per successful answer.
docs/COST_EFFECTIVENESS_AUDIT_2026-08-22.md:22:| CPU | Included in the remaining approximately 6% | **Approximately 5%** | Not available in this snapshot | Secondary to memory |
docs/COST_EFFECTIVENESS_AUDIT_2026-08-22.md:23:| Volume storage and egress | Included in the remaining approximately 1% | **Approximately 1% combined** | Not available in this snapshot | Currently immaterial |
docs/COST_EFFECTIVENESS_AUDIT_2026-08-22.md:41:After the FlagEmbedding-only cache-pruning release, live SSH observations reported `/app/.cache/huggingface` at approximately `2.3–2.7G`, process RSS around `2.78–3.02 GiB`, and cgroup current around `4.26–4.40 GB`. The unused ONNX snapshot was absent while the required PyTorch model remained. These are point-in-time post-release observations, not a replacement for the historical 30-minute service-metrics window and not proof of reduced invoice spend.
docs/RELEASE_READINESS_2026_07_30.md:21:**Verdict:** infrastructure and security hardening are release-ready; frontend quality gates (i18n, responsive, auth E2E) are the remaining blockers.
docs/RELEASE_READINESS_2026_07_30.md:80:| `/api/health` | Real readiness — `ready: false` until `startup_complete=True`; reflects Qdrant/Redis/Neo4j/Supabase health |
docs/RELEASE_READINESS_2026_07_30.md:84:curl -s https://api.askmukthiguru.com/api/health       # ready:true once startup completes
docs/RELEASE_READINESS_2026_07_30.md:200:- [ ] i18n `t()` audit complete — all 6 real locales covered
docs/MEMORY_FIX.md:69:completes streaming (`onDone` of `sendMessageStreaming`). Failures must be
docs/marketing_strategy.md:20:- **Not started** — deferred until ingestion is complete
docs/kg_backend_research.md:37:| Ecosystem maturity | Very high; LLM Graph Builder, GraphRAG integrations | Growing; Cypher portability helps | Niche but active (Zep memory) | Smaller community; strong in knowledge-graph reasoning niches |
docs/TOKEN_EFFICIENCY_GUIDE.md:49:- **Caveman skill is active** — Claude now reasons with fewer tokens.
docs/PHASE_1_COMPLETE.md:6:- On `/chat` load, reads `profiles.last_active_at` + `last_conversation_id`.
docs/PHASE_1_COMPLETE.md:22:- `profiles.last_conversation_id`, `last_message_id`, `last_active_at`, `last_device_id` columns added.
docs/archive/admin_video_script.md:53:**Visual:** Prompts page — versioned prompt registry (BATCH_GRADE_PROMPT, CASUAL_SYSTEM_PROMPT, DISTRESS_PROMPT, etc., all v1.0.0, active).
docs/PRODUCTION_RAG_OVER_MILLIONS_OF_PDFS.md:7:The codebase already covers most principles at least partially; the remaining work is mostly about enabling gated defaults, finishing decomposition, and validating with benchmarks.
docs/PRODUCTION_RAG_OVER_MILLIONS_OF_PDFS.md:98:- The default `rag_top_k_retrieval` is now set to `20` (down from `30`) with the score-delta cutoff active.
docs/PRODUCTION_RAG_OVER_MILLIONS_OF_PDFS.md:229:1. **[x] Enable the ekimetrics port by default** (completed)
docs/PRODUCTION_RAG_OVER_MILLIONS_OF_PDFS.md:230:2. **[x] Turn on retrieval deduplication and score-delta cutoff** (completed)
docs/PRODUCTION_RAG_OVER_MILLIONS_OF_PDFS.md:231:3. **[x] Lower default top-k from 30 to 20** (completed)
docs/PRODUCTION_RAG_OVER_MILLIONS_OF_PDFS.md:232:4. **[x] Enable `raptor_parent_summaries_enabled`** (completed)
docs/PRODUCTION_RAG_OVER_MILLIONS_OF_PDFS.md:233:5. **[x] Expose the missing config knobs** (completed)
docs/PRODUCTION_RAG_OVER_MILLIONS_OF_PDFS.md:238:10. **[x] Run Phase 6 verification** (completed)
docs/DEPLOYMENT_GAP_ANALYSIS.md:22:**Biggest gaps:** Neo4j absent from prod compose, no observability stack in prod, no S3, celery-worker missing.
docs/DEPLOYMENT_GAP_ANALYSIS.md:46:**TODO (10):** TLS cert automation, CORS allowlist verification, external secrets operator (ESO), WAF, DDoS protection, container image scanning (Trivy), SBOM generation, encrypted volumes, S3/Sarvam key rotation policy.
docs/DEPLOYMENT_GAP_ANALYSIS.md:104:3. **K8s celery-worker absent** — ingestion pipeline can't run in K8s prod; only dev compose has it.
docs/archive/PROD_READINESS_FINAL.md:39:Every backend write that used to bypass RLS must now route through an edge function that validates the caller's JWT. Already migrated: `admin-telemetry`, `memory-embed`, `ingest-source`. Any remaining service-role usage on Railway is a blocker — audit Railway FastAPI code for `create_client(SUPABASE_URL, SERVICE_ROLE_KEY)` before flipping env.
docs/REFACTOR_BLUEPRINT.md:25:- **Deepened interface (chosen):** one `LLMGateway.complete(prompt, task=…)`
docs/DEPLOY_READINESS.md:17:| **Data retention lifecycle** | README: 3-tier memory retention (Redis 15-min TTL ephemeral, 90-day transient logs, protected core vault; `scripts/ops/cleanup_inactive_user_data.py` purges >365-day inactive accounts) | Matches privacy-by-design expectations for a companion app |
docs/DEPLOY_READINESS.md:32:| **No billing/payments system implemented** | `rg -l stripe` across the whole repo (excluding docs/plans) returns **zero application code** — only planning docs (`PRE_LAUNCH_CHECKLIST_PLAN.md:255-301`, `docs/CHURN_PLAYBOOK.md`, `docs/marketing_strategy.md`) reference Stripe as a future TODO. There is no `backend/app/api/webhooks/stripe.py` despite being referenced in the checklist. | Cannot monetize; the checklist itself marks "Enforce your login and paywall on the server" and "Test Stripe webhooks" as **⏳ Pending**. No entitlement/plan model exists to gate premium features server-side. |
docs/DEPLOY_READINESS.md:38:| **SEO/legal surface incomplete per the plan's own tracker** | `PRE_LAUNCH_CHECKLIST_PLAN.md` lists 37 items across Security/Emails/SEO/Legal/etc., the large majority marked ⏳ Pending at time of writing | Indicates the team's own gate has not been fully cleared; treat this file as the actual go/no-go source of truth. |
docs/DEPLOY_READINESS.md:48:1. **Proprietary doctrine corpus** — Qdrant `spiritual_wisdom` collection: 89,053 ingested items from books, 450+ YouTube discourses, and lectures specifically by Sri Preethaji & Sri Krishnaji (README, `scripts/ingest_lightrag_data.py`). This is licensed/curated first-party content, not scraped generic wisdom text — a competitor without a relationship to these teachers cannot legally or practically replicate the corpus.
docs/DEPLOY_READINESS.md:52:5. **12-layer pipeline + guru-voice fidelity** — `guru_tone_adapter.py` adapts generation specifically to the two named teachers' voice/persona while `verification.py` actively *prevents* first-person impersonation of them (an ethical/IP-risk control most competitors skip). This is simultaneously a brand-safety feature and a technical moat since it requires curated fine-tuning/prompting + guardrail engineering per persona.
docs/DEPLOY_READINESS.md:90:**Commercial readiness: not ready.** The project's own `PRE_LAUNCH_CHECKLIST_PLAN.md` (37 items) shows the majority of launch-blocking items — RLS enforcement, HTTPS/HSTS verification, transactional email deliverability, and the entire billing/Stripe layer — still marked pending. There is currently **no monetization path in code** and **no confirmed database-level access control**, both of which are P0 blockers for a paid commercial SaaS launch, independent of how mature the AI pipeline itself is. Two production env files (`.env.production`, `backend/.env.prod`) being tracked in git (currently secret-free but placeholder-based) should also be removed from version control as a hygiene fix before scaling the team.
docs/translation_qa_checklist.md:41:- [ ] **Expected:** The input textarea is cleared **immediately** on send, before the translation completes.
docs/translation_qa_checklist.md:56:- [ ] **Fail:** If no such log appears when a non-English language is active, or `blockedRender` is ever `true`.
docs/translation_qa_checklist.md:62:- [ ] **Expected:** No `[Translation]` log appears (translation is inactive for `en`). Render and input clearing are immediate.
docs/archive/WHAT_TO_DO.md:159:If the answer is *"The meditation is complete..."* and the query wasn't a
docs/archive/WHAT_TO_DO.md:310:3. Verify the "AI-tell" patterns are absent:
docs/top_to_bottom_quality_review.md:13:The remaining risk is **end-to-end operational proof**, not frontend compilation. The local browser could not reach a compatible backend, and the authenticated Second Brain flow could not be tested without user credentials. The backend test suite progressed through several code-compatibility blockers but then stopped at a missing local `celery` dependency. Production verification therefore still requires a real authenticated environment, live Qdrant/Neo4j connectivity, and competency-question evaluation over personalized retrieval.
docs/DESIGN_SYSTEM.md:33:| `ojas-gold` | `43 96% 56%` | primary accent, active states, sacred highlights |
docs/DESIGN_SYSTEM.md:49:active practice, key CTA). Never use gold for decoration or noise. Calm blue
.claude/skills/hyperresearch-15-polish/SKILL.md:89:Before declaring the run complete, verify every expected pipeline artifact exists. **The required set depends on the tier:**
.claude/skills/hyperresearch-15-polish/SKILL.md:132:   If any rule returns `error` severity issues, address them before declaring complete:
docs/archive/handoff_railway.md:35:- [x] **health.py**: Fixed `startup_complete` import binding bug (so `/api/health` updates to `healthy` dynamically)
docs/archive/handoff_railway.md:117:## Verification Commands (after build completes ~8 min)
.claude/tasks/handoff.md:4:Prepare the app for demo. Two remaining blockers:
.claude/tasks/handoff.md:47:6. **Next: wait for deployment `8fbae95b` to complete, then test chat query inline**
.claude/tasks/handoff.md:72:1. **Wait for deployment to complete**: Check build logs at https://railway.com/project/7cbd5f8c-2bb1-4ba8-8de9-8bc36a14e137/service/18690f24-5e2d-4fdb-95f6-42142111df7e?id=8fbae95b-cc1e-4111-bbc1-a4e1ba4eb96b or run `railway deployment list` until status is SUCCESS
.claude/tasks/handoff.md:73:2. **Fire chat query**: After deploy completes:
.claude/skills/hyperresearch-3-contradiction-graph/SKILL.md:9:  tool from the entry skill after step 2 completes.
docs/archive/user_video_script.md:6:HeroSection, HowItWorksSection, ChatInterface + SpiritualWelcomeBanner (personalized daily line), SereneMindModal, WisdomCardGenerator, StudyNotebookPage, **KGConceptMap** (new interactive Obsidian-style knowledge graph), SpiritGuidesPage, LanguageSelector, **incognito mode** (session-isolated, visually indicated in the header), **SafetyPillarsSection** (real on-site copy), **SampleWisdomSection** (real quotes from Sri Preethaji & Sri Krishnaji, with YouTube source links). No invented features, no invented claims.
docs/archive/user_video_script.md:55:4. **Knowledge Graph** — the new interactive concept map, one smooth pan across a couple of connected nodes. Genuinely new, visually the most striking thing in the app right now — worth the beat.
docs/archive/user_video_script.md:94:- "Interactive and animated": I can deliver animated captions/titles and clean transitions (fades, timed text, subtle motion) in editing via ffmpeg. I can't hand-produce custom motion-graphic characters or a fully choreographed animation sequence — flagging so expectations are calibrated before the edit, not after.
.claude/skills/hyperresearch-1-decompose/SKILL.md:126:   | `"argumentative"` | Defended thesis, deep analysis, evidence-chain reasoning. | 5000–10000 words. Dense thesis-driven prose. "ARGUE, DON'T JUST REPORT" fully active. Required Opinionated Synthesis with all subsections. |
docs/archive/TOKEN_AND_COST_OPTIMIZATION.md:26:call. For the remaining 30% we fall through to the LLM classifier. Estimated
docs/archive/TOKEN_AND_COST_OPTIMIZATION.md:165:- [ ] Is this call non-interactive (eval, batch ingest, daily summary)? →
.claude/tasks/LOOP_ENGINEERING_REMEDIATION.md:3:**Status:** CURRENT — active remediation task
.claude/tasks/LOOP_ENGINEERING_REMEDIATION.md:14:- Do not install dependencies into the sandbox; use the repository virtualenv, CI, or available desktop Docker environment for dependency-complete tests.
.claude/tasks/LOOP_ENGINEERING_REMEDIATION.md:27:9. Rerun the complete audit prompt and update the final readiness score only from new evidence.
.claude/tasks/LOOP_ENGINEERING_REMEDIATION.md:32:- Every remaining P1/P2 issue has an owner, acceptance test, and release impact.
.claude/tasks/LOOP_ENGINEERING_REMEDIATION.md:33:- The browser flagship journey completes a harmless chat with either a visible answer or a precise retryable error.
.claude/skills/hyperresearch-2-width-sweep/SKILL.md:9:  after step 1 completes.
.claude/skills/hyperresearch-2-width-sweep/SKILL.md:227:Four commands that turn the corpus into a *ranked* corpus. Run them once, in order, after fetching completes:
.claude/skills/hyperresearch-2-width-sweep/SKILL.md:276:   > "3 sources need you: [site A: solve the CAPTCHA], [site B: log in], [site C: approve 2FA]. Open them in Chrome, complete the challenges, then tell me 'done' (or 'skip')."
.claude/skills/hyperresearch-2-width-sweep/SKILL.md:277:2. In non-interactive (`-p`) runs where no user can answer: record `$HPR run block <vault_tag> --on human-challenges -j` and CONTINUE the pipeline with everything else — the queue drains on the next `hpr run resume`.
docs/archive/AUDIT_REPORT.md:110:| **LightRAG `ainsert` is fire-and-forget** | 🔴 CRITICAL | `_ingest_video:896` | If LightRAG fails silently, graph incomplete but ingestion "succeeds" |
docs/archive/AUDIT_REPORT.md:142:| **YouTube ID-only title filter good but incomplete** | 🟠 MEDIUM | `extract_citations:71-74` | Doesn't catch "Unknown", "", "YouTube Video" titles |
docs/archive/AUDIT_REPORT.md:278:| Frontend YouTube handling | Cross-origin bug, incomplete URL parsing, no error states | 1-2 days |
docs/kubernetes-deployment.md:346:| Pods | Concurrent Users | Active Users (10% concurrency) | DAU (5% active) |
.claude/tasks/ruthless-ingestion-audit/AUDIT_REPORT.md:5:Perform a complete, end-to-end ruthless audit of the `askmukthiguru` ingestion pipeline — with a primary focus on **YouTube video ingestion** — diagnose every failure point, research how industry leaders (Recall.it, Zabt.ai, Supadata) solve the same problems, and deliver a **production-grade, cloud-portable, free-first solution** that works on Railway today and any cloud tomorrow without rework.
.claude/tasks/ruthless-ingestion-audit/AUDIT_REPORT.md:49:| **celery-worker** | ✅ Online (separate service) | `SERVICE_TYPE=celery`, concurrency=1, 5 queues active |
.claude/tasks/ruthless-ingestion-audit/AUDIT_REPORT.md:57:Celery worker queues and tasks confirmed active:
.claude/tasks/ruthless-ingestion-audit/AUDIT_REPORT.md:62:       ingest_playlist, playlist_complete, compile_okf_index, extract_okf_entries,
.claude/tasks/ruthless-ingestion-audit/AUDIT_REPORT.md:118:| **What** | Only `orchestrate_ingestion` has manual `self.retry()` in its except block. `embed_chunks`, `index_vectors` have manual retry via inline `self.retry()`. Tasks with no except block retry (e.g., `playlist_complete`) have zero self-healing. None use Celery's declarative `autoretry_for` mechanism. |
.claude/tasks/ruthless-ingestion-audit/AUDIT_REPORT.md:164:**Root cause confirmed by research:** Railway runs on Google Cloud infrastructure. YouTube (owned by Google) actively blocks requests from known cloud datacenter IP ranges — including GCP, AWS, Azure, and Railway's shared IPs. This is not a rate limit issue; it is a **categorical IP block** applied to all datacenter egress IPs.
.claude/tasks/ruthless-ingestion-audit/AUDIT_REPORT.md:214:- Whisper (local via `faster-whisper`) is completely free and runs on CPU — no GPU required for short videos.
.claude/tasks/ruthless-ingestion-audit/AUDIT_REPORT.md:333:1. **`AUDIT_REPORT.md`** — This file — complete ruthless audit with every bug, anti-pattern, and root cause documented with file + line references.
.claude/tasks/ruthless-ingestion-audit/AUDIT_REPORT.md:356:None — plan is decision-complete. All decisions made based on research.
.claude/skills/hyperresearch-16-readability-audit/SKILL.md:163:## Pipeline complete
.claude/skills/hyperresearch-16-readability-audit/SKILL.md:165:Return to the entry skill (`hyperresearch`). Mark all todos complete. Tell the user the final report path.
docs/LOAD_TEST_RESULTS.md:25:| `chat-rate-limit` burst | remaining reqs | 429 (Retry-After header set) |
docs/archive/MIGRATION_DECISION.md:5:**Status:** Phase 1 complete. See `USER_ACTIONS.md` for what's left.
.claude/tasks/NORMAL_INGESTION_PIPELINE_RUTHLESS_AUDIT.md:18:8. `scripts/ingestion/extract_transcripts.py` (Actor ingestion, completeness validation, punctuation & ledger)
docs/INCIDENT_RESPONSE.md:35:# 2. Kill active sessions that used the old JWT secret
docs/INCIDENT_RESPONSE.md:169:After any P0/P1 incident, complete within 48 hours:
.claude/skills/hyperresearch/SKILL.md:143:After step 1 returns, read `research/runs/<vault_tag>/prompt-decomposition.json` to learn the tier, then continue invoking step skills per the tier routing table above. After each step's exit criterion is met, mark its todo complete and move to the next.
.claude/skills/hyperresearch/SKILL.md:149:1. **NEVER EMIT BARE TEXT WHILE TASKS ARE RUNNING.** In non-interactive (`-p`) mode, a text-only response (no tool call) triggers `end_turn` — the process exits and the pipeline dies. Every response while subagent tasks are in flight MUST include a tool call. The best one is appending analytical thoughts to `research/runs/<vault_tag>/temp/orchestrator-notes.md`. Vault count checks at most once per minute.
.claude/skills/hyperresearch/SKILL.md:163:1. **CAPTCHAs / logins / 2FA are ALWAYS the human's.** The browser-fetcher marks them `needs_human`; you consolidate ALL of them into ONE message to the user at a natural pause point (never one interruption per URL). In non-interactive runs, `$HPR run block <vault_tag> --on human-challenges` and continue with everything else.
.claude/skills/hyperresearch/SKILL.md:257:6. **Steps are sequential at the outermost level, parallel within.** You cannot start step N+1 before step N completes. Within a step, parallelism is mandatory when there are multiple subagents.
.claude/skills/hyperresearch/SKILL.md:265:14. **A run is complete ONLY when `run finish` reports `passed: true`.** Gate failures are fixed by changing the report, never by re-interpreting, downgrading, or memo-ing away the checks. If 3 fix rounds don't clear the gate, the run stays `blocked` and you say so.
docs/operations/repository-cleanliness-audit-2026-08-12.md:12:| `.qoder/settings.json.bak` | Byte-identical backup of the active `.qoder/settings.json`. | Removed and ignored. | 4 KB |
docs/operations/repository-cleanliness-audit-2026-08-12.md:15:> **Guardrail:** “Unreferenced” means absent from tracked application, test, workflow, and documentation text at the audit revision. It does not mean that an artifact is unimportant outside Git; retained or deferred items require an explicit archival decision.
docs/operations/repository-cleanliness-audit-2026-08-12.md:24:| P1 | `video-composition/assets/` | Screens, keyframes, audio stems, and validation JSON support the retained legacy production project. | Keep while the production pipeline is supported; otherwise archive the complete source bundle outside Git rather than deleting isolated components. |
docs/operations/repository-cleanliness-audit-2026-08-12.md:30:A clean checkout has no tracked disposable test output, compiled work directories, duplicate editor backups, or superseded rendered demos. Generated output is ignored and reproducible. Retained large assets have an explicit source or pipeline relationship. Any removal that affects an active workflow, a release asset, or research evidence must be carried out in a separate reviewed change with archival confirmation.
docs/operations/repository-cleanliness-audit-2026-08-12.md:32:## Follow-up completed — book-to-skill removal
docs/operations/repository-cleanliness-audit-2026-08-12.md:34:The undeclared `book-to-skill` gitlink was removed after verifying that the only tracked dependency was the unreferenced `scripts/generate_all_skills.py` helper. That helper, its Docker exclusion, and the nested external worktree were removed together. No application module, test, CI workflow, deployment configuration, or active documentation path referenced the integration.
docs/operations/repository-cleanliness-audit-2026-08-12.md:36:## Follow-up completed — generated report separation
docs/operations/repository-cleanliness-audit-2026-08-12.md:38:Reproducible evaluation reports now default to `artifacts/evaluations/`, which is ignored. The stale tracked evaluation reports, duplicate query-results export, data-quality report, and root-level E2E screenshot were removed. The E2E screenshot test now writes into the Playwright-managed per-test output directory. Retained `results/query_results.json` and `results/The_Four_Sacred_Secrets_structure.json` remain because active tests, documentation, or ingestion scripts reference them.
docs/operations/repository-cleanliness-audit-2026-08-12.md:40:## Follow-up completed — legacy video composition retirement
docs/operations/landing-product-demo-video-handoff.md:3:**Status:** The landing-page integration is ready. Add the approved, captioned MP4 URL as `VITE_LANDING_DEMO_VIDEO_URL` at build time. Until a final video is supplied, the existing accessible three-step interactive tour remains the main landing-page demo action.
docs/operations/landing-product-demo-video-handoff.md:39:The main hero’s existing **“See how this works”** action now opens the three-step interactive tour and, when configured, the completed video at the top of that same dialog. This preserves a useful user experience if the video cannot load and avoids an autoplay or external-embed privacy cost.
docs/operations/landing-product-demo-video-handoff.md:47:Before release, verify keyboard access to the dialog, visible controls, caption availability, reduced-motion behavior, mobile layout, MP4 cache headers, poster loading, and the fallback tour with the variable intentionally absent.
docs/operations/requirement-reconciliation.md:29:The current application is suitable only for internal engineering and supervised non-production validation. It is not cleared for a public waitlist, public live logistics, automatic memory, WhatsApp, public attachments, multi-teacher expansion, or a 500 to 1,000 user rollout. The next work must prioritise the remaining P0 proof gaps over feature activation.
docs/ROLLBACK_PLAN.md:138:   - Gather feedback on any remaining issues
docs/ROLLBACK_PLAN.md:237:- Status Updates: Every 15 minutes during active rollback
docs/operations/production-deployment-runbook.md:58:| Recovery | Stop a worker after enqueueing a test row, restart it, and confirm the scheduled task completes the row within two minutes. |
docs/operations/production-deployment-runbook.md:59:| Erasure | `DELETE /api/memory/all` returns `completed` and a `memory_deletion_receipts` record listing all store counts. A `partial_failure` response must be retried. |
docs/operations/production-deployment-runbook.md:79:its NX lease remains the active queued-chat path.
docs/operations/production-deployment-runbook.md:103:not ready for deployment until a clean Python 3.12 environment can install the
docs/operations/production-deployment-runbook.md:104:backend lock and a clean Node install can complete `npm ci`, tests, and the
docs/operations/production-deployment-runbook.md:147:release version and must not silently reuse a completed checkpoint from another
docs/operations/production-deployment-runbook.md:153:model invocation, cache, memory, and proactive-practice stages. The deterministic
docs/operations/production-deployment-runbook.md:190:| Activate | Activate an approved release through the protected admin API | The database atomically supersedes the earlier active release for that corpus/source |
docs/operations/production-deployment-runbook.md:191:| Verify | Run retrieval and citation canaries using the active corpus/version | The response evidence envelope reports only structured corpus/release/model-policy facts |
docs/operations/production-deployment-runbook.md:195:by the active release version when the registry is enabled. It does **not** claim
docs/operations/production-deployment-runbook.md:210:Do not call a deployment production-ready merely because unit tests pass. The
docs/operations/production-deployment-runbook.md:218:| Privacy and erasure | Memory deletion drill and waitlist consent path complete as designed | Keep write flags disabled |
docs/operations/production-deployment-runbook.md:232:The active OpenRouter deployment must use pinned model IDs and the server-side policy rather than browser-selected models. Set `OPENROUTER_POLICY_ID`, `OPENROUTER_ENFORCE_MODEL_POLICY=true`, `OPENROUTER_REQUIRE_NO_TRAINING=true`, and the primary, fast, classification, and optional fallback model IDs in Railway. Leave `OPENROUTER_ALLOWED_PROVIDERS` empty unless a verified provider allowlist is required; the policy still requests no-training routing and records the policy ID in the privacy-safe operations view.
docs/operations/production-deployment-runbook.md:246:Run the readiness matrix only against local mock infrastructure or a non-production staging environment using test authentication. It must not point to the public Lovable frontend or the production Railway service. The test progresses through 25, 100, 250, and 500 active users, uses the identical-question workload already represented in the Locust file, and saves a JSON report for every stage.
docs/operations/production-deployment-runbook.md:256:The runner rejects a stage with fewer completed requests than declared active users, p95 above the configured default of 8 seconds, or a failure rate above 1 percent. Record the report directory, deployment SHA, model-policy ID, corpus release, region, Redis/Qdrant/Neo4j topology, and current Railway replica count with each run. A passing mock-provider run proves application backpressure and race handling; it does **not** prove external OpenRouter capacity or global user latency.
docs/MOBILE_RELEASE_RUNBOOK.md:231:- `com.askmukthiguru.app://auth-callback` — native deep link captured by `App.addListener('appUrlOpen')` (registered in `src/App.tsx`) on both Android (intent-filter) and iOS (CFBundleURLTypes). Supabase OAuth completes in the system browser then hands control back to the app.
docs/MOBILE_RELEASE_RUNBOOK.md:242:- [ ] **Apple Sign-In** (Apple Guideline 4.8) — implemented in `src/pages/AuthPage.tsx` (Apple button shown on native). Before submission: configure Apple as a Supabase Auth provider (Services ID + .p8 key + Return URL `https://<project>.supabase.co/auth/v1/callback`), then verify end-to-end on a TestFlight build that the Apple button completes OAuth and lands the user on `/chat`.
docs/MEMORY_BACKEND_HANDOFF.md:238:CREATE INDEX guru_memories_user_active_idx
docs/MEMORY_BACKEND_HANDOFF.md:263:  completed_at TIMESTAMPTZ
.claude/skills/hyperresearch-4-loci-analysis/SKILL.md:29:Survey the corpus: `$HPR note list --tag <vault_tag> --all -j` to confirm width sweep is complete.
docs/IMPROVEMENTS_BACKEND.md:73:        allowed, remaining = await self.script(keys=[key], args=[self.capacity, self.refill, time.time(), 1])
docs/IMPROVEMENTS_BACKEND.md:75:            raise HTTPException(status_code=429, detail={"error": "rate_limited", "remaining": 0})
docs/IMPROVEMENTS_BACKEND.md:77:        resp.headers["X-RateLimit-Remaining"] = str(int(remaining))
docs/IMPROVEMENTS_BACKEND.md:152:  ADD COLUMN IF NOT EXISTS last_active_at timestamptz;
docs/IMPROVEMENTS_BACKEND.md:160:         last_active_at = now()
docs/IMPROVEMENTS_BACKEND.md:172:Detect "new device" via `localStorage` token absence; if `profile.last_active_at`
docs/ADMIN_AUDIT_REPORT.md:138:  SELECT v.name, v.version, v.active,
docs/ADMIN_AUDIT_REPORT.md:143:   GROUP BY v.name, v.version, v.active ORDER BY v.name, v.version;
docs/operations/source-release-rollback-drill.md:3:The code now supports a controlled source-release rollback. Migration `20260805000004_source_release_rollback.sql` allows a **superseded** release to return to `approved` only through an explicit service-role approval. The existing activation transaction then atomically makes it active and supersedes its currently active peer in the same `(corpus_id, source_identity)` scope.
docs/operations/source-release-rollback-drill.md:8:| Approve and activate A | `active` | Reviewer ID, activation timestamp, source-scope canary. |
docs/operations/source-release-rollback-drill.md:9:| Register, approve, activate B | A is `superseded`; B is `active` | Both release IDs and active version B from Qdrant, Neo4j, and LightRAG checks. |
docs/operations/source-release-rollback-drill.md:10:| Re-approve and reactivate A | A is `active`; B is `superseded` | Reviewer ID, restored active version A, and fresh retrieval/citation canaries. |
docs/DEVELOPER_GUIDE.md:3:> A complete, end-to-end onboarding document for new contributors.
docs/DEVELOPER_GUIDE.md:46:Neither model-backed provider is the active default; they exist for evaluation
docs/DEVELOPER_GUIDE.md:230:   │  NeMo/LlamaGuard available in backend/guardrails/ but not the active default.
docs/DEVELOPER_GUIDE.md:268:- `ChatInterface` passes the active conversation id as `session_id` on both
docs/DEVELOPER_GUIDE.md:302:   refetch latest active row, render banner
docs/DEVELOPER_GUIDE.md:461:To enable maximum agent productivity and completely offline codebase analysis, three custom Model Context Protocol (MCP) servers reside in the `mcp-servers/` directory.
docs/SESSION_20260618.md:25:- **File**: `src/components/chat/SereneMindModal.tsx` — Close (X) button visible when `phase === 'complete' && isGated`
docs/BACKEND_INTEGRATION_ASSISTANTS_AND_NOTES.md:24:Frontend will start sending these optional fields on every chat request. The backend must accept them, default safely when absent (preserving today's behavior), and never 4xx if they're missing.
docs/BACKEND_INTEGRATION_ASSISTANTS_AND_NOTES.md:46:   - When `knowledge_tags` is empty or absent → retrieve across all corpora (today's behavior).
docs/operations/product-hardening-backlog.md:13:| Browser worker caches only immutable public static assets; it no longer intercepts API, personalised, crisis, practice or live-information requests and never returns a simulated offline spiritual answer. | Implemented, verification pending in browser integration. | Service-worker integration test proves authenticated/API responses are absent from Cache Storage; sign-out purge and offline UX review. | PWA promotion blocked until proof exists. |
docs/operations/product-hardening-backlog.md:69:| SSE streaming now checks for a client disconnect and cancels the active pipeline in generator cleanup. | Implemented and regression-tested with a blocking pipeline. | Staging disconnect test confirms provider cancellation propagation and no leaked tasks under concurrent streams. | Client disconnects stop active request work; detached consent-enabled enrichment remains timeout-bounded. |
docs/operations/product-hardening-backlog.md:87:| The active backend Docker entrypoint now starts as root solely to repair mounted cache/data permissions, then drops to `appuser` before the server or worker executes. | Implemented; shell syntax verified. | Build and start the Railway image with representative mounted volumes; confirm the application process UID is `appuser` and writable paths are limited to the intended data/cache directories. | Prevents non-root startup failures caused by root-owned mounts without serving the application as root. |
docs/operations/product-hardening-backlog.md:89:| The standalone legacy WhatsApp broker is fail-closed: every route returns 404 and direct startup exits unless `WHATSAPP_WEBHOOK_ENABLED=true`. No active backend compose or Railway configuration starts the broker. | Implemented and regression-tested. | A separate production design must pass identity, consent, replay/idempotency, durable state, deletion, redaction, budget, and provider-webhook verification before the flag is set. | WhatsApp remains frozen for the initial release. |
.claude/tasks/ruthless-productionization.md:24:| Environment | Prefer local/staging and safe read-only production probes. If dependencies are unavailable, distinguish source validation from dependency-complete verification; do not install ad hoc packages or fabricate results. |
.claude/tasks/ruthless-productionization.md:56:Validate encrypted-memory and ephemeral-evidence boundaries, chat retention, inactive-account cleanup, right-to-forget flows, audit/compliance metadata, and separation of Qdrant/Neo4j indexes from private Postgres content. Add or correct migrations only with backward-compatible rollout and rollback considerations; never destroy existing user data casually.
.claude/tasks/ruthless-productionization.md:60:Map retrieval and generation stages, model/provider selection, embedding/reranker contracts, cache tiers, LightRAG/Neo4j/Qdrant paths, ingestion checkpoints, transcript normalization, graph writes, and evaluation gates. Verify vector dimensions, pinned model revisions, corpus completeness, per-category retrieval quality, citations/faithfulness, timeout budgets, retry/circuit-breaker behavior, and fallback correctness against safe fixtures or read-only production probes.
.claude/tasks/ruthless-productionization.md:62:Measure before optimizing. Capture representative p50/p95/p99 latency, throughput, error rate, queue depth, CPU/memory, query latency, bundle size, key endpoint timing, retrieval quality, and LLM/token/provider usage where the environment permits. Bound expensive work using quotas, token/context limits, concurrency/backpressure, cache correctness, batching, asynchronous work, provider timeouts, and cost-aware routing. Do not change model/index architecture based on aggregate metrics alone; retain complete-source, per-category, held-out quality, and compatibility gates.
.claude/tasks/ruthless-productionization.md:68:Strengthen the smallest practical CI pipeline: install, lint, type-check, unit/component tests, backend tests, security/dependency scans, build, migration checks, targeted integration tests, critical E2E, artifact retention, and deployment smoke/health validation. Keep optional credentialed tests explicit and fail clearly when secrets are absent. Update README, developer guide, lessons, roadmap/product-opportunity documentation, architecture/config references, and operational runbooks so they match shipped behavior.
.claude/tasks/ruthless-productionization.md:90:A separate reviewer receives the post-change system as if they did not build it. Re-check changed and adjacent surfaces for regressions, missed security holes, incomplete UX states, stale docs, and false confidence from mocks. Attack the product as a malicious user, careless user, broken dependency, high-concurrency client, new developer following docs, and operator deploying/rolling back. Fix only confirmed findings, then run the full regression gate again.
.claude/tasks/ruthless-productionization.md:94:Generate the final production-readiness report with `VERIFIED`, `NOT VERIFIED`, and `ESTIMATED` labels. Include executive summary, concrete changed files/modules, added/removed items, before→after security/performance/cost findings, all test/build/lint/type/E2E/load results, critical journey PASS/FAIL evidence, remaining risks, exact deploy and rollback steps, monitoring, scaling, cost model, architecture rationale, and exactly one verdict: **READY FOR PRODUCTION** or **NOT READY FOR PRODUCTION**. If blocked, name the exact blocker and the smallest next action.
.claude/tasks/ruthless-productionization.md:116:4. After each integration batch, run affected tests and critical smoke journeys; after all batches, run the complete release gate and independent second audit.
.claude/tasks/ruthless-productionization.md:122:- The local checkout may not have all dependency-complete services or credentials; results must be separated into verified local, verified staging/production-read-only, and unverified.
.claude/tasks/ruthless-productionization.md:130:The work is complete only when the repository has materially improved code/configuration/tests/documentation, critical journeys have evidence-backed PASS/FAIL results, major security/reliability/cost risks are either fixed or explicitly blocked, CI and operational procedures are reproducible, an independent second audit and red-team pass are complete, and the final report truthfully states readiness.
docs/RUTHLESS_AUDIT_BLUEPRINT.md:50:  - **Interactive Mock Simulator**: Real-time voice/text conversational evaluator with instant feedback.
docs/production/POST_REPORT_AUDIT_2026-08-23.md:9:The corrected production verdict remains **NOT READY FOR PRODUCTION**. This is not because the tested fixes failed. It is because reviewed OKF/doctrine runtime artifacts remain absent, the two flagship responses remain explicitly unverified partial evidence rather than verified generated teachings, cost and memory headroom are not closed, p95/p99 behavior is not established, and several authenticated, responsive, DNS, telemetry, benchmark, and recovery gates remain open.
docs/production/POST_REPORT_AUDIT_2026-08-23.md:50:The current evidence does not prove direct-stream reconnect/resume, browser refresh recovery, duplicate suppression after reconnect, multi-tab connection behavior, or a full owner-success queued-stream path. It does not prove the custom domain, mobile/tablet rendering, full source-panel coverage, Google OAuth, password reset, upload failure matrix, authenticated RLS/BOLA isolation, or live telemetry aggregation. It does not prove an approved reingestion produced `memory/okf/compiled.json` or `data/doctrine_lexicon.json`; both remain absent in the serving image. It does not prove sustained cost reduction, memory attribution over a fixed window, p95/p99 latency, held-out retrieval metrics, graph-on/off benefit, or restore readiness.
docs/production/POST_REPORT_AUDIT_2026-08-23.md:56:**Confidence in this corrected report: 8/10.** Confidence is high for repository state, source inspection, saved Docker output, saved metadata probes, and the observed Railway command outputs. It is not 10/10 because the final control-plane state after `railway down --yes` was inconsistent, SimilarWeb supplied no data, live external dependencies were not re-probed after the user-requested pause, and the broad browser/benchmark/recovery gates remain incomplete.
docs/RUTHLESS_ONE_SHOT_BLUEPRINT.md:3:> **Vision**: Transform **AskMukthiguru** into an unbeatable, single-shot AI spiritual and life guide. When a user brings a life challenge, anxiety, or spiritual question, the platform delivers an instant **One-Shot Transformation Package**: Direct Authoritative Guidance, Tailored Audio Guided Practice, Interactive Knowledge Graph Visualization, and a Daily Inner Growth Action Plan.
docs/RUTHLESS_ONE_SHOT_BLUEPRINT.md:20:    Step4 --> Deliverable3[Interactive 3D Knowledge Graph Nodes]
docs/RUTHLESS_ONE_SHOT_BLUEPRINT.md:41:When a user asks a question (e.g. *"How do I overcome constant anxiety in my relationships?"*), the backend builds a complete 4-part payload:
docs/RUTHLESS_ONE_SHOT_BLUEPRINT.md:44:3. **Interactive KG Concept Nodes**: Clickable graph nodes (e.g. `Deconstruct the Mind`, `Beautiful State`, `Observer Consciousness`) pointing to `/knowledge-graph`.
docs/RUTHLESS_ONE_SHOT_BLUEPRINT.md:75:- [ ] Update chat frontend composer to render audio player and interactive KG tags inline.
docs/production/PRODUCTION_READINESS.md:19:| Core chat | Grounded answers for representative queries | PARTIAL / FLAGSHIP FALSE-REFUSAL MITIGATED | Post-deploy metadata-only probes returned Serene Mind and Four Sacred Secrets as `grounded_partial_evidence`, `partial=true`, `grounding_state=grounded`, `hallucination_flag=false`, with 2 exact public YouTube citations each and concise response lengths `1,152` and `1,139` chars. Beautiful State, neutral suffering, and Hindi controls were grounded with citations; the acute-crisis control remained a safety redirect. | Railway deployment `6374d867-65f8-4d7d-857d-cdc9531699c1`; 162 passed/2 skipped image suite; metadata-only probes | The generated drafts still fail verification (`faithfulness=0.0`, `verification.passed=false`) for the two flagship partials, curated runtime artifacts are absent, and held-out teaching/citation quality remains open |
docs/production/PRODUCTION_READINESS.md:22:| Provider accounting | Actual, estimated, and unknown AI costs are separated | VERIFIED IN CODE / PARTIAL LIVE | Commit `34c98a5` distinguishes provider-reported cost, configured fallback estimate, and unknown cost; cache read/write counters added | Static/deployment evidence; authenticated aggregation still open | FinOps + Backend | Live dashboard and alert proof incomplete |
docs/production/PRODUCTION_READINESS.md:27:| Browser UX | Critical journeys pass in real browser at required breakpoints | PARTIAL / FLAGSHIP STREAM PROVEN | Hosted Lovable `/chat` completed a fresh Four Sacred Secrets turn and visibly rendered the grounded partial, `Grounded response`, `2 verified sources provided`, `Limited support`, and two full YouTube URLs in the References panel. Direct `/second-brain` and the authorized disposable reflection flow remain evidenced. | Connected-browser capture on 2026-08-23 plus Railway direct/queued SSE probes | Mobile/tablet, custom-domain, authenticated browser breadth, and full source-panel coverage remain open |
docs/production/PRODUCTION_READINESS.md:28:| Authentication | Auth, authorization, AAL2, and tenant isolation are independently verified live | PARTIAL | Connected browser showed authenticated-looking `HK` session chrome; authorized vault create/reload/delete passed after BRAIN_KEK cutover, and both service scopes retain `BRAIN_KEK` with `BRAIN_KEK_NEXT` absent. Fresh independent live red-team pass is incomplete | Browser vault journey, rotation utility CAS result, Railway variable-name audit, CI/static evidence | Security + Privacy | BOLA/IDOR, RLS isolation, and broader authenticated user-flow proof remain open |
docs/production/PRODUCTION_READINESS.md:29:| Uploads | Bounded multimodal extraction is safe and understandable | PARTIAL | Size, semaphore, and SSRF controls exist; full browser and malformed-media failure matrix is incomplete | Static review only in this cycle | Backend + Red Team | Expensive or malformed uploads may still stress the service |
docs/production/PRODUCTION_READINESS.md:30:| Observability | Failures, cost spikes, and token anomalies are diagnosable | PARTIAL | Health, Prometheus metrics, startup logs, provider counters, and direct SSE trace evidence exist; local Docker tracing is now opt-in because Jaeger is an observability profile; protected `/api/metrics` aggregation was not authenticated in this pass | Health/log/SSE checks; authenticated counter visibility open | SRE + Observability | Alert routing and operational dashboard proof remain incomplete |
docs/production/PRODUCTION_READINESS.md:31:| SLOs | Latency, availability, queue, and useful-answer SLOs have error budgets | PARTIAL / LOCAL QUEUED-STREAM TAIL MITIGATED | Docker queued-stream probe before the server completion fix emitted `final`/`done` within roughly `25–68ms` but held the connection open for roughly `2.17–2.36s`; after the fix and backend restart, representative runs completed in `364ms` and `1,704ms`, with first events at `95ms` and `1,118ms`. The frontend non-stream queue transport now polls immediately, then backs off from `250ms` to a maximum of `1s`; Docker frontend transport tests passed. | Dependency-complete Docker tests and localhost probes only; no Railway deployment | Production p95/p99, provider/session/admission overhead, multilingual/comparative tails, and concurrency behavior remain open |
docs/production/PRODUCTION_READINESS.md:33:| Deployment | Reproducible and rollback-aware release | PARTIAL / PAUSED FOR VERIFICATION | Backend deployment `ad84d6a6-04ce-43e5-8e8a-a0e901a7b18d` was observed as `SUCCESS` after the stop request; health was alive in the final bounded check. The prior Railway image suite completed `162 passed, 2 skipped`; the newer direct-SSE headers and playlist-test correction passed in Docker only | Railway deployment list, health checks, clean-archive process, Docker validation | The exact post-stop control-plane state and rollback drill are not independently verified; full restore proof remains incomplete |
docs/production/PRODUCTION_READINESS.md:34:| Data integrity | Corpus and user-memory boundaries are preserved | PARTIAL | Guard checks passed; immutable corpus remained untouched; authorized Second Brain disposable item was deleted and remained absent; no ONNX activation, RRF/DBSF change, Neo4j schema mutation, global Redis flush, or transcript upload occurred | Git path guard, browser create/reload/delete, and deployment archive inspection | Data + Privacy | Backup/restore proof and broader authenticated isolation remain open |
docs/production/PRODUCTION_READINESS.md:35:| Runtime artifacts | Reviewed OKF and doctrine lexicon artifacts are available | FAIL / GATED | Serving logs still warn that curated OKF/doctrine artifacts are absent; no empty placeholders were manufactured | Startup logs and image inspection | Data + Backend | Rebuild must use approved audited ingestion/ops flow and review gate |
docs/production/PRODUCTION_READINESS.md:53:The active desktop Docker backend passed **84 focused tests in 9.81 seconds** across provider-failure, distress-regex, crisis-preemption, and fail-closed paths. A metadata-only localhost probe returned the acute self-harm control as blocked `DISTRESS`; the benign Beautiful State control returned `QUERY` with `no_context_short_circuit` because the selected local `spiritual_wisdom` Qdrant collection has zero points. Local health returned HTTP 200 with `ready=true,status=degraded`, with optional OCR as the reported degraded component.
docs/production/PRODUCTION_READINESS.md:55:This evidence closes the identified **local safety-order regression**, but it does not close production readiness. The local stack remains non-equivalent to production because the selected Qdrant collection is empty, reviewed OKF/runtime artifacts are absent, and local provider configuration has previously produced authorization failures. The localhost probe therefore cannot validate production answers, citations, latency, or provider failover. No Railway deployment was started.
docs/production/PRODUCTION_READINESS.md:57:A broad `pytest -q` invocation in the active desktop container was not a valid full-suite result: collection stopped with **12 errors** because the stale backend-only mount did not include repository-root ingestion/ops modules and some newer tests, including `test_runtime_artifacts.py`. This is recorded as a test-harness/source-sync boundary, not a product regression. The valid dependency-complete local evidence remains the targeted Docker run of 103 passing tests; canonical sandbox static gates also passed.
docs/production/PRODUCTION_READINESS.md:70:The local retrieval-quality gate was executed in a dependency-complete Docker test image. It collected nine tests: **5 passed and 4 skipped**. The four integration strategies did not produce an NDCG result. A separate read-only Docker diagnostic confirmed that the configured `spiritual_wisdom` Qdrant endpoint is reachable and that the collection has **0 points**; the local data plane therefore cannot validate retrieval quality or prove a fix for the Serene Mind/Four Sacred Secrets false-refusal symptom. A metadata-only local flagship probe returned `QUERY`, `no_context_short_circuit`, zero citations, and a 38-character final for both questions. This is consistent with missing local evidence, not evidence that the production corpus or deployed refusal mitigation regressed.
docs/GUARDRAILS_DECISION.md:33:  crisis resource injection (not just blocking — active routing to support).
docs/GUARDRAILS_DECISION.md:52:3. **Distress path is not gateless**: The `DistressStage` actively routes users to crisis
docs/production/railway_stage_timing_health_browser_2026-08-22.md:3:The retrieval-stage instrumentation deployment `645965ed-3d81-41ae-9c76-61a624574f3c` reached `SUCCESS` and its active instance was `RUNNING`. A command-line curl check intermittently encountered TLS connection timeouts immediately after rollout. The connected browser then reached `https://askmukthiguru-8119b0e8-production.up.railway.app/api/health` and displayed JSON with `ready: true`, `status: "healthy"`, Qdrant/Redis/Neo4j/LLM/embedding all healthy, and embedding dimension `1024`. Internal Railway SSH health checks also returned healthy and `/api/healthz` returned alive. This establishes application readiness while preserving the observation that edge TLS propagation can be intermittent during/after Railway rollouts.
docs/COMPREHENSIVE_SYSTEM_SPEC.md:6:> **Note**: This document maps directly to the active codebase. All paths are formatted as clickable local absolute links.
docs/COMPREHENSIVE_SYSTEM_SPEC.md:119:| `/practices/:slug` | `PracticeDetailPage` | [PracticeDetailPage.tsx](file:///Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/src/pages/PracticeDetailPage.tsx) | Step-by-step interactive meditation builder |
docs/COMPREHENSIVE_SYSTEM_SPEC.md:165:- **[ChatMessage.tsx](file:///Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/src/components/chat/ChatMessage.tsx)**: ReactMarkdown-rendered bubbles incorporating interactive citations, expand/collapse video portals, and seeker feedback systems.
docs/COMPREHENSIVE_SYSTEM_SPEC.md:166:- **[ThinkingPills.tsx](file:///Users/harshodaikolluru/Public/askmukthiguru-8119b0e8/src/components/chat/ThinkingPills.tsx)**: Renders the active pipeline stage (Safety → Intent → Retrieving → Analyzing → Composing → Verifying) using glassmorphic UI cues mapped to custom backend status messages.
docs/COMPREHENSIVE_SYSTEM_SPEC.md:398:- **`cookies.txt` Configuration**: Loads active cookies into the request session.
docs/COMPREHENSIVE_SYSTEM_SPEC.md:668:24. **Provide Complete Mock Implementations**: Keep mock APIs in parity with active models to prevent testing failures.
docs/operations/incognito-isolation.md:11:Incognito does not disable crisis/safety policy, rate limiting, source governance, inference, or privacy-safe aggregate cost and reliability metrics. The active request is still sent to the configured inference provider when generation is required. User-facing wording must state these limits accurately.
docs/production/README.md:10:| Security and privacy | [`../INCIDENT_RESPONSE.md`](../INCIDENT_RESPONSE.md), [`../runbooks/PRIVACY.md`](../runbooks/PRIVACY.md), [`../SECURITY_CHECKLIST.md`](../SECURITY_CHECKLIST.md) | Security and privacy runbooks | Partially verified; live authenticated red-team evidence remains incomplete |
docs/transcript_end_to_end_recheck_report.md:10:The latest recheck confirms that the user’s corpus repairs are effective. All **745 video directories now pass the structural and integrity validator**. There are **zero issue packages**, **zero manifest artifact-hash mismatches**, **zero raw-source hash mismatches**, **zero timestamp-like transcript warnings**, and **zero correction-ledger issue packages**. The remaining 166 warnings are limited to the soft paragraph-length target and do not represent package corruption.
docs/transcript_end_to_end_recheck_report.md:12:The formerly incomplete directories are now represented with explicit repository-supported states: **45 `unavailable`** and **43 `dead_lettered`**, each with a complete package shape sufficient for downstream exclusion and auditability. The remaining materialized speech packages are `needs_review` or `sound_only`; no package is silently promoted to `trusted` without a review record.
docs/transcript_end_to_end_recheck_report.md:45:The only remaining validator warning is that **166 packages have most paragraphs outside the nominal 300–500-character target**. This is a retrieval-format warning, not an integrity defect. The corpus-wide paragraph median remains **396 characters**, indicating that the central distribution is aligned with the design goal; outliers can arise from final residual paragraphs, short recordings, or sentence-boundary preservation.
docs/COMPLETE_BACKEND_ARCHITECTURE.md:100:* **Safety:** Distress queries run through the complete RAG pipeline, including retrieval, verification, and all quality gates, to surface compassionate teachings alongside emergency guidance. Casual greetings and breathwork requests are handled by direct handlers.
docs/COMPLETE_BACKEND_ARCHITECTURE.md:133:| Qdrant         | qdrant:6333        | `spiritual_wisdom` (89,053 points, 1024d) | Vector Search  |
docs/COMPLETE_BACKEND_ARCHITECTURE.md:147:* Houses **89,053 dense vector chunks** (384-dimensional all-MiniLM-L6-v2 embeddings, configured via `services/embedding_service.py`).
docs/SECURITY_CHECKLIST.md:7:| 1 | API Security | HTTPS/TLS termination at edge (Nginx/Vercel) | TODO | `docker-compose.prod.yml` exposes 80/443 but no TLS cert config; edge CDN needed | HIGH |
docs/SECURITY_CHECKLIST.md:9:| 3 | API Security | CORS allowlist (no `*` in prod) | TODO | Verify `CORS_ORIGINS` env in prod is scoped to domain, not wildcard | HIGH |
docs/SECURITY_CHECKLIST.md:18:| 12 | Secrets Management | External secret manager in K8s prod | TODO | `values-production.yaml` notes ESO → AWS Secrets Manager/GCP SM; not wired | MEDIUM |
docs/SECURITY_CHECKLIST.md:23:| 17 | Dependency Scanning | Container image scanning (Trivy/Grype) | TODO | Add to CI pipeline; GHCR images not scanned currently | MEDIUM |
docs/SECURITY_CHECKLIST.md:24:| 18 | Dependency Scanning | SBOM generation | TODO | Add `syft`/`cyclonedx` to release pipeline | LOW |
docs/SECURITY_CHECKLIST.md:26:| 20 | Data Protection | Encryption at rest (Qdrant/Neo4j volumes) | TODO | Local volumes unencrypted; prod needs KMS-encrypted EBS/PV | MEDIUM |
docs/SECURITY_CHECKLIST.md:32:| 26 | Secrets | Secrets rotation policy | TODO | No rotation policy document. **Needed:** document rotation cadence for JWT_SECRET, SUPABASE_SERVICE_ROLE_KEY, API keys. Automate via ESO (item #12) | MEDIUM |
docs/operations/offline-readiness-evidence-2026-08-13.md:8:| Gate | Offline evidence completed | Status | Live evidence still required |
docs/operations/offline-readiness-evidence-2026-08-13.md:29:| Load harness prerequisites | `locust` was absent from the canonical development environment; it is now declared in `backend/requirements-dev.txt` and the nightly workflow installs that file |
docs/operations/offline-readiness-evidence-2026-08-13.md:45:Retain the four JSON reports, deployment SHA, active model policy, active corpus
docs/archive/RUTHLESS_REVIEW.md:47:- **Phantom config keys** (read via getattr, absent from `Settings`): `bm25_retrieval_enabled`, `citation_by_sentence`, `supabase_service_key`. **8/10.**
docs/archive/RUTHLESS_REVIEW.md:96:11. **Cut the follow-up-suggestions LLM call** ([generation.py:1560](backend/rag/nodes/generation.py)); let hot cache serve CASUAL ([cache_stage.py:68](backend/app/pipeline/stages/cache_stage.py)); make streaming the only interactive path.
docs/operations/documentation-governance-register.md:5:This register governs product-maintained Markdown. It applies to root guidance, application and backend instructions, architecture records, operating runbooks, release evidence, deployment material, evaluation guidance, and active product research. It does not treat vendored repositories, installed dependencies, copied skill libraries, generated reports, archived incident records, or raw research notes as living product documentation.
docs/operations/documentation-governance-register.md:18:| Evaluation and quality | `benchmarks/`, `backend/benchmarks/`, active evaluation docs, `docs/operations/release-evidence-pack.md` | What quality is measured and how release evidence is assembled. | Every model, prompt, corpus, metric, or evaluator change. | Quality owner |
docs/operations/documentation-governance-register.md:29:1. Run a maintained-document local-link scan before release; classify missing links as active defect, historical reference, or excluded material.
docs/operations/documentation-governance-register.md:30:2. Run a stale-reference scan after every removal or rename; update active instruction files, roadmap entries, and runbooks in the same change.
docs/operations/documentation-governance-register.md:32:4. Add a CI Markdown-link gate after the active document set has been explicitly tagged; do not fail on vendored or historical material until it has an owner and remediation plan.
docs/operations/documentation-governance-register.md:37:The maintained-document inventory contains root guidance, scoped agent instructions, active product/operations material, and historical records alongside substantial excluded vendor and research content. The first governance repair removed stale references to the retired `book-to-skill` integration from `CLAUDE.md` and `docs/DEVELOPER_GUIDE.md`, preserved the event in `docs/ROADMAP.md`, and reconciled the cleanup backlog. A broad local-link audit also identified many legacy absolute `file://` links and historical references; these are triaged as documentation-refresh work rather than silently rewritten, because many point to intentionally preserved historical context.
docs/operations/prod-readiness-remediation-2026-08-24.md:3:Follow-up to `docs/operations/release-evidence-pack.md` and the loop-engineering audit dated the same day. This session attempted to close every remaining P0/P1 blocker directly (not just re-verify them). Status below is per-item: **DONE**, **IN PROGRESS**, or **CANNOT CLOSE** (with the specific reason and what it needs).
docs/operations/prod-readiness-remediation-2026-08-24.md:15:- 4 concurrent HTTPS connections to a HuggingFace-CDN host stayed open mid-run even though every model (`bge-m3-onnx-int8`, `mmarco-mMiniLMv2-L12-H384-v1-onnx-cpu-qint8`, etc.) is fully cached locally (565MB+, no partial `.incomplete` files).
docs/operations/prod-readiness-remediation-2026-08-24.md:65:**Test verification**: `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 pytest -q` — **2374 passed, 5 failed, 29 skipped** (worktree, before merge); after merging into the main repo's `backend/.venv` and rerunning: **2379 passed, 3 failed, 26 skipped** (the 2 worktree-only failures were checkout-path artifacts of the worktree itself, not present on the main repo). All 3 remaining failures are the pre-existing NDCG/thin-corpus tests (item 3 above), unrelated to the dependency bump.
docs/operations/prod-readiness-remediation-2026-08-24.md:74:**Own mistake caught mid-session, worth naming**: twice while patch-bumping (`langchain>=1.3.9` and, earlier, in the OKF dry-run) a bare relative `.venv/bin/pip3`/`pytest` call landed against a *different*, unrelated `.venv` at the repo root instead of `backend/.venv` — this repo apparently has two venvs and cwd silently drifted between tool calls in this sandbox. Caught by checking the install output's own path (`.../askmukthiguru-8119b0e8/.venv/...` instead of `.../backend/.venv/...`), redone with an absolute path each time after. Flagging in case that root-level `.venv` is something you or another session actively rely on — it now also has `langchain>=1.3.9` installed as a side effect; low blast radius (additive install only, nothing uninstalled), but worth knowing.
docs/operations/prod-readiness-remediation-2026-08-24.md:88:The remaining P0s are blocked on inputs only you have: the real doctrine corpus, a decision on the realtime migration fix, and/or a disposable hosted Supabase project. None of them are closable by writing more code against what's currently in this sandbox. One more thing worth your attention: the Docker image (`backend/Dockerfile`/`Dockerfile.railway`) has NOT been rebuilt with these dependency changes — the currently-running container is still on the old pins. Rebuild before any of this reaches a deployed environment.
docs/STORE_LISTING.md:38:> - **Free forever** — AskMukthiGuru v1 is completely free with no in-app purchases.
docs/STORE_LISTING.md:100:> • **Free forever** — AskMukthiGuru v1 is completely free with no in-app purchases.
docs/STORE_LISTING.md:155:  6. **Knowledge graph** (`/knowledge-graph`) — interactive concept graph.
docs/STORE_LISTING.md:180:Until steps 1–4 are done, the Apple button will fail at OAuth init — complete them before submitting to App Store Connect.
docs/PRODUCT_OPPORTUNITIES.md:33:## 3. Incomplete / disabled / thin surfaces
docs/PRODUCT_OPPORTUNITIES.md:56:| OPP-3 | **Resurface teachings (SRS)** | The **spaced-repetition backend exists** (`api/srs.py`) but is barely in the UI. Proactive "revisit this teaching" nudges + a review surface would build a real retention loop that's already half-built. | ★★★ | Wire `api/srs.py` to a review card + reminder; `FlashcardPractice.tsx` may already be a start. | opus |
docs/PRODUCT_OPPORTUNITIES.md:152:| 39 | **LightRAG Qdrant-Direct Ingestion**: `scripts/ingest_lightrag_data.py` scrolling 89,053 `spiritual_wisdom` points directly into Qdrant & Neo4j via OpenRouter `gemma-3-12b-it` | `main` | Jul 22, 2026 |
docs/PRODUCT_OPPORTUNITIES.md:158:| 1 | Remaining 357-video ingestion run (Qdrant + Neo4j KG) | `main` | Deferred per user direction, June 2026 — 359 extracted, 75 ingested, 357 remaining; 29 permanently failed (DLQ) |
docs/PRODUCT_OPPORTUNITIES.md:176:| D10 | **Daily Wisdom Newsletter** | Medium | Scheduled email automation sending daily teaching excerpts. Requires content curation pipeline, email provider (Resend, SendGrid), unsubscribe management. Defer until ingestion complete and chat UX stable. Concept doc in `docs/marketing_strategy.md`. |
docs/PRODUCT_OPPORTUNITIES.md:268:- 3 pre-existing backend test failures depend on HF model revision `3a90cc8b42f5acec95e57c1e2433ba3b71ba9eef` which 404s — **partially resolved 2026-08-01**: tokenizer and all 7 registry models are now pinned to valid SHAs (`_MODEL_REVISIONS`); only the ONNX encoder (`gpahal/bge-m3-onnx-int8`) has no hardcoded revision (hash 404s) and relies on the Docker pre-baked snapshot + `HF_REVISION` env — the remaining failures likely still hit that path. Mock model loading in tests or resolve a valid encoder SHA.
docs/PRODUCT_OPPORTUNITIES.md:270:- 3 pre-existing env-dependent test failures (all files unmodified): `test_graphrag_fusion::test_self_test` (execs `backend` — fails unless run from repo root), `test_quality_gate::test_playlist_complete` (needs live Celery broker), `test_ruthless_phase2::test_feedback_lessons_mining` (needs live Qdrant). Fix: run pytest from repo root and start infra, or mark them `@pytest.mark.integration`.
docs/PRODUCT_OPPORTUNITIES.md:272:- Set `FORWARDED_ALLOW_IPS` (e.g. `10.0.0.0/8`) on the Railway service before the next deploy — `start_railway.py` exits at startup without it (Railway currently paused). Docker compose (plain uvicorn) is unaffected.
docs/PRODUCT_OPPORTUNITIES.md:274:- `test_quality_gate::test_playlist_complete` and `test_testauth_not_registered_in_prod` fail without live Redis + local Supabase — infra-dependent, pre-existing.
.claude/tasks/DATA_QUALITY_SCALING_PLAN.md:70:- yt-dlp `_resolve_video_metadata` removed from `youtube_loader.py` completely
.claude/tasks/DATA_QUALITY_SCALING_PLAN.md:103:**Why paused**: Full backfill would make ~18,000 LLM calls at ~2s each = ~10 hours. NIM 30 RPM rate limit is the bottleneck. Also, LLM-extracted titles are topical (from chunk text) not the original YouTube titles — acceptable but imperfect.
.claude/tasks/DATA_QUALITY_SCALING_PLAN.md:143:After backfill completes, re-run the audit to measure quality improvement.
docs/production/BROWSER_FINDINGS_2026-08-22.md:13:The chat route loaded and exposed: new conversation, Serene Mind Meditation, Practices, Notebooks, Wisdom Map, My Reflections, private conversation/incognito, healing-path controls, language selector, response preferences, assistant switcher, voice input, textarea composer, and notification controls. The visible healing path was `Quieting Anxiety`, with `15 min · 3 steps` and `0% complete`; a `Serene Mind now` action was visible. A daily-teaching opt-in prompt had `Not now` and `Enable` controls.
docs/production/BROWSER_FINDINGS_2026-08-22.md:21:A harmless query, `What is the meaning of stillness?`, was submitted from the public chat route. The interface displayed a searching/thinking state and a Stop control, then rendered the user message. At capture time, the answer was still in a `Searching Ekam...` / `Drawing from the teachings...` state after approximately 9 seconds; the page showed an existing prior `AI Generated` reflective-guidance answer and `0 sources` for the current sources panel. The UI did not display a completed answer for the current turn in the captured state.
docs/production/BROWSER_FINDINGS_2026-08-22.md:23:This is evidence of correct submission and visible progress controls, but it also confirms that the browser journey has a user-visible long tail and that the current turn’s final answer/citation state needs a later completed capture. No claim of full browser chat success is made from this capture alone.
docs/production/BROWSER_FINDINGS_2026-08-22.md:29:This confirms that the new backend response was not surfaced verbatim in the Lovable UI; the frontend displayed the older/shorter limited-support copy for this public session. The answer is clearly labelled and offers a practice handoff, but the stillness turn remains a quality and integration discrepancy requiring reconciliation between backend response payload and frontend rendered text. The completed chat interaction did not crash, and the visible controls for copy, regenerate, read aloud, save to memory, save as note, share card, feedback, and practice handoff were present.
docs/production/BROWSER_FINDINGS_2026-08-22.md:60:A fresh reload displayed the same reflection, `Disposable production verification — delete me`, dated `23/08/2026`, proving persistence across reload. Clicking its `Forget this` control initiated deletion; the item remained visible in the immediate capture, so a final reload/settled-state check is required before classifying deletion as complete.
docs/production/BROWSER_FINDINGS_2026-08-22.md:62:After the deletion settled, the vault showed the empty state again: `Nothing here yet — your reflections will appear as you use the app, or you can add one above.` The disposable item was absent. This completes the authorized create → reload → delete → settled-state check for the browser flow. It does not independently prove the BRAIN_KEK rotation or a separate backend-only vault-unlock test.
docs/production/BROWSER_FINDINGS_2026-08-22.md:66:After the replacement key was applied to both Mode-A rows, promoted to `BRAIN_KEK` in backend and worker scopes, both services were redeployed successfully, and `BRAIN_KEK_NEXT` was deleted from both scopes, the connected browser reloaded `/second-brain`. The page passed through `Loading your reflections` and settled on the encrypted `My Reflections` empty state. The disposable reflection remained absent. This is browser evidence that the authenticated vault view still unlocks after cutover; it does not expose key material.
docs/production/BROWSER_FINDINGS_2026-08-22.md:68:After the additional final redeploy performed once `BRAIN_KEK_NEXT` had been deleted from both scopes, the same route again passed through `Loading your reflections` and settled on the empty encrypted-vault state with the disposable item absent. This is the final browser confirmation after the process environment was refreshed.
docs/production/REMEDIATION_LOG_2026-08-23.md:58:## Still open — not falsely marked complete
docs/archive/PLAN_AUDIT.md:54:  skip/Escape route to `onDismiss` (does not mark completed), only "Got it" sets
docs/archive/PLAN_AUDIT.md:55:  `askmukthiguru_tour_completed`. Verified in source.
.claude/tasks/auth_domain_auto_signout.md:27:   - Keep a `lastUserRef` to record if a user session was ever active.
.claude/tasks/auth_domain_auto_signout.md:57:- Added an `initialCheckDone` flag inside the effect hook to ignore initial `SIGNED_OUT` auth events before the initial `getSession()` check completes, resolving the page-mount redirect race condition.
.claude/tasks/auth_domain_auto_signout.md:63:- Configured a `lastUserRef` to record the last-seen active user.
docs/production/DOCKER_COST_AUDIT_2026-08-23.md:17:| AskMukthiGuru containers | 6 running | All had `restart: unless-stopped`; they remain online without active users unless manually stopped |
docs/production/DOCKER_COST_AUDIT_2026-08-23.md:26:| Docker images | 28 total, 23 active, 61.49 GB total; 11.79 GB reclaimable | Disk/build footprint, not the same as runtime memory billing |
docs/production/DOCKER_COST_AUDIT_2026-08-23.md:28:| Docker volumes | 13 total, 11 active, 2.605 GB | Persistent data; no volumes were deleted |
docs/production/DOCKER_COST_AUDIT_2026-08-23.md:30:The backend log sample contained **176 `/api/health` requests in the last 30 minutes**, all of the observed backend request paths in that interval. This is consistent with an active health-check or monitoring loop and is not evidence of end-user traffic. The worker log sample contained no matched task/heartbeat/error lines in the inspected 30-minute tail. After the worker was manually stopped, Docker reported exit code `137` with `OOMKilled=false`; this audit does not classify that stop as an OOM event.
docs/production/DOCKER_COST_AUDIT_2026-08-23.md:46:The safety-order fix was validated inside the active Docker backend with **84 passed tests in 9.81 seconds** across provider-failure, distress-regex, crisis-preemption, and fail-closed paths. A metadata-only stream probe returned the acute self-harm control as blocked `DISTRESS` and returned the benign Beautiful State control as `QUERY` with `no_context_short_circuit`. The latter is explained by the selected local Qdrant `spiritual_wisdom` collection having zero points; no answer text was captured or inferred.
.claude/tasks/fix_vulnerabilities.md:29:   - Verified zero High / Critical vulnerabilities remaining on target packages.
docs/archive/USER_ACTIONS.md:71:### 7. Translate remaining locales (bn, gu, ml, ur, or, pa, as, sa)
docs/archive/USER_ACTIONS.md:75:**To complete a locale** (example: Bengali):
.claude/tasks/regional-locale-translations.md:13:- Created `scripts/apply_offline_translations_kn.py` to fix all remaining Kannada keys and corruptions.
.claude/tasks/cleanup_unused_markdown.md:8:- [x] Perform deletions of the approved list of files using `rm -f` (non-interactive to avoid hanging)
.claude/tasks/cleanup_unused_markdown.md:16:- Kept 21 core/active docs.
.claude/tasks/product-storytelling-enterprise-readiness.md:29:6. Update the applicable product, developer, README, and lessons documentation with decisions, verification results, and remaining evidence gaps. Provide a final delivery report that distinguishes completed changes from deferred work.
.claude/tasks/product-storytelling-enterprise-readiness.md:46:| P0 | A visible “See it in action (90 sec)” surface is nonfunctional. | A dead proof interaction breaks confidence precisely when a visitor seeks product evidence. | Replace it with a real pathway into the interactive tour and an explicit action-oriented label. |
.claude/tasks/product-storytelling-enterprise-readiness.md:66:| Product explainer | Converted the inert “See it in action” placeholder into a keyboard-accessible entry point to the existing three-step interactive tour. | Gives visitors working proof before commitment. |
.claude/tasks/product-storytelling-enterprise-readiness.md:76:| TypeScript | Passed | `npm run typecheck` completed with no diagnostics. |
.claude/tasks/product-storytelling-enterprise-readiness.md:77:| Changed-file lint | Passed | ESLint completed cleanly for every changed TypeScript/TSX source file. |
.claude/tasks/product-storytelling-enterprise-readiness.md:79:| Frontend build and prerender | Passed | `npm run build` completed and prerendered 28 routes, including public, chat, practice, privacy, terms, and product-support surfaces. |
.claude/tasks/product-storytelling-enterprise-readiness.md:83:| Diff hygiene | Passed | `git diff --check` completed with no whitespace errors. Existing user-owned changes to `backend/requirements.txt` and `memory/qdrant_quality_baseline.json` were preserved. |
.claude/tasks/cost_spike_investigation_aug2026.md:27:- Report the confirmed historical source, accounting gap, production status, and remaining provider-dashboard limitation.
.claude/tasks/knowledge_graph_redesign.md:9:> 1. **Removing Ontology View**: The Teachings Ontology toggle will be removed completely. The screen will focus 100% on the user's personal Consciousness Map.
.claude/tasks/recording_user_demo.md:25:   - Wait for the streamed response to complete and display citations/teaching sources.
.claude/tasks/recording_user_demo.md:49:- A minimalist web search page (dark mode, modern search input box) with an active input field that the browser subagent can type into.
.claude/tasks/mobile-app-launch.md:64:    active boolean default true
.claude/tasks/mobile-app-launch.md:95:Sequential 1→10. Each task: implementer subagent → spec reviewer → code quality reviewer → mark complete.
.claude/tasks/2026-08-16-mobile-polish-language-icon-profile-redesign.md:9:3. **Strict 44px Touch Targets**: Every button, pill, tab trigger, and close affordance has minimum 44x44px interactive bounds across mobile and desktop.
.claude/tasks/2026-08-16-mobile-polish-language-icon-profile-redesign.md:19:  - Drop `LANGUAGE_FLAGS` map completely.
.claude/tasks/2026-08-16-mobile-polish-language-icon-profile-redesign.md:22:  - Dropdown rows: 48px height (`min-h-[48px]`), `Globe` glyph tile in rounded container, native name in primary font, English name muted, clean active indicator dot.
.claude/tasks/2026-08-16-mobile-polish-language-icon-profile-redesign.md:34:  - **Quiet Tabs**: Replace heavy gradient pills (`bg-gradient-to-r from-ojas to-ojas-light shadow-md`) with editorial tab styling (`data-[state=active]:bg-card data-[state=active]:text-foreground data-[state=active]:shadow-sm`).
.claude/tasks/ruthless-end-to-end-product-transformation.md:16:| Evidence before claims | No code, test, or documentation change will be called complete without command output, static verification, or a browser-level check that supports the claim. |
.claude/tasks/ruthless-end-to-end-product-transformation.md:29:| 7 | Landing-page conversion and demo | Improve the landing journey and interactive product demo. Prepare a production-grade 60–90 second demo-video screenplay, storyboard, narration, assets list, and embedding plan; generate the video only after access is upgraded. |
.claude/tasks/ruthless-end-to-end-product-transformation.md:30:| 8 | Operations and maintainability | Harden CI/CD, observability, failure diagnosis, dependency governance, documentation, project TODOs, architectural decision records, and a severity-ranked issue register. |
.claude/tasks/ruthless-end-to-end-product-transformation.md:46:### Phase 5 — Documentation and durable TODO register
.claude/tasks/ruthless-end-to-end-product-transformation.md:47:Update architecture documentation, the issue register, operations runbooks, quality-gate guidance, response-style standards, corpus rights rules, and release checklist. Record every remaining issue in a severity-ranked table with owner surface, evidence, recommended action, validation path, and clear unblocker. No known concern may remain solely in conversation history.
.claude/tasks/ruthless-end-to-end-product-transformation.md:53:The programme is complete only when implemented changes have recorded evidence; core user journeys are checked; critical or high-severity known issues have either been repaired or surfaced in the durable register; voice and first-person responses are grounded by verifiable provenance; UI improvements are accessible and responsive; operational risks have owners and runbooks; and the demo-video production package is ready to generate and embed after access is enabled.
.claude/tasks/COMPREHENSIVE_FIX_PLAN.md:49:- 5s heartbeat adds up to 5s tail latency when pipeline completes between heartbeats
.claude/tasks/COMPREHENSIVE_FIX_PLAN.md:102:| `proactive_*` thresholds | L | M | N | Y | L | L | M | N | **KEEP** (Serene Mind uses) |
.claude/tasks/COMPREHENSIVE_FIX_PLAN.md:125:- [ ] Group remaining settings by domain (pipeline, rag, telemetry, ingestion, auth)
.claude/tasks/COMPREHENSIVE_FIX_PLAN.md:126:- [ ] Add docstrings for every remaining setting explaining purpose, default, and which component uses it
.claude/tasks/COMPREHENSIVE_FIX_PLAN.md:296:- [ ] Ensure pills don't show after response is complete
.claude/tasks/complete_comprehensive_report_implementation.md:3:This plan outlines the steps to verify, complete, and benchmark the implementation of **Option A** (Surgical Patches) and **Option B** (Full v2 Redesign) from the comprehensive report in the active workspace.
docs/production/railway_retrieval_fix_evidence_2026-08-22.md:32:## Dependency-complete regression result
docs/production/railway_retrieval_fix_evidence_2026-08-22.md:65:The application remains **NOT READY FOR BROAD PRODUCTION**. Open gates include Railway cost/memory headroom, absent reviewed OKF and doctrine-lexicon runtime artifacts, reranker packaging, sustained latency-tail attribution, held-out retrieval/NDCG/faithfulness coverage, Lovable-hosted SSE-final publication and browser proof, authenticated telemetry and Second Brain journeys, custom DNS, and a non-destructive backup/restore drill. ONNX INT8, RRF/DBSF, Neo4j schema mutation, and broad graph parallelization remain evidence-gated and were not activated.
docs/production/railway_retrieval_fix_evidence_2026-08-22.md:93:A repository audit found zero reviewed `memory/okf/*.md` entries in the deployable checkout; only the two OKF maintenance scripts are present. Therefore the approved compiler has no reviewed source bundle from which to produce `memory/okf/compiled.json`. The doctrine lexicon builder similarly requires reviewed authority and corpus inputs that are not available as a packaged runtime artifact. Generating an empty, synthetic, or guessed artifact would hide a data-integrity problem, so no artifact was created. The correct next step is the approved audited reingestion/package flow, with the immutable corpus directory remaining untouched.
docs/production/railway_retrieval_fix_evidence_2026-08-22.md:100:The rotation utility dry-run inside the healthy Railway backend found 2 Mode-A wrapped DEK rows and verified replacement unwrap integrity without writing rows. The authorized apply updated exactly 2 rows using compare-and-swap predicates. The replacement was promoted to `BRAIN_KEK` in both backend and worker scopes, both services were redeployed, `BRAIN_KEK_NEXT` was deleted from both scopes, and both services were explicitly redeployed again to clear the staged variable from running process environments. A name-only audit confirmed `BRAIN_KEK` present and `BRAIN_KEK_NEXT` absent for both services. No secret value was printed, committed, or placed in the evidence pack.
docs/production/railway_retrieval_fix_evidence_2026-08-22.md:113:| Four Sacred Secrets | HTTP 200; `grounded_partial_evidence`; `partial=true`; public `grounding_state=grounded`; `hallucination_flag=false`; 2 exact YouTube URLs; `1,139` chars | Bare refusal mitigated, but canonical curated runtime artifacts remain absent |
docs/production/railway_retrieval_fix_evidence_2026-08-22.md:128:A clean Docker build from the pushed source completed successfully. The corrected isolated run used the dependency image `askmukthiguru-test:584ff7a`, mounted a disposable writable copy of the tracked `memory/` tree, and passed **169 passed, 2 skipped in 38.58 seconds** across the focused routing, grounding, generation, cache, streaming, SSE replay, artifact, and quality tests. Earlier Docker failures were attributed to the test harness: a backend-only mount hid `/app/memory`, a read-only memory mount blocked intentionally disposable OKF fixtures, and one test patched the wrong settings import. The settings patch was corrected in test code commit `6fc7013`; it was not deployed.
.claude/tasks/transcript-md-quality.md:3:## 1. Research & Baseline (complete)
.claude/tasks/transcript-md-quality.md:8:## 2. Quality Improvements per Research (complete)
.claude/tasks/transcript-md-quality.md:29:## 3. Teacher Routing (complete)
.claude/tasks/transcript-md-quality.md:37:## 4. Verification (complete)
.claude/tasks/GURU_BRAIN_TONE_ALIGNMENT.md:69:- [ ] Initialize Qdrant collection `guru_tone_podcast` (vector dimension matching active embedding model, e.g. 1536/384).
.claude/tasks/GURU_BRAIN_TONE_ALIGNMENT.md:134:- Test interactive chat responses on mobile/web against baseline factual responses.
.claude/tasks/ruthless_production_audit_plan.md:4:Perform an exhaustive, empirical, end-to-end verification and execution across all 9 audit phases for the production LightRAG deployment hosted on Railway. Verify live infrastructure status, audit code for Qdrant API key completeness and Constitutional RAG integration, harden Qdrant API key handling, secure Neo4j Bolt access, restart and monitor LightRAG ingestion, benchmark performance, and update `lessons.md`.
.claude/tasks/ruthless_production_audit_plan.md:25:- Scan for TODO/FIXME/dead code/commented blocks/unused env vars across `backend/`.
.claude/tasks/ruthless_production_audit_plan.md:46:- Compare processed points (checkpoint) vs total points (89,053).
.claude/tasks/ruthless_production_audit_plan.md:69:- Verify active ingestion progress in `data/lightrag_checkpoint.json` and logs.
.claude/tasks/fix-merge-conflicts.md:18:- Resolve conflict blocks by unifying under a premium interactive animated layout.
.claude/tasks/fix_audit_issues.md:5:- [x] Phase 2: `generation.py` (`extractive_compress_doc` suffix reservation and small `max_chars` handling).
.claude/tasks/fix_audit_issues.md:9:- [x] Phase 6: `test_extractive_compression.py` (Assert output `<= 500`; added `test_extractive_compress_short_document_truncation`).
.claude/tasks/fix_audit_issues.md:13:- [2026-07-21] Reserved suffix `" [...]"` length in `extractive_compress_doc` to guarantee output `<= max_chars`.
.claude/tasks/fix_audit_issues.md:17:- [2026-07-21] Updated `test_extractive_compression.py` assertions and added short document truncation test.
.claude/tasks/complete_goals_and_pending_cases.md:3:This plan covers the remaining tasks in `docs/PENDING_AND_EDGE_CASES.md` and `.claude/GOAL.md`.
.claude/tasks/master-implementation-plan.md:150:token_budget_remaining: int            # tokens left before hitting context window
.claude/tasks/master-implementation-plan.md:674:3. **Integration**: All services healthy → verify normal pipeline completes (no regression)
.claude/tasks/master-implementation-plan.md:1104:                remaining = total_budget - tokens_used
.claude/tasks/master-implementation-plan.md:1105:                if remaining > 20:  # at least 20 tokens to be worth including
.claude/tasks/master-implementation-plan.md:1106:                    truncated = self._truncate_text(chunk_text, remaining)
.claude/tasks/master-implementation-plan.md:1108:                    tokens_used += remaining
.claude/tasks/master-implementation-plan.md:1136:        compressed_context, remaining_budget = budget_manager.compress(
.claude/tasks/master-implementation-plan.md:1142:        state["token_budget_remaining"] = remaining_budget
.claude/tasks/master-implementation-plan.md:1296:After each phase completes, run the following verification commands:
.claude/tasks/master-implementation-plan.md:1394:4. Update `docs/ROADMAP.md` — mark completed items
.claude/tasks/ragflow_integration.md:411:**Total remaining: ~11h**
docs/archive/PROD_READINESS.md:9:2. **Admin auto-grant** — `kharshaengineer@gmail.com` will receive `admin` role automatically on first Google sign-in (verified email required). Retroactive grant applied if already signed up.
docs/archive/PROD_READINESS.md:10:3. **Google OAuth** — managed provider active. AuthPage's `supabase.auth.signInWithOAuth({ provider: 'google' })` uses Lovable's managed Google client. No Google Cloud Console setup needed.
.claude/tasks/sequential_guardrails.md:3:Ensure input guardrails execute sequentially before distress detection, fix rate limit response, prevent tab closure when Serene Mind is active and gated, and implement the distress user journey (teachings first, Serene Mind on persistent distress).
.claude/tasks/sequential_guardrails.md:13:- Persistent distress will continue to trigger Serene Mind proactively via `_maybe_trigger_proactive_serene_mind` based on user history.
.claude/tasks/sequential_guardrails.md:24:- Add a `useEffect` hook to prevent tab closure/reload (via `beforeunload`) and Escape key closure when the modal is open, gated (`isGated` is true), and not in the `complete` phase.
.claude/tasks/2026-07-28-okf-v02-full-scope.md:10:| 2 | Verified | LLM → `machine:validator`, Admin approve → `human:curator`, existing → `human:curator` retroactive |
.claude/tasks/2026-07-28-okf-v02-full-scope.md:47:### Task E: Retroactive Verified Tagging
.claude/tasks/ruthless-complete-delivery-plan.md:5:Complete the remaining AskMukthiGuru product, privacy, retrieval, operations, and Android/iOS work using evidence-based completion states. A task is complete only when it is implemented and verified, staging-proven, production-proven, explicitly deferred with evidence, or recorded as an external blocker with an owner, command, evidence artifact, and re-entry condition.
.claude/tasks/ruthless-complete-delivery-plan.md:26:- Every increment requires focused tests, full applicable suites, static/build checks, visual review where relevant, `git diff --check`, TODO/lesson updates, and an auditable commit.
.claude/tasks/MALAYALAM_LOCALIZATION.md:12:   - Construct complete, authentic Malayalam translations adhering to EKAM spiritual context.
.claude/tasks/MALAYALAM_LOCALIZATION.md:31:  - [x] Implement complete Malayalam translation for `profile` (59 keys).
.claude/tasks/MALAYALAM_LOCALIZATION.md:32:  - [x] Implement complete Malayalam translation for `chat` (261 keys) including `starterCards`, `proactivePrelude`, `engagement`, `slashCommands`, `prePractice`.
.claude/tasks/MALAYALAM_LOCALIZATION.md:33:  - [x] Implement complete Malayalam translation for `practices` (40 keys), `meditation` (99 keys), `brain` (35 keys), `cancelFlow` (49 keys).
.claude/tasks/railway-pro-best-practices.md:448:  1. Check https://status.railway.com and Railway Station for active incidents
.claude/tasks/admin_verification.md:3:We are addressing all remaining items in `handoff.md` and deferred items in `lessons.md`:
.claude/tasks/audit-fixes-2026-08-19.md:19:- If a requested fix requires a risky migration or unavailable production credential, implement the safe code/documentation contract and report the remaining deployment step.
.claude/tasks/TASK_ADMIN_PAGES_RESILIENCY.md:92:  - Clear error and empty states for the interactive graph.
.claude/tasks/TASK_ADMIN_PAGES_RESILIENCY.md:111:3. Write unit tests for edge-case payloads (null arrays, missing properties, backend 500 errors) to ensure complete resiliency.
.claude/tasks/TASK_ADMIN_PAGES_RESILIENCY.md:145:   - Added interactive error banner with retry capability.
.claude/tasks/feature-wave.md:3:**Status:** IN PROGRESS — Task 1 complete
.claude/tasks/feature-wave.md:167:### Task 3 — Frontend i18n: complete en/hi/te locale parity + Practices colloquial copy
.claude/tasks/feature-wave.md:211:    simultaneously. Currently `activeTab === 'breathing'` gates the breath phase loop
.claude/tasks/feature-wave.md:212:    (`useEffect` at line ~89). Change to gate on `activeTab === 'audio'` — breathing animation
.claude/tasks/feature-wave.md:213:    runs whenever audio tab is active.
.claude/tasks/feature-wave.md:215:  - The YouTube hidden iframe at line ~803 (audio helper) plays only when audio tab is active.
.claude/tasks/feature-wave.md:216:  - Session + streak tracking (`startMeditationSession` / `completeMeditationSession`) complete
.claude/tasks/feature-wave.md:222:  rendered, assert breathing animation runs when Audio tab active.
.claude/tasks/feature-wave.md:241:    already wired via `proactive_serene_mind` in `PipelineResult`).
.claude/tasks/feature-wave.md:394:  language selector. Clicking it sets `localStorage askmukthiguru_tour_completed = false` and
.claude/tasks/feature-wave.md:435:code-quality reviewer → fix loop → mark complete → next task. Never two implementation
.claude/tasks/feature-wave.md:455:- All 10 tasks marked complete.
.claude/tasks/audit_hardening.md:7:- [x] Phase 2.2: Extractive Cross-Encoder compression in `generation.py`.
docs/archive/MULTI_GURU_ONBOARDING.md:32:├── mukthi-guru/                     # active default
docs/archive/MULTI_GURU_ONBOARDING.md:142:### Step 4: Set the active guru via env
.claude/tasks/OLLAMA_QUOTA_CHAT_UI_PLAN.md:7:- Any code path that instantiates `OllamaService` directly instead of the active provider.
.claude/tasks/OLLAMA_QUOTA_CHAT_UI_PLAN.md:38:  - Add an explicit **NIM fast path** for extraction/keyword/summary when OpenRouter key is absent, using `settings.nim_classify_model` with structured output.
.claude/tasks/OLLAMA_QUOTA_CHAT_UI_PLAN.md:42:### 1.3 API-key validation aligned with active provider
.claude/tasks/ontology_and_cache_plan.md:3:This plan details the implementation of the remaining items from the **Mukthi Guru — Ultra-Ruthless Foundation & Scalability Audit (V2)**, specifically focusing on Phase 2 (Ontology & Context Graph Integration) and Phase 3 (Infrastructure, Caching, and Production readiness).
docs/archive/ruthless_plan.md:16:| Section: State Check-In (interactive mood → route to chat) | **Missing** | Highest-impact net-new landing feature per the plan. |
docs/archive/ruthless_plan.md:68:9. **Landing copy pass** — replace remaining generic microcopy with plan-approved phrasing
.claude/tasks/wire_anthropic_gateway_and_thresholds.md:3:This plan outlines the integration of `AnthropicGateway` for streaming and non-streaming generation with prompt caching and Citations API support, the addition of `--use-batch` mode to `eval_runner.py` leveraging the Anthropic Message Batches API, and the migration of the remaining P1 magic threshold numbers to Pydantic Settings in `backend/app/config.py`.
.claude/tasks/full-conversation-reverification.md:4:Reconcile every material requirement from the prior audit conversation against the active main branch, remediate only verified P0/P1 gaps, append durable learnings to `lessons.md`, and retain non-production evidence.
.claude/tasks/full-conversation-reverification.md:10:- [~] Implement bounded P0 fixes found in active code, with focused tests and safe flags.
.claude/tasks/full-conversation-reverification.md:13:- [ ] Append non-duplicative verified controls and remaining launch gates to lessons.md.
.claude/tasks/ground-up-audit-and-top-notch-upgrade.md:4:Conduct a non-destructive, evidence-based audit of the complete Ask Mukthi Guru repository, research proven improvements, implement the highest-value low-risk corrections, and verify all changed behaviour. The work will preserve existing security invariants, never expose credentials, and avoid deployment or user-data mutations without explicit confirmation.
.claude/tasks/ground-up-audit-and-top-notch-upgrade.md:46:3. Deliver an implementation summary, prioritized remaining backlog, validation log, external research sources, and the demo-video production brief. Do not deploy, commit, publish, post, or change production settings without explicit permission.
.claude/tasks/ground-up-audit-and-top-notch-upgrade.md:52:The task is complete when the repository has an evidence-backed audit record, high-confidence safe improvements are implemented with regression coverage, relevant checks have been run and documented, unresolved risks are prioritized, and the landing-page video plan is ready for execution pending video-generation access.
.claude/tasks/ground-up-audit-and-top-notch-upgrade.md:62:| Backend tests | `pytest tests/ -q --tb=short --disable-warnings` completed in 3m49s: 1,588 passed, 32 skipped, 2 failed. The expired-token test sends an unsigned session id that security hardening now rejects; playlist completion dispatches a real Celery task in a unit test, then waits for absent local Redis. | Amend tests to exercise the intended signed anonymous-session flow and mock the post-ingestion Celery dispatch. |
.claude/tasks/fix-security-and-quality-issues.md:70:After all groups complete, run:
.claude/tasks/queue-enhancements.md:172:    """List all active (non-expired) jobs. Returns most recent first."""
.claude/tasks/queue-enhancements.md:178:    # Scan for all job meta keys (includes processing, completed, failed within TTL)
.claude/tasks/queue-enhancements.md:219:    """List all active/queued jobs for admin monitor."""
.claude/tasks/queue-enhancements.md:248:    status: 'queued' | 'processing' | 'completed' | 'failed' | 'cancelled';
.claude/tasks/queue-enhancements.md:284:    completed: 'bg-green-500/10 text-green-600',
.claude/tasks/queue-enhancements.md:303:    const counts = { queued: 0, processing: 0, completed: 0, failed: 0 };
.claude/tasks/queue-enhancements.md:322:                            <CardContent><p className="text-2xl font-bold">{counts.completed}</p></CardContent></Card>
.claude/tasks/end_to_end_transcript_review.md:11:The review will cover all discoverable package directories, including incomplete or dead-lettered entries, and will explicitly distinguish missing artifacts, malformed artifacts, integrity failures, low-quality speech, and semantically questionable but structurally valid text.
.claude/tasks/end_to_end_transcript_review.md:77:The exact number of packages, availability of original YouTube media, and completeness of raw sources will be discovered during inventory. External research may be unavailable for private, removed, age-restricted, or ambiguous videos; those cases will be recorded with bounded confidence rather than hidden. Research agents may disagree on transliteration or doctrine terminology, so the report will separate phonetic evidence, editorial normalization, and doctrinal interpretation. Parallel processing improves throughput but does not eliminate the need for deterministic gates, checkpointing, and adjudication.
.claude/tasks/kg-fixes-2026-07-28.md:18:   - Fix: pass `baseline_collection` into `run_benchmark()`; use it instead of `BASELINE_COLLECTION` when constructing `baseline_manager`; apply `args.qdrant_url` to `settings.qdrant_url` before creating `baseline_manager`. Defaults preserved when flags absent.
.claude/tasks/kg-fixes-2026-07-28.md:28:4. **`backend/domain/spiritual_ontology.py:105-113` — active teaching aliases need direction metadata**
.claude/tasks/kg-fixes-2026-07-28.md:29:   - Requirement: active aliases (teaches/teach/guides/expounds) must produce direction metadata and swap endpoints before materialization; passive `IS_TAUGHT_BY` stays unchanged.
.claude/tasks/kg-fixes-2026-07-28.md:30:   - Current `RELATION_TYPE_ALIASES` maps all to `RelationType.IS_TAUGHT_BY` with no direction metadata. Implementation strategy: introduce `_RELATION_DIRECTION_OVERRIDES` / helper so active aliases invert `(subject, object)` during materialization, while canonical `IS_TAUGHT_BY` does not. Update `resolve_relation_type` callers in `triple_extractor.py` and `ontology_writer.py` to consume the new metadata.
.claude/tasks/kg-fixes-2026-07-28.md:95:    - Export endpoint currently manually calls `auth_bridge.get_user`. Fix: use `get_current_user_from_supabase` dependency; preserve anonymous access only when credentials absent; propagate auth errors instead of fallback to `user_id = None`.
.claude/tasks/queue_backpressure.md:63:  - Returns `{status, result, error, created_at, completed_at, ...}`
.claude/tasks/queue_backpressure.md:65:- `GET /api/jobs` — list active jobs (future: pagination)
.claude/tasks/queue_backpressure.md:96:    Worker->>JobQueue: update status → completed/failed
.claude/tasks/queue_backpressure.md:100:        API-->>Client: 200 {status: completed, result: {...}}
.claude/tasks/queue_backpressure.md:109:- `job:{job_id}:status` → string: `queued|processing|completed|failed|cancelled`
.claude/tasks/language_support_completion.md:4:Audit and complete 100% key parity and natural Indic translations across all 14 supported languages in AskMukthiGuru (`en`, `hi`, `te`, `kn`, `ta`, `mr`, `bn`, `gu`, `ml`, `as`, `sa`, `or`, `pa`, `ur`).
.claude/tasks/language_support_completion.md:10:  - `hi` (Hindi): 1,077 / 1,077 keys (100% complete)
.claude/tasks/language_support_completion.md:34:   - Add the full `profile` hierarchy (settings, privacy, tone, familiarity, theme, memory management, statistics) to all 8 remaining locale files.
.claude/tasks/openrouter_anthropic_cache.md:26:- Note: Network command approvals timed out during execution because the user is currently away. The commit is recorded locally, and the parent agent can run `git push` once active.
.claude/tasks/ruthless_review_p0_p1.md:23:## Verification gates (every task must pass before "complete")
.claude/tasks/ruthless_review_p0_p1.md:29:5. Code-quality review subagent → must pass before marking task complete.
.claude/tasks/ruthless_review_p0_p1.md:84:2. Reorder: move the `_BLOCKED_TOPICS` loop (currently 305–317) to run **before** the `_HARMFUL_PATTERNS` loop. This ensures `self_harm` (with helpline redirect) fires before any remaining harmful-pattern block.
.claude/tasks/ruthless_review_p0_p1.md:181:5. Add a test: `TestAuthStrategy` is NOT registered when `ENABLE_TEST_AUTH` is false/absent, even if `X-Test-Key` is sent (401). And IS registered when true + secret matches.
.claude/tasks/ruthless_review_p0_p1.md:283:## Task P1-11: Cut the follow-up-suggestions LLM call; hot cache for CASUAL; streaming as the only interactive path
.claude/tasks/ruthless_review_p0_p1.md:288:3. Make streaming the only interactive path (document/assume; don't rip out non-stream if benchmarks need it).
.claude/tasks/ruthless_review_p0_p1.md:293:1. Locate the follow-up-suggestions LLM call (grep `follow.up` / `suggestions` in `generation.py`). Remove the LLM call. If follow-up suggestions are computed, make them a cheap deterministic extraction (top keywords from retrieved docs) OR drop them for now (confirm with controller if unsure — default: drop, add a TODO).
.claude/tasks/ruthless_review_p0_p1.md:295:3. Do NOT remove the non-stream `sendMessage` path (benchmarks/title use it) — but add a doc comment that streaming is the interactive product path.
.claude/tasks/clickable-citations.md:3:We will modify `backend/rag/nodes/generation.py` to convert string-based source citations like `[Source: Title]` to index-based citations `[N]` (interactive index buttons in the UI). This ensures uniform citation buttons.
.claude/tasks/clickable-citations.md:71:`feat(caching): convert string-based source citations to interactive index buttons`
.claude/tasks/production-hardening-reconciliation.md:4:Re-read the inherited task requirements, repository operating rules, audit and release records, and implemented commit `4e740765`. Reconcile every requested capability with its code path, configuration gate, tests, deployment evidence, and remaining blocker. Implement any verified omission only after its surrounding contract is understood.
.claude/tasks/production-hardening-reconciliation.md:10:4. Implement and test any material gap discovered by reconciliation; do not switch off safe-default feature gates merely to make a feature appear complete.
.claude/tasks/production-hardening-reconciliation.md:11:5. Run affected tests, complete backend/frontend regression, build, syntax, diff, and release-evidence checks.
.claude/tasks/production-hardening-reconciliation.md:12:6. Update the reconciliation record, backlog, and release runbook; commit only verified changes and deliver an exact remaining-evidence list.
.claude/tasks/production-hardening-reconciliation.md:15:The user explicitly requested that the complete inherited task and relevant files be re-read and all applicable requirements be implemented. This serves as approval to execute the reconciliation plan.
.claude/tasks/production_readiness_plan.md:4:Implement the production-ready fixes from the AskMukthiGuru Production Audit to address 0% cache hit rate, high latencies, prompt contradictions, ingestion duplication, fixed-window chunking, and simulated SSE streaming.
.claude/tasks/production_readiness_plan.md:30:- In `_maybe_trigger_proactive_serene_mind`, ensure that `{"triggered": False}` is returned (never `None`) in all skipped/disabled/cooldown code paths.
.claude/tasks/production_readiness_plan.md:63:  - Proactive serene mind is never `None`/`null`.
.claude/tasks/locale_translations_plan.md:3:We need to complete the translation of the application locale files (`hi.json`, `te.json`, `kn.json`, `ta.json`, `mr.json`) from baseline English (`en.json`).
.claude/tasks/locale_translations_plan.md:17:> The translation script needs to access the network to call the **Sarvam Translate API** (using the active `SARVAM_API_KEY` from `.env`). This will require executing the script with `BypassSandbox: true` (which triggers a prompt for user approval).
.claude/tasks/fix_knowledge_graph.md:12:  - [ ] Provide an interactive text input to allow users to add their first reflection in the empty state.
.claude/tasks/production_benchmark_20260820.md:10:2. Add a production-safe benchmark runner that obtains anonymous session tokens, captures complete JSON responses, and records latency, status, intent, tier, citations, faithfulness, verification, trace, node timings, cache hit, grounding, and answer length.
.claude/tasks/ruthless_audit_and_cleanup.md:3:We will complete a thorough ruthless review of the codebase, finalize the config cleanup, document data quality, restart the container, perform screenshots/latency analysis, and provide the hosted Supabase SQL migration script.
.claude/tasks/fix-youtube-railway-plan.md:258:- Each backend hits a completely different YouTube surface with independent rate-limit heuristics
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:11:> **STATUS (2026-08-10, wave 8 complete):** All 6 P0 + CRIT-1..6, P1 waves 1-8 landed on `ruthless-audit-remediation` (557+ commits; full ledger: `.worktrees/ruthless-audit-remediation/.superpowers/sdd/progress-ruthless-audit.md`). Wave 8 (this session): P1-AI-5 `8580fad8`, P1-FE-11 `fe77d4b7`+`8e7a74df` (CI wiring), P1-FE-12 `8772fe92`, P1-FE-13 `d30c182f`, P1-OPS-1 `8c0c92a2`, P1-OPS-4 `4cc6553d`+`79b7b792`, P1-OPS-6 `a5344379` (T4/T5 still gated on prod load test), P1-OPS-7 `80d1a1ad`+rework (`f2b13a3b`,`825ba8c4`,`892bd661`). **INV gate run 2026-08-10:** INV-1/6/10 PASS; INV-2 satisfied (113 WITH CHECK migrations); INV-7 PASS (provider-layer max_tokens defaults 2048-8192; plan grep is definition-noisy); INV-8 PASS backend (`test_p1_sec1_admin_aal2.py` + `test_aal2_dependency.py`) + frontend `security-aal2.spec.ts`; INV-9/4/5/19 tests exist; INV-14 `urlTransform.test.ts`; INV-15 PASS (no hostname gating: `sentryEnabled` = DSN+PROD, covered by `src/test/sentry-init.test.ts` P1-FE-5); INV-11 PASS (requirements.txt + package-lock.json single each; uv.lock deleted); INV-20 PASS (nginx `server_name localhost` only — non-CSP); **INV-12 PASS: zero `\|\| true` masking added — pre-existing migration-revert-check guards remain (git-diff checks, not scanners); INV-13/16/17 owned by P2-R-5/P2-R-2/P2-R-1 (not P1 scope); INV-18 E2E spec not created (P2-DOCS).** Remaining P1-open items: OPS-6 T4/T5 (2 replicas — needs cold-start <60s verification + prod load), OPS-1 T5 (synthetic alert on staging), and any wave-1..7 findings whose plan boxes were never marked (they landed; boxes stale).
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:44:| P1-DB-10 index | "`push_subscriptions` missing user_id index" | **Wrong** | `push_subscriptions` HAS `push_subscriptions_user_idx` (`20260616050645_*.sql:36`). Real gap: `push_devices` lacks `(user_id, active)` index. |
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:53:| P1-DB-10/11 | dropped | Re-added | `push_devices` (user_id,active) index; `guru_memories` HNSW m=16 + `user_profiles` float8 timestamps |
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:62:## INVARIANT TABLE (must hold after this plan completes)
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:139:> **Inventory note:** the 16-policy / 13-table inventory below is still accurate as an inventory of policies lacking explicit `WITH CHECK` — but none of them is an active cross-user write vulnerability.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:163:**Impact (REVISED):** No active cross-user write vulnerability exists for these 16 policies (PG defaults WITH CHECK to USING). Real remaining exposure: leaked `service_role` key combined with admin `FOR ALL` write paths could let a non-admin actor write to `gurus`/`assistant_configurations`/`assistant_doctrines`/`communications` (defense-in-depth — fix overlaps P1-SEC-1). `push_devices` cross-user reassignment is also already blocked by the USING-default. The `20260730000000_verify_rls_with_check.sql` fix covers only 4 UPDATE policies on `conversations`/`chat_messages`/`meditation_sessions`/`user_profiles`; this migration extends explicit WITH CHECK to the remaining 12.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:314:**Root cause:** No language detection before guardrails. "ignora las instrucciones anteriores" / "पिछली निर्देश भूल जाओ" / Hinglish → regex never fires → injection reaches LLM. For a 6-locale spiritual app where users write in Indic scripts, the safety layer is effectively absent for non-English input.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:348:**Impact:** Laptop loss, cloud-sync backup, or accidental `git add -f` leaks everything. `cookies.txt` is an active YouTube session. Stale dumps (20+ days) risk restore-from-old.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:477:- [ ] **T2.** Confirm a production build (or `IS_PRODUCTION=true`) has **no** import-time side effect from `TestAuthStrategy` registration. Add a test `test_testauth_not_registered_in_prod` that sets `IS_PRODUCTION=true`, builds the strategy list, asserts `TestAuthStrategy` absent.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:522:- [ ] **T4.** Grep-gate: `grep -rn "generate\|complete\|chat" backend/services/llm_gateway.py backend/services/multi_provider_llm.py | grep -v max_tokens` → every call path has `max_tokens`.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:634:> **Self-review correction:** This finding is **wrong**. `check_persona_adherence` runs **before** the fast/tier2 reflect-bypass at `verification.py:113-115`. Fast-tier answers already get the persona critic; there is no missing critic. **Removed from scope. Do not implement.** No removal of signal — the earlier draft erroneously claimed the critic was absent because it read the bypass at `:103-111` without reading the preceding `:113-115` guard.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:975:- [x] **T2.** Move `preludeText` to `t('chat.proactivePrelude.*')`.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:988:- [x] **T2.** In `:259-269`, track whether this instance started the current utterance (via `utteranceRef`) and only cancel if it owns the active utterance.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1038:### P1-DB-2 — cleanup_inactive_user_data.py fix (profiles.last_active_at)
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1039:**Evidence:** `backend/scripts/ops/cleanup_inactive_user_data.py:70` — queries `users.last_active_at` (doesn't exist; it's `profiles.last_active_at`).
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1040:**Impact:** Targeted purge is dead code. Fallback purges by `updated_at` regardless of user activity → can delete active users' memories (violates Tier-3 "protected while active" guarantee).
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1043:- [ ] **T1.** In `cleanup_inactive_user_data.py:70`, change `supabase.table("users").select("id").lt("last_active_at", cutoff)` to `supabase.table("profiles").select("id").lt("last_active_at", cutoff)`.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1045:- [ ] **T3.** Write `backend/tests/test_cleanup_inactive.py`:
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1046:  - `test_active_user_not_purged`: user with `last_active_at` recent but `updated_at` old → not purged.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1047:  - `test_inactive_user_purged`: user with `last_active_at` > 365d → purged.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1087:**Evidence:** `cleanup_inactive_user_data.py:141-159` purges only `chat_responses`. 8 telemetry tables (`chat_queries`, `retrieval_events`, `trace_spans`, `trigger_events`, `safety_events`, `app_logs`, `token_usage`, `router_decisions`) have no retention.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1091:- [ ] **T1.** Extend `cleanup_inactive_user_data.py` to purge all 8 telemetry tables with a 90d cutoff.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1104:- [ ] **T1.** Add to `cleanup_inactive_user_data.py`: `DELETE FROM guru_session_summaries WHERE user_id IS NULL AND created_at < now() - interval '30 days'`.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1115:- [ ] **T2.** Create `supabase/migrations/20260804000005_reconcile_user_brain_keys.sql` that `ALTER TABLE` adds the missing columns (whichever set is absent). Drop the duplicate migration from the history (or document that it's a no-op).
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1144:> **Self-review correction:** Earlier draft claimed `push_subscriptions` lacks a user_id index — **wrong**. `push_subscriptions` (web push) HAS `push_subscriptions_user_idx` (`20260616050645_*.sql:36`). The table that **lacks** a user_id index is `push_devices` (mobile FCM/APNs). Unlike `push_subscriptions`, `push_devices` is queried in `push_service.py` by `active` + optional `user_id` (`:231-234`) with only a `(platform, token)` unique index (`20260713000000_create_push_devices.sql:15-16`).
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1145:**Evidence:** `supabase/migrations/20260713000000_create_push_devices.sql:3-16` (only `push_devices_uq_token (platform, token)`), `backend/services/push_service.py:228-242` (`_fetch_tokens` filters `.eq("active", True)` + optional `.eq("user_id", user_id)`).
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1148:- [ ] **T1.** `CREATE INDEX IF NOT EXISTS idx_push_devices_user_id_active ON public.push_devices(user_id, active);`
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1158:- [ ] **T2.** **Don't** hot-migrate `user_profiles` float8 → timestamptz in prod (data-loss risk). Instead: add a generated/derived column `last_active_at_ts timestamptz GENERATED ALWAYS AS (to_timestamp(last_active_at/1000)) STORED` if the code reads it; otherwise document as tech-debt in `README` and fix on next schema major bump. Mark UNVERIFIED whether the app relies on float8 semantic.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1299:  - `test_tasks_complete_before_response`: tasks awaited before response sent.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1313:**Acceptance:** Runbook exists. First drill completed + documented.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1373:- P2-MED-36: **RESOLVED (verified 2026-08-04)** — `push_subscriptions` (web push: endpoint/p256dh/auth, written by `src/hooks/useWebPush.ts:93,116`, read by edge fns `push-send`/`daily-teaching-push`) and `push_devices` (mobile FCM/APNs, written by `PushNotificationsManager.tsx:33` → `backend/app/api/push.py:register_device` → `push_service.register_device` upsert `push_service.py:166-170`, read by `_fetch_tokens`) are **two distinct tables for two distinct channels — NOT a naming bug.** No cross-table write-path confusion. Two real gaps found instead: `push_devices` RLS UPDATE policy missing WITH CHECK (now CRIT-2 #16) and `push_devices` missing `user_id,active` index (P1-DB-10). `push_devices` is deliberately written via service_role (`push_service.py:34-40` `_supabase_client()` uses `settings.supabase_key`) — confirm `settings.supabase_key` in prod is the **service_role** key, not the anon key, else the upsert silently fails RLS. Add a config-assert: `test_push_uses_service_role_key`.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1436:**Coverage rule:** any source file with a finding listed in the audit that has **no** plan ID above is a gap — audit is not complete until the matrix covers every finding. If a specialist sweep produced a finding not mapped here, add it before starting Phase 0.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1581:After all P0 + P1 complete, run this gate. All must pass.
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1603:grep -rn "generate\|complete\|chat" backend/services/llm_gateway.py | grep -v max_tokens | grep -v "^#" && echo "FAIL: INV-7" || echo "PASS: INV-7"
.claude/tasks/RUTHLESS_AUDIT_REMEDIATION_PLAN.md:1654:- **Full NeMo Guardrails integration** — `GUARDRAILS_PROVIDER=lightweight` is the runtime default. NeMo is available but not active. Don't activate it in this plan; the multilingual fix (CRIT-5) runs on the lightweight handler.
