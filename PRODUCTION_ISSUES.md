# AskMukthiGuru Production Issue Register

**Last updated:** 2026-08-22  
**Source of truth:** current repository state, Railway telemetry, deployment logs, live smoke evidence, and attached Master Mission requirements. Historical audit notes remain provenance and are not treated as current proof.

This register intentionally separates **verified**, **partially verified**, **blocked**, and **not yet verified** findings. It is not a claim that every historical checklist item remains open; stale items must be rechecked against current source and production evidence before closure.

| ID | Severity | Category | Affected area | Evidence | Root cause / interpretation | Business and technical impact | Recommended fix | Owner | Status | Verification evidence / regression status |
|---|---|---|---|---|---|---|---|---|---|---|
| COST-001 | P0 | FinOps / capacity | Railway project | Latest recorded usage `$29.6933` against `$30` hard limit; forecast `$55.2651`; memory is `$27.9814` / `94.1%` | Always-on backend and database memory floor is the dominant billable resource | Cost limit can be reached before broad traffic; production benchmark capacity is constrained | Run staging/blue-green memory attribution by initialization phase; measure cost per successful grounded answer; keep broad production load disabled | FinOps + Performance | OPEN | Latest billing snapshot remains near the hard limit; post-release point snapshot is recorded in `railway_current_memory_artifacts_2026-08-22.txt`; no invoice reduction proven |
| QUAL-001 | P0 | RAG quality | Peace/stillness meaning queries | The retrieval timing instrumentation introduced a final-return `NameError`, forcing empty/error results; after repair, Hindi `शांति का अर्थ क्या है?` was `5/5` HTTP 200 and grounded at faithfulness `0.80`, while Telugu `శాంతి అంటే ఏమిటి?` was `4/5` grounded and `1/5` honest abstention | The return-contract defect is fixed and the abrupt 38-character refusal is not reproduced for the held Hindi prompt; authoritative multilingual retrieval quality is still not established | Exact Hindi UX improved, but one Telugu run still needed a citation-free reflective fallback and broader source quality remains open | Preserve the narrow no-context reflective branch, add held-out multilingual evidence/citation benchmarks, and inspect remaining zero-doc candidates | Backend + QA | MITIGATED / OPEN | Commits `4a17853` and `0bb0782`; deployment `f1d6d75e-2e83-48f6-b269-2a940f7bff91`; scoped Railway suite `83 passed, 2 skipped`; Hindi post-fix metadata probe `railway_hindi_post_retrieval_fix_2026-08-22.json`; Telugu probe `railway_telugu_post_retrieval_fix_2026-08-22.json` |
| PERF-001 | P1 | Performance / concurrency | `/api/chat` | Concurrent post-deploy smoke showed roughly `18–23s` wall-clock while internal route timings were much lower | Queueing, connection, cold-start, or concurrency interaction is not isolated | User-perceived latency remains too high; higher concurrency can increase cost without proportional throughput | Capture admission, quota, history, queue, dependency, and pipeline spans; compare serial versus bounded concurrency in a non-production target | Performance + Observability | OPEN | Health is healthy; root cause not yet proven |
| MEM-001 | P1 | Deployment / memory | Backend model startup | Latest read-only snapshot: `VmRSS=2,946,116 kB`, `VmHWM=3,848,128 kB`, cgroup current `5,926,916,096` bytes, HF cache `2.7G`; CPU reranker still absent | Image does not contain all expected model artifacts or cache verification is stale | Cold-start latency and memory spikes; repeated downloads may add network/startup work | Reconcile Docker bake and runtime cache paths; verify pinned artifacts in a test image; do not switch embedding backend without held-out proof | DevOps + Performance | OPEN | Snapshot `railway_current_memory_artifacts_2026-08-22.txt`; point measurements do not prove sustained billing reduction |
| DATA-001 | P1 | Retrieval / packaging | OKF and doctrine lexicon | Latest image inspection still reports `/app/memory/okf/compiled.json` absent and `/app/data/doctrine_lexicon.json` absent; no reranker files found in inspected cache paths | Runtime archive/image does not contain optional curated artifacts | Curated evidence injection may be absent, reducing quality and causing unnecessary fallback work | Rebuild/package artifacts through the approved reingestion process; never create empty placeholders or modify `scripts/ingestion/corpus/` | Data + DevOps | OPEN | Read-only snapshot `railway_current_memory_artifacts_2026-08-22.txt`; artifact contents not fabricated |
| COST-002 | P1 | Provider accounting | OpenRouter | Provider cost may be present, estimated, or missing; production model override is Gemini 2.5 Flash | Previous fallback table did not cover the deployed model and unknown cost could look like zero | Spend dashboards and budget decisions could be wrong | Implemented model-aware fallback rates plus actual/estimate/unknown/cache-token metrics | FinOps + Backend | VERIFIED | Commit `34c98a5`; deployed backend `888e9f55-a087-4680-ab45-a5fa215fa7d1`; static gates and healthy smoke passed |
| DB-001 | P1 | Database / schema | Neo4j | Three uniqueness constraints and owned indexes are ONLINE and populated; legacy index script lacks maintenance safeguards | Required lookup indexes exist; speculative ordinary-index mutation is unsafe | Mutation could increase memory/storage or alter plans without proven savings | Keep schema unchanged; create a lock/snapshot/timeout/rollback maintenance path before any mutation | Database | VERIFIED / MUTATION BLOCKED | Read-only remote schema and EXPLAIN evidence recorded in Neo4j audit |
| GRAPH-001 | P1 | Graph scalability | Graph/vector fusion | Source topology has bounded concurrent read-only branches, but no valid graph-on/off held-out A/B | Parallelism overlaps work but does not guarantee lower billable work | Higher concurrency may increase peak memory/DB work without improving cost per useful answer | Benchmark graph-on/off with equal workload and quality/cost gates before widening concurrency | Architecture + Performance | OPEN | Source verified; performance gain not proven |
| TEST-001 | P1 | QA / CI | Local dependency-complete tests | `pytest`, `pydantic-settings`, and `langgraph` are unavailable in sandbox | Environment is not dependency-complete under the no-install constraint | Some critical tests and dynamic graph compilation cannot be executed locally | Run locked tests in CI or the production/test image and attach output to release evidence | QA + DevOps | BLOCKED LOCALLY | `py_compile`, regex safety, diff, and corpus guards pass; pytest not claimed |
| E2E-001 | P1 | Browser / UX | Custom domain and authenticated flows | Custom domain browser bridge was previously unavailable; authenticated Second Brain E2E and full responsive verification remain open | Live browser/session evidence is incomplete | Critical user journeys may still contain unverified failures | Run authenticated browser journeys on the connected user browser when available; record network, console, persistence, and mobile evidence | UX + QA | NOT VERIFIED | Lovable route smoke exists; complete browser evidence absent; no new browser publication evidence in this release |
| OPS-001 | P1 | Recovery / operations | Railway release and backup | Deployment is reproducible by clean archive; rollback and restore drills are only partially evidenced | Release path exists, but independent recovery proof is incomplete | Incident recovery may be slower or fail under pressure | Run a safe rollback/readiness drill and a restorable backup test in a non-destructive target | DevOps + SRE | PARTIAL | Combined clean-archive deployment `f1d6d75e-2e83-48f6-b269-2a940f7bff91` reached `SUCCESS` and health reached ready/healthy; full restore proof remains open |

## Closure rule

An issue may be closed only when the register records the changed file or configuration, exact command or browser action, test result, production evidence where applicable, and an independent verification result. A theoretical saving, source inspection, or healthy readiness probe is not sufficient to close a cost, performance, or quality issue.


## Latest Railway end-to-end evidence — 2026-08-22 16:00–16:30 UTC

- Backend deployment `fc45e021-d08f-4c02-a8c4-dc13c2b7f58e` reached `SUCCESS`; readiness eventually returned `ready=true`, `status=healthy`, with the expected transient startup interval. The reranker is still not baked into `/app/model_cache`; startup logs report a first-use download risk.
- Dependency-complete tests executed inside the Railway image: the focused set reached **86 passed, 3 skipped**. The first broad non-live collection was intentionally stopped by missing root ingestion/ops files that are excluded from the serving image (`test_batched_ingest_pipeline.py`, `test_context_graph_expansion.py`, and `test_regex_safety_scanner.py`); this is a test-packaging boundary, not a production runtime pass.
- Five repeated Telugu peace queries after the multilingual fallback release were all `200`, grounded, faithfulness `0.80`, with one evidence item each. Chat time was approximately `4.9–6.1s` for four warm runs; one run reached `17.5s`, confirming a remaining cold/queue tail. The fallback remains abstained and citation-free only when the evidence-aware refusal path actually fails.
- A warm-token concurrent probe measured greeting `1.64s`, Hindi `10.10s`, Telugu `10.12s`, and safety `7.42s`; the sequential warm-token control measured greeting `1.48s`, safety `1.26s`, Hindi `5.13s`, and Telugu `5.66s`. Anonymous-session issuance itself measured about `4.0–4.7s` under concurrent setup. These measurements separate session overhead from chat latency but do not yet isolate Railway edge, admission, provider, or database spans.
- Live backend RSS after the latest deployment was approximately `2.26 GiB` with a high-water mark around `3.74 GiB` in `/proc/1/status`; cgroup current was about `7.54 GB`, while `/app/.cache/huggingface` occupied `4.4 GB` on disk. The discrepancy requires memory-map/page-cache attribution before any model or cache removal. No memory reduction is claimed.
- Current Railway usage snapshot: `$29.6933` against the `$30` hard limit, estimated bill `$55.2651`, memory `$27.9814`, CPU `$1.3618`, volume `$0.2709`, egress `$0.0791`. Memory remains the dominant scaling blocker.
- No corpus files, global Redis state, Neo4j schema, embedding backend, RRF/DBSF policy, or broad graph concurrency were changed during this evidence wave.


## Cache-pruning and Railway regression result — 2026-08-22 16:50–17:00 UTC

The FlagEmbedding-only cache maintenance was deployed in `de020c1e-23a5-4ea8-a80e-87ebb6b79fdd`, followed by the comparison-scope correction and test-fixture release in `3b499c53-da32-4b32-be95-68b117dfe0b2`. The final image reached `SUCCESS` and `/api/health` returned `ready=true`, `status=healthy` after the expected warm-up interval.

The cache cleanup removed the unused BGE-M3 ONNX snapshot from the serving cache while preserving `pytorch_model.bin`. The live Hugging Face cache fell from approximately `4.4G` to `2.3–2.7G` across post-rollout observations. `/proc/1/status` reported RSS around `2.78–3.02 GiB` and cgroup current around `4.26–4.40 GB`; this is a measured footprint improvement, but a sustained Railway billing reduction is not yet proven.

The dependency-complete focused Railway suite completed **105 passed, 3 skipped** across embedding lifecycle, fallback formatting, OKF layout, answer-path regressions, generation, pipeline fallbacks, latency shortcuts, and text quality. A broad all-tests collection remains intentionally split because serving images exclude root ingestion and security/ops scripts; the omitted tests are repository/CI checks rather than runtime regressions.

Post-prune logs still report that the CPU reranker is not baked into `/app/model_cache/sentence_transformers` and that OKF/doctrine curated runtime artifacts are absent. No empty replacement artifacts were created. The reranker packaging and approved OKF rebuild remain open blockers.

## Latest post-detector and browser evidence — 2026-08-22 17:00–17:15 UTC

The native-language peace-meaning detector release `a88a9362-a256-49c1-b654-6a9a8e314b72` reached `SUCCESS`; readiness returned `ready=true`, `status=healthy`. The targeted Railway suite completed **27 passed, 2 skipped**. A five-run production probe for the exact Telugu query `శాంతి అంటే ఏమిటి?` returned **5/5 HTTP 200, 5/5 grounded, faithfulness 0.80**, with internal latency `3.65–4.49s` and wall time `5.09–5.72s`. This closes the observed native-language detector miss for this exact phrase, but it does not close the broader multilingual or retrieval-quality benchmark.

A fresh public Lovable browser conversation remained visually functional and showed the expected processing state, controls, and support labels. The completed current turn did not expose the normalized fallback answer body in extracted text, while older entries still showed the historical short refusal. Therefore the Lovable-hosted bundle still lacks independent proof of the repository’s authoritative SSE-final integration. No authenticated or destructive browser action was taken.

A live serving-image audit on the healthy backend found `/app/memory/okf/compiled.json` absent, `/app/data/doctrine_lexicon.json` absent, and no matching CPU reranker directory under `/app/model_cache` or `/app/.cache/huggingface`. These warnings are now independently confirmed by both startup logs and filesystem inspection. No placeholder artifacts were created; approved artifact compilation and reproducible reranker packaging remain gated work.

The final non-destructive operational check returned Railway `/api/healthz` HTTP 200 with `status=alive`, and `/api/health` HTTP 200 with `ready=true`, `status=healthy`, and no startup error. `api.askmukthiguru.com` still failed DNS resolution from the verification environment, so the custom-domain gap remains open; no DNS record was changed.

## Targeted cache flush — 2026-08-22

The approved flusher ran inside the healthy Railway backend. It deleted and recreated only `mukthi_semantic_cache_1024d` and `semantic_query_cache`; Redis reported zero matching keys for both `mukthiguru:cache:*` and `mukthiguru:semcache:*`. The verifier then reported zero keys in the protected queue, session, quota, telemetry, and memory patterns. The recreated Qdrant collections were confirmed present. This was not a Redis `FLUSHALL`; user memory, queue state, sessions, quotas, telemetry, rate limits, and corpus data were not flushed. The Railway health endpoint remained healthy after the operation.

## Reranker-prefetch experiment and rollback — 2026-08-22

A narrowly scoped attempt to prefetch the pinned multilingual CPU reranker into the Railway image was halted. The first build failed because the installed `CrossEncoder` API does not accept `cache_folder`; the corrected build succeeded, but the runtime became unhealthy because the new cache-path configuration caused `/app/model_cache/huggingface/.locks/models--BAAI--bge-m3` permission failures and the 1024-dimensional encoder could not load. Production was restored by deploying the prior known-good FlagEmbedding-only configuration. The rollback deployment `cf5dca19-659d-440b-aaee-32c18df96280` reached `SUCCESS`, and the live backend returned `ready=true`, `status=healthy`, embedding dimension `1024`.

The attempted prefetch change was reverted in commit `74273fa`; no ONNX activation, embedding-vector change, corpus change, or user-data mutation occurred. The reranker packaging issue remains open and must be solved in an isolated test image with verified cache ownership and a build/runtime environment contract before another production attempt.

## Retrieval timing instrumentation and post-flush latency — 2026-08-22

The behavior-neutral stage-timing release `645965ed-3d81-41ae-9c76-61a624574f3c` reached `SUCCESS`; the active Railway instance was `RUNNING`. Internal SSH health returned `ready=true`, `status=healthy`, embedding dimension `1024`, and `/api/healthz` returned alive. The connected browser also reached both health routes. Command-line TLS timeouts/EOFs occurred intermittently during rollout, so public-edge propagation remains an operational observation rather than a backend readiness failure.

The release now emits `CHAT_STAGE_TIMING` and `RETRIEVAL_STAGE_TIMING` records containing only trace ID, tier, provider, state, and numeric timings; prompts, answers, memory, and raw provenance are not logged. One post-rollout Hindi smoke recorded `latency_ms=20499`, with `intent_router=1371.6ms`, `retrieve_documents=1094.3ms`, and `generate_answer=1006.4ms`, while a later internal control showed a `15562ms` result whose retrieval breakdown was `prepare=327.6ms`, `encode=522.9ms`, and `primary_retrieval=158.6ms`. This proves the coarse retrieval bucket is not always the dominant tail and that remaining time can occur in preparation, translation, graph/pipeline, provider, or finalization stages; no quality-affecting routing change was made from these samples.

The targeted cache flush did not cause an observed health or semantic failure. A post-flush sequential control returned HTTP 200 for greeting, safety, Hindi, and Telugu; Hindi reached `18.253s` wall / `17.009s` internal in one tail sample, while a five-run same-session Hindi control ranged `3.754–5.183s` wall and `2.961–4.266s` internal with all five grounded at faithfulness `0.8`. The concurrent control remained slower than target. Latency is therefore improved in some warm samples but not closed as a production gate.


## Retrieval return-contract repair and post-fix multilingual evidence — 2026-08-22

The initial scoped run against the no-context fallback release failed one regression: `test_retrieve_documents_empty_results_is_safe` returned `error="name 'documents' is not defined"`. Review of `backend/rag/nodes/retrieval.py` showed the final return path referenced `documents` after the assembled list had been consistently named `all_docs`. Commit `0bb0782` changes only that return field to `all_docs`; no ranking, graph policy, embedding, corpus, or cache behavior was changed.

The combined clean-archive backend deployment `f1d6d75e-2e83-48f6-b269-2a940f7bff91` reached `SUCCESS`. After the expected warm-up interval, `/api/healthz` returned HTTP 200 with `status=alive`, and `/api/health` returned HTTP 200 with `ready=true`, `status=healthy`, `startup_error=null`, and embedding dimension `1024`. Live SSH source inspection found both the generation no-context marker and the terminal short-circuit marker in the serving image.

The dependency-complete serving-image suite then passed **83 tests with 2 skips in 23.97 seconds** across final-answer formatting, fail-closed paths, generation, pipeline fallbacks, latency shortcuts, embedding lifecycle, OKF integrity, quality gates, and reranking fail-safe behavior. The pre-fix `42 passed, 1 failed, 2 skipped` result is retained as the regression trigger, not as the final release result.

A five-run metadata-only Hindi probe for `शांति का अर्थ क्या है?` with explicit `language=hi` returned `5/5` HTTP 200, `5/5` grounded, faithfulness `0.80`, response length `243–507` characters, internal latency `2.925–6.470s`, and wall time `3.994–7.917s`; the historical 38-character refusal was not reproduced. A five-run Telugu probe for `శాంతి అంటే ఏమిటి?` returned `5/5` HTTP 200, with `4/5` grounded at faithfulness `0.80` and `1/5` citation-free reflective abstention at faithfulness `0.0`; response length was `240–346` characters and internal latency `2.768–5.746s`. These are narrow held-prompt checks, not closure of multilingual quality or p95/p99 latency.

No transcript corpus file was modified, staged, or uploaded. No global Redis flush, Neo4j schema mutation, ONNX INT8 activation, RRF/DBSF tuning, graph-concurrency widening, or worker deployment occurred.


## Post-correctness memory and artifact snapshot — 2026-08-22

A read-only SSH inspection after deployment `f1d6d75e-2e83-48f6-b269-2a940f7bff91` reported process state `sleeping`, `VmRSS=2,946,116 kB`, `VmHWM=3,848,128 kB`, cgroup `memory.current=5,926,916,096` bytes, and `/app/.cache/huggingface=2.7G`. `/app/memory/okf/compiled.json` and `/app/data/doctrine_lexicon.json` remained absent, and no reranker files were found in the inspected cache paths. These are point-in-time operational measurements only; they do not prove sustained Railway invoice savings or justify a production cache-path change.


The post-correctness four-case concurrent control separated token issuance from chat time. Greeting took `3.007s` token issuance plus `3.215s` chat (`0ms` internal); Hindi took `2.843s` token issuance plus `6.365s` chat (`5.056s` internal, grounded `0.8`); Telugu took `2.831s` token issuance plus `6.380s` chat (`5.079s` internal, grounded `0.8`); safety took `2.871s` token issuance plus `1.087s` chat (`22ms` internal, safety redirect `1.0`). This supports the diagnosis that session issuance is a large user-perceived component and concurrent semantic turns still amplify latency. It is a bounded sample, not p95/p99 closure or justification for a concurrency setting change. Metadata artifact: `railway_split_latency_post_correctness_2026-08-22.json`.


A source-availability audit confirms that the checkout contains zero `memory/okf/*.md` reviewed entries and only the two OKF maintenance scripts; `memory/okf/compiled.json` is not present. The approved compiler would therefore have no reviewed source bundle to embed, and creating an empty or synthetic compiled index would conceal a data-integrity gap. The doctrine lexicon builder likewise requires authority and corpus source inputs that are not present as a reviewed runtime artifact in this checkout. The artifact gate remains correctly blocked pending the approved audited reingestion/package flow; no placeholders were generated.
