\n## What already exists\n\n- **ScrollToBottomFab** with unread count and 400px threshold -- already wired in ChatInterface\n- **Streaming** via `sendMessageStreaming` async generator -- already integrated with SSE parsing\n- **Response cache** (LRU, 50 entries, 5-min TTL) -- already used in ChatInterface\n- **Soul Journey tab** in ProfilePage with KPI cards (Prepared, Rate%, Favourite, Streak) and practice timeline\n- **Hero image** at `src/assets/hero-spiritual.jpg` used in HeroSection\n- **Latency roadmap** doc at `docs/architecture/latency-roadmap.md`\n\nSeveral requested features are already partially or fully implemented. This plan fills gaps and adds new capabilities.\n\n---\n\n## Phase 1 -- Shareable Wisdom Cards\n\n**New files:**\n- `src/components/chat/WisdomCardGenerator.tsx` -- A dialog/modal that takes a guru message, renders it as a styled card (golden gradient background, lotus watermark, quote text, AskMukthiGuru branding), and uses `html-to-image` to export as PNG.\n- `src/components/chat/WisdomCardPreview.tsx` -- Preview component showing the card before download/share.\n\n**Modified files:**\n- `ChatMessage.tsx` -- Add a \