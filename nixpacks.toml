[providers]
python = {}

[phases.install]
cmds = ["python -m pip install -r backend/requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python -m uvicorn backend.app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 2"
