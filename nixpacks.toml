[variables]
PYTHONPATH = "backend"
WEB_CONCURRENCY = "2"

[providers]
python = {}

[phases.install]
cmds = ["python -m pip install -r backend/requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python backend/start_railway.py"
