{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile.railway",
    "watchPatterns": [
      "backend/**",
      "memory/**",
      "railway.json",
      "backend/Dockerfile.railway"
    ]
  },
  "deploy": {
    "healthcheckPath": "/api/healthz",
    "healthcheckTimeout": 120,
    "startCommand": "python start_railway.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "drainingSeconds": 60,
    "overlapSeconds": 120,
    "limits": {
      "cpu": 4000,
      "memory": "4Gi"
    },
    "_memory_note": "Optimized memory footprint: ONNX INT8 quantized BGE-M3 (550MB) + INT8 Reranker (150MB) + In-RAM SQ Qdrant reduces working set from 5.6GB to ~1.2GB, safely fitting inside 4Gi cap.",
    "_cold_start_note": "P1-OPS-6: overlapSeconds 120 keeps old replica serving while the new one boots. EMBEDDING_BACKEND=onnx_int8 is pre-baked in Dockerfile.railway for instant sub-3s cold boot."
  }
}
